# Packet Dump Corpus

This directory contains text packet dumps used for MxO protocol research.

## mxopackets1

- Source repository: `https://github.com/Vibrantic/mxo-resurgence`
- Source commit inspected: `862d4ad470f96f8265908d0db970d13ddd57335c`
- Source path: `packets_and_assets/mxopackets1-master/mxopackets1-master`
- Imported files: `.txt` and text-like `.log*` packet dumps
- Files imported: 83
- Approximate size: 6.1 MB

The source README credits packets from HD_Neo, entilsar, medanon, sonyblack, and luzio, and credits Rajkosto for the sniffer. Binary images, game assets, and client/key material were intentionally not imported. The `.log*` files include encrypted margin handshake sections and decrypted game packet sections; the packet research tool filters for hex dump lines and protocol markers when summarizing them.

Run `scripts/packet-research.sh` to summarize this corpus and compare protocol 04 headers against the local RPC map.

For external corpora that should remain outside Git, set `PACKET_RESEARCH_EXTRA_DUMPS` and redirect the report outputs:

```bash
PACKET_RESEARCH_EXTRA_DUMPS=/tmp/mxo-warboy-extracted/warboy \
PACKET_RESEARCH_JSON=/tmp/packet-research-with-warboy.json \
PACKET_RESEARCH_MARKDOWN=/tmp/packet-research-with-warboy.md \
scripts/packet-research.sh
```

External packet samples are labeled by root name, such as `warboy (external)/ProxyM1/m1_packets.log`, so reports do not depend on the temporary extraction path.

Use `docs/PACKET-RESOURCE-LEADS.md` and `docs/PACKET-CAPTURE-OUTREACH.md` when requesting additional logs from preservation contacts.

## mxopackets2 Warboy/Rajko archive

- Source repository: `https://github.com/Vibrantic/mxo-resurgence`
- Source path: `packets_and_assets/mxopackets2-master/mxopackets2-master`
- Source archive: `https://raw.githubusercontent.com/Vibrantic/mxo-resurgence/master/packets_and_assets/mxopackets2-master/mxopackets2-master/warboy.zip`
- Archive SHA-256: `3dde9509d4f1b97e09938315dc591acfde2e32c5f4f1fb3153ce738f816f6341`
- Git blob SHA: `e19d7f56cb13e1cc6301ab359dbd321cefb2620e`
- Compressed size: 46,083,275 bytes
- Uncompressed size: 365,169,730 bytes across 12 `.log` files
- Triage report: `research/packet-dumps/public-mxopackets2-warboy-triage.md`

The source README describes this as MxO packets from Rajko and Warboy and credits Rajkosto for the sniffer. The extracted logs are intentionally not committed here: two individual logs are larger than GitHub's 100 MB file limit, and the archive expands to roughly 365 MB. Keep the original archive outside the repository and use the tracked triage report for reproducible evidence until a storage policy for large packet corpora is chosen.

Use `scripts/run-warboy-packet-research.sh` as the preferred repeatable workflow for this archive. The script downloads or reuses `warboy.zip`, verifies the expected SHA-256 when an archive is used, rejects unsafe archive paths before extraction, writes scratch reports under `data/experiment_artifacts/packet-corpora/warboy/reports/` by default, and runs `scripts/packet-research.sh` with stable `warboy (external)` report labels. If the archive is already extracted, use `--source-dir /path/to/warboy --work-dir /tmp/mxo-warboy-script-test` to rerun the triage and Warboy-inclusive packet report without downloading.

The last full-archive 2026-06-08 refresh of that ignored scratch workflow, before the selected Warboy derivative was imported into the checked-in corpus, covered the checked-in corpus plus the external Warboy extraction: 272 packet-like files, 2,681,011 packet-like lines, 857,696 protocol 03 object views, 429 framed protocol 04 interaction payloads, and 2,466 protocol 04 server payload shape samples. Its selector-`ff` repeat-list samples preserve 86 vendor-header windows: 50 `SERVER_VENDOR_OPEN`, 29 `CLIENT_VENDOR_SELL`, and seven `CLIENT_VENDOR_BUY`. The active Warboy-inclusive priority queue is deep-body boundary candidates with 28 targets / 29 windows, mid-body boundary candidates with 13 targets / 15 windows, one short-span boundary candidate, modeled field overlaps with five targets / 32 windows, and near-position overlaps with nine targets / nine windows. The current probe/action split suppresses all 50 continuation/pre-position probe rows as packed resource-field, lower-confidence low-half resource, or high-half header-byte collisions until new capture context contradicts that model.

The tracked triage report confirms the archive is high value for the current decoder work: 207 vendor-open signature hits, 105 vendor-buy hits, 112 vendor-sell hits, 3,321 Object599 creation-prefix hits, 31,339 object/state bundle hits, movement selector/tail coverage, ability load/unload signatures, and effect-id evidence including adrenaline booster and fast healing IDs.

The Warboy-inclusive packet research report also exports Object599 / `NPC_BASE` dynamic-tail summaries as JSON. The larger corpus expands the remaining unbounded nested dynamic creation tail evidence to 122 rows: 118 header-like object-view body leads and four zero-extended `04 ... 16 80` leads. It also adds 98 terminal five-byte zero tails, 301 bounded next-`NPC_BASE` dynamic object-view tails, body-anchor rows that split selector-body bytes before and after embedded `cd ab` separators, and extended-boundary interpretations that split 57 candidates into 11 `cd ab` separator collisions, 22 post-separator later-header candidates, and 24 other later-header candidates. This gives the next body-length parser work exact offsets and prefixes without committing the full extracted archive.

A scratch `tools/PacketResearch` run against the committed corpus plus an external extraction of this archive produced 1,712,918 packet-like lines across 95 files after mapping Warboy `Client->WORLD` / `WORLD->Client` frame labels to the normal game client/server directions. That direction-fixed run extracted 607,278 protocol 03 object views, 100 protocol 04 interaction payloads, 1,187 additional protocol 04 server payload shape samples, and 282 static object correlations after including selector-`ff` `u32 +9` field candidates in the static-object candidate load. Three framed Warboy static interaction ids also join to tracked vendor inventory rows: `277874480` (`ElevatorPanel`, 14 interactions), `277874441` (`Door Hell Club Elevator`, 11 interactions), and `959447528` (`Stance Site Invis Bench`, two interactions).

The normal report path surfaced embedded vendor packet signals from Warboy: 105 `CLIENT_VENDOR_BUY`, 113 `CLIENT_VENDOR_SELL`, and 210 `SERVER_VENDOR_OPEN` byte-window hits. The protocol 04 block parser still reports zero parsed top-level vendor RPC hits for all three, and context grouping places those bytes mostly inside server protocol 03 object/state frames or unknown packet bodies, so treat them as high-priority packet-boundary targets rather than decoded vendor exchanges.

The Warboy-inclusive report also groups the immediate bytes around those embedded hits and joins them to the containing protocol 03 object-view segment when packet offsets are available. Its strongest repeated `SERVER_VENDOR_OPEN` windows sit in server protocol 03 unknown-line contexts, commonly with `00 00 00 00 e0 2d` before `81 0d` and `22 89 20 00 0b 0c 11 79 80 00` after it. The top exact containment is a count-3 selector `0c` object view with a parsed position segment followed by selector `ff` as an unresolved payload containing the repeated window at object-relative offset `+29`; other repeated rows land in unresolved appearance/attribute or selector `2a`/`2e` tails. That stable window is a protocol 03 boundary lead, not a decoded vendor-open RPC.

The Warboy-inclusive variable-tail report broadens that evidence from immediate byte windows to selector-body families. Its top vendor-bearing family is selector `ff` in count-3 first-selector `0c` object views with lead prefix `01 43 00 00 00 00 00 00 00 e0 2d 81 0d 22 89 20`: 21 records contain `SERVER_VENDOR_OPEN` at selector-payload offset `+11` / object-relative offset `+29`. The nearby-four-byte column shows those bytes consistently live inside `e0 2d 81 0d` at payload offset `+9`, so this is probably packed selector body data rather than a clean nested vendor-open RPC boundary. The selector-`ff` packed-field table now exposes the surrounding slices and checks `u32 +9` against imported resource tables; the repeated `e0 2d 81 0d / 226569696` value and the other top Warboy selector-`ff` field families have no imported resource refs. The largest subfamily has 18 count-3/first-`0c` records with bytes `+13 = 22 89 20`, bytes `+16 = 00 0b 0c 11 79 80 00 00`, bytes `+24 = 01 00 00 00 00 12 01 00`, and bytes `+31 = 00 02 ff 00 00 00 00 ba`; smaller first-`0c` and first-`0e` rows keep the same stable fields while switching the `+24` slice to `01 00 00 00 00 1b 01 00`. The selector-`ff` position-like table now shows the same vendor-bearing family has a 24-byte little-endian xyz triple at payload offset `+51`: 16 first-`0c` rows at `17595.543,495,2447.504`, three first-`0c` rows at `17615.635,495,1816.967`, and matching first-`0e` rows. The nearest decoded-position hints make these coordinates look like object-state body anchors: the first cluster is 85.4 units from player `Se7` and 102.2 units from `Zion Duelist`, while the second is 43.7 units from player `ricky303` and 96.9 units from player `Tsurai`. The post-position tail table adds a stronger suffix target: all 27,716 Warboy selector-`ff` position leads with bytes after the 24-byte position start their top groups with post-position `+0 = ff`, ten zero bytes, and a little-endian `u16` at first nonzero offset `+11`. The repeat-stride scan shows 25,393 of those suffixes expose at least two `ff` markers at a consistent stride, with stride `18` dominating the visible top groups, and 25,245 repeat tails expose a local field inside entry 2. The largest first-`0c` repeat groups keep `ba 13 / 5050` as the first record field, then split into second-entry zero-run 5 / local field `+6 = 66 00 / 102` across 1,133 records and a broader zero-run 0 / local field `+1 = ff 00 / 255` family across 708 records. The repeat-entry transition table pairs all 25,393 repeat-stride tails, and the matrix adds 50,786 observations across the first two consecutive entries: entry 1 preserves zero-run 10 / local field `+11`, while entry 2 carries the `66 00`, `ff 00`, and smaller variant buckets that the next state-list parser needs to resolve. The continuation table shows every paired Warboy tail has bytes after offset `36`, with visible prefixes like `00 41 00 ff`, `00 00 00 ff`, `00 01 00 ff`, and `00 01 04 ff`; that points to a two-entry list followed by a separate continuation body rather than a third repeated `ff` record. The continuation-field table confirms all 25,393 continuation bodies with at least four bytes have local byte `+3 = ff`, then splits useful u16 windows at offsets `+4`, `+6`, `+8`, `+10`, `+12`, and `+14`; representative groups include `00 41 00 ff` with `b0 74 / 29872` at `+8` and `08 00 / 8` at `+14`, `00 00 00 ff` families that often stay zero until `+14 = 08 00 / 8`, and `00 01 00 ff` / `00 01 04 ff` variants with nonzero `+4`, `+6`, and `+8` fields. The marker-tail table narrows that further: all 25,393 bodies expose a post-marker u16 after local `+3 = ff` after full-continuation parsing, split between immediate `+4` fields, four-zero `+8` fields, and ten-zero `+14` fields. The prefix-family table collapses those marker bodies into 532 global families; the largest are `00 01 00 ff` immediate `+4 = f0 0f / 4080` (912 bodies), `00 01 00 ff` immediate `+4 = fd 0f / 4093` variants (714 and 692 bodies), `00 01 04 ff` four-zero `+8 = f2 22 / 8946` (666 bodies), `00 01 10 ff` four-zero `+8 = 12 c9 / 51474` (598 bodies), and `00 01 04 ff` immediate `+4 = f7 0f / 4087` (548 bodies). The repeat-list candidate table confirms all 25,393 marker continuations have exactly two stride-18 entries before continuation offset `36` and expose a post-marker u16; the largest combined shapes are `ba 13 -> ff 00 -> 00 01 00 ff/+4 f0 0f` (912), `ba 13 -> 66 00 -> 00 01 04 ff/+8 f2 22` (666), `01 00 -> ff 33 -> 00 01 00 ff/+4 fd 0f` (644), `7a 14 -> 66 00 -> 00 01 04 ff/+4 f7 0f` (444), and `ba 13 -> 66 00 -> 00 00 00 ff/+8 3f 24` (436). The global field-value summary collapses the same 25,393 candidates into 76,179 original role observations: entry 1 is led by `ba 13 / 5050` and `01 00 / 1`, entry 2 by `ff 00 / 255` and `66 00 / 102`, and the continuation post-marker by `fd 0f / 4093`, `f2 22 / 8946`, `3f 24 / 9279`, and `f0 0f / 4080`. The Warboy JSON exports this as 2,621 `Protocol03SelectorFfRepeatListFieldSummaries` groups, split into 1,857 entry-1 groups, 245 primary entry-2 groups, 298 effective entry-2 groups, and 221 post-marker groups; 2,422 groups now have imported resource references. Entry-1 references are dominated by clothing item/game-object IDs with animation-symbol collisions (`ba 13 / 5050` -> `Fclothing_Shirt_A13_C7` / "Orange/Yellow Sif Leather Fighting Dress" / `NPCDJAtTurnTable_Idle4`; `7a 14 / 5242` -> `Fclothing_Shirt_A19_C26` / "Deep Magenta Knight Leather V-Neck Top" / `AD_ADLb_Aggro`). The new effective entry-2 role keeps `66 00 / 102` visible as both 5,064 zero-padded primary observations and 4,679 secondary-sourced marker-adjacent observations, with both groups matching the imported animation symbol `AD_Arc_L40`. Post-marker leaders split between animation refs (`fd 0f / 4093`, `f0 0f / 4080`) and clothing item/game-object refs (`f2 22 / 8946`, `3f 24 / 9279`). Vendor-bearing coordinate clusters keep `a0 13 / 5024`, `b9 13 / 5049`, or `ba 13 / 5050`. Treat all resource names as candidate labels until gameplay context proves the field semantics.

The same Warboy scratch JSON now exports 3,567 `Protocol03SelectorFfRepeatListShapeSummaries`, each grouping the full entry-1 -> entry-2 -> continuation -> post-marker shape and carrying at least one imported resource reference. The top shapes are `ba 13 / 5050 -> ff 00 / 255 -> 00 01 00 ff -> f0 0f / 4080` (912), `ba 13 / 5050 -> 66 00 / 102 -> 00 01 04 ff -> f2 22 / 8946` (666), `01 00 / 1 -> ff 33 / 13311 -> 00 01 00 ff -> fd 0f / 4093` (644), `7a 14 / 5242 -> 66 00 / 102 -> 00 01 04 ff -> f7 0f / 4087` (444), and `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff -> 3f 24 / 9279` (436). The normalized effective-shape export adds 3,662 Warboy groups keyed by effective entry-2 value; the leading row is `ba 13 / 5050 -> 66 00 / 102 -> 00 01 00 ff -> f0 0f / 4080` with 878 candidates, all secondary-sourced from primary `ff 00 / 255`. The new field-layout export separates Warboy entry 2 into 11,997 zero-padded u16 candidates, 10,754 marker-adjacent u16 candidates, 2,494 immediate u16 candidates, and 148 no-field candidates. The layout-prefix roll-up adds one checked-in group and 99 Warboy groups; the largest Warboy group is long-zero entry 1 / marker-adjacent entry 2 / `00 01 00 ff` / immediate post-marker with 3,308 candidates across 235 exact shapes. The secondary-slot export shows 15,047 Warboy entry-2 records have a nonzero local `+6` field, led by `ff 00 -> 66 00` (3,179). The effective-field export now normalizes marker-adjacent entry-2 layouts to that secondary slot when present: 10,754 Warboy candidates use the secondary source, and `66 00 / 102` becomes the dominant effective entry-2 value with 9,743 observations. Treat these rows as object-state boundary and resource-label targets, not final decoded semantics.

The Warboy scratch JSON also exports `Protocol03SelectorFfRepeatListEffectiveShapeHeaderSummaries`, joining normalized repeat-list shapes to local two-byte RPC-header windows. Warboy has 892 known-header shape groups covering 2,843 candidate instances, including 81 candidate instances with vendor-header windows. Top vendor-correlated shapes include `ba 13 / 5050 -> 21 0a / 2593 -> 00 41 00 ff -> b0 74 / 29872`, where all 12 candidates carry `SERVER_VENDOR_OPEN` at selector-payload `+11` / object `+29` inside `e0 2d 81 0d`, and `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff -> 1b ee / 60955`, where all nine candidates carry vendor windows led by `CLIENT_VENDOR_SELL` at `81 11`. The companion `Protocol03SelectorFfRepeatListHeaderNameSummaries` rollup inverts this by header name: Warboy has 89 header-name rows and three vendor rows, with `SERVER_VENDOR_OPEN` at 49 candidates / 49 windows / 15 shapes, `CLIENT_VENDOR_SELL` at 27 / 27 / 11, and `CLIENT_VENDOR_BUY` at 5 / 5 / 5. Treat these as packet-boundary leads, not decoded nested RPCs.

`Protocol03SelectorFfRepeatListHeaderSamples` keeps bounded byte slices for those header-window correlations. The checked-in corpus exports 28 non-vendor samples; the Warboy scratch JSON is capped at 120 samples and includes all 81 vendor de-duplicated windows, split into 49 `SERVER_VENDOR_OPEN`, 27 `CLIENT_VENDOR_SELL`, and five `CLIENT_VENDOR_BUY` samples. Each row keeps source file/line, payload and object offsets, `HeaderRegion`, header context bytes, repeat-list context bytes, normalized shape fields, and decoded position anchors. The Warboy vendor region split is 28 `field +9 u32`, 27 `continuation body`, 22 `pre-position body`, and four `repeat entry 1` hits.

`Protocol03SelectorFfRepeatListHeaderRegionSummaries` makes that split queryable without expanding every sample. Warboy has 207 header-region rows total and 10 vendor rows: `SERVER_VENDOR_OPEN` spans `field +9 u32` (28 windows), `continuation body` (12), `pre-position body` (8), and `repeat entry 1` (1); `CLIENT_VENDOR_SELL` spans `pre-position body` (13), `continuation body` (12), and `repeat entry 1` (2); `CLIENT_VENDOR_BUY` spans `continuation body` (3), `pre-position body` (1), and `repeat entry 1` (1). The region rows now also carry resource-reference labels: all 10 vendor rows have entry-1 and effective entry-2 refs, and 9 have post-marker refs. The largest region bucket, `SERVER_VENDOR_OPEN` in `field +9 u32`, is labeled by `21 0a / 2593` for effective entry 2 and `Shield_F_Coat_A4_C5_5` for the post-marker field.

The vendor continuation/pre-position target exports now classify byte-window spans for parser work. Warboy continuation-body vendor windows split into 22 deep body tails, four mid-body tails, and one short tail after the matched header bytes; pre-position vendor windows split into nine near-position overlaps, 10 mid-body gaps, and three deep pre-position gaps. The boundary interpretation roll-up now separates those continuation rows into 21 deep-body targets / 22 windows, three mid-body targets / four windows, and one short-span target / one window, and separates pre-position rows into nine mid-body targets / 10 windows, three deep-body targets / three windows, and nine near-position targets / nine windows. Target rows and interpretation rows now also export `TargetBodySpanBytes`, such as `continuation tail 125` or `pre-position gap 53`, plus `TargetBodyHexes` with clamped local continuation/pre-position body slices around the suspect window. Treat the short-tail and near-position rows as likely byte-window collisions until a capture contradicts that, and prioritize the mid/deep unresolved rows for body-length work.

`Protocol03SelectorFfRepeatListVendorBoundaryPrioritySummaries` now turns that interpretation split into a compact work queue. Checked-in dumps have zero rows; the refreshed Warboy scratch report has deep-body boundary candidates with 24 targets / 25 windows, mid-body boundary candidates with 12 targets / 14 windows, one short-span boundary candidate, modeled field overlaps with five targets / 32 windows, and near-position overlaps with nine targets / nine windows. The next decoder work should start with the deep/mid continuation and pre-position body-length rows, then use the modeled-overlap and near-position rows as collision controls.

`Protocol03SelectorFfRepeatListVendorBodyWindowProbeSummaries` now provides the concrete body-length fixture table for that work queue. Checked-in dumps have zero probe rows; Warboy has 45 rows after excluding modeled packed-field and repeat-entry overlaps: 25 continuation-body rows covering 27 windows, and 20 pre-position rows covering 22 windows. The probe table keeps body-local offsets, `mod 2` / `mod 4` alignment, bytes before and after the two-byte header-like window, full body byte counts, body slices, payload/object offsets, four-byte little-endian windows, header `u16` values, half-window `u16` resource references, nearby `u16` windows, and simple length-match checks. Start parser tests with the repeated continuation `+39` / tail 51, continuation `+81` / tail 81, and pre-position `+137` / gap 53 rows, then keep the near-position rows as collision controls. None of the 45 Warboy probe rows has a nearby `u16` matching bytes-after-header, header-plus-tail, body offset, offset-plus-header, or full body bytes, so adjacent length-field matching does not currently explain these windows. All 45 rows have at least one half-window resource collision, but high-half matches mostly come from the header-looking `81 xx` bytes colliding with animation ids. The more useful low-half signal appears in 28 rows / 31 windows, including 23 rows / 25 windows with item refs. `Protocol03SelectorFfRepeatListVendorBodyWindowEvidenceSummaries` buckets this as 23 low-half item/gameobject collision-control rows / 25 windows, five lower-confidence low-half resource-collision rows / six windows, 17 high-half-only header-byte collision rows / 18 windows, and zero nearby length-match rows. `Protocol03SelectorFfRepeatListVendorBodyWindowParserActionSummaries` turns those buckets into three suppression actions and no active body-length-test rows: suppress packed item/gameobject field collisions, suppress lower-confidence low-half resource collisions unless new capture context corroborates them, and suppress high-half header-byte collisions. The new `Protocol03SelectorFfRepeatListVendorBodyWindowResourceCollisionSummaries` export condenses the labels into 19 Warboy collision rows, including 16 low-half rows. Repeated examples such as `e0 2d / 11744` -> `Armor_F_Shirt_A14_C2_1` and `00 28 / 10240` -> `Armor_F_Shirt_A4_C24_6` support treating many of these windows as packed clothing/state-field collisions unless body-length tests prove otherwise.

`Protocol03SelectorFfRepeatListContinuationMarkerSummaries` now turns the local continuation byte `+3 = ff` hypothesis into a compact export and joins each marker family to known-header windows. Warboy has 405 marker rows covering all 25,393 modeled two-entry candidates and all 3,662 effective shapes; every row uses marker byte `ff`. The largest prefix families are `00 01 00` (15,228 candidates), `00 01 10` (3,842), `00 00 00` (2,909), and `00 01 04` (1,441), with top concrete buckets led by `00 01 00 ff` immediate `fd 0f` (1,849 candidates / 85 shapes) and `00 01 00 ff` immediate `f0 0f` (912 / 2). Known-header evidence appears in 232 marker rows covering 2,843 candidates, and 22 marker rows carry all 81 vendor windows. The marker export now also keeps resource-reference labels for entry 1, effective entry 2, and post-marker fields: 384 Warboy rows have entry-1 refs, 386 have effective entry-2 refs, and 339 have post-marker refs. Vendor-only resource labels are split out as well: 21 vendor-bearing rows have vendor-only entry-1 refs, 21 have vendor-only effective entry-2 refs, and 16 have vendor-only post-marker refs. The strongest vendor-bearing row is `00 41 00 ff -> b0 74` with 19 `SERVER_VENDOR_OPEN` windows, vendor-only effective entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5` / "Shielded Green Farrel Cinch Jacket Highcollar"; `00 01 00 ff -> b0 74` has 16 vendor windows and the same post-marker refs.

## community-intake

`research/packet-dumps/community-intake/20260606T201844Z-hdneo-mxopackets1-vendor-marketplace-selected` contains a reviewed, targeted import from the public standalone `https://github.com/hdneo/mxopackets1` archive. It imports 11 vendor, marketplace, inventory, loot, and item-move text logs, preserves source paths and SHA-256 hashes in `SOURCE.md`, records two skipped binary/non-text vendor-session logs in `SKIPPED.md`, and keeps byte-string triage in `TRIAGE.md`.

`research/packet-dumps/community-intake/20260606T202620Z-hdneo-mxopackets1-sell-inventory-selected` contains the next reviewed public standalone `hdneo/mxopackets1` import. It imports 15 sell, vendor-use, inventory, loot, code upload, and item-move text logs, preserves source paths and SHA-256 hashes in `SOURCE.md`, records one skipped possible-secret-marker inventory session in `SKIPPED.md`, and keeps triage in `TRIAGE.md`.

`research/packet-dumps/community-intake/20260607T003822Z-hdneo-mxopackets1-combat-ability-selected` contains a reviewed public standalone `hdneo/mxopackets1` combat and ability import. It imports 12 combat, ability load/unload, hyperspeed, and individual ability-use text logs, preserves source paths and SHA-256 hashes in `SOURCE.md`, and keeps triage in `TRIAGE.md`.

`research/packet-dumps/community-intake/20260607T013506Z-hdneo-mxopackets1-marketplace-vendor-selected` contains a reviewed public standalone `hdneo/mxopackets1` marketplace and archive-vendor import. It imports 23 marketplace category, empty marketplace search, archive-vendor teleport, and vendor-adjacent movement/state text logs, preserves source paths and SHA-256 hashes in `SOURCE.md`, records two skipped non-text Trinity vendor-session logs in `SKIPPED.md`, and keeps triage in `TRIAGE.md`.

`research/packet-dumps/community-intake/20260607T014317Z-hdneo-mxopackets1-trinity-vendor-redacted-selected` contains two reviewed public Trinity vendor-session logs imported as sanitized derivatives. Password-bearing auth lines were removed, a single NUL byte was stripped from each file, the original non-text/source files remain outside Git, and `SOURCE.md` records the derivative hashes plus redaction context.

`research/packet-dumps/community-intake/20260607T015831Z-hdneo-public-inventory-lastday-rajko-nearend-selected` contains 11 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` inventory, item-move, manageable last-day, and Rajko near-end logs imported as sanitized derivatives where needed. Password-bearing auth lines were removed from affected sources, NUL bytes were stripped, the original raw sources remain outside Git, `SOURCE.md` records derivative hashes and one duplicate-hash lead, and `TRIAGE.md` keeps the focused vendor/static/object-state evidence.

`research/packet-dumps/community-intake/20260607T022135Z-hdneo-public-two-person-actions-sanitized-selected` contains two reviewed public `hdneo/mxopackets1` two-person/player-interaction logs imported as sanitized derivatives. NUL bytes were stripped, auth/margin handshake blocks through `MS_EstablishUDPSessionReply` were removed, the original raw sources remain outside Git, `SOURCE.md` records derivative hashes and redaction context, `SKIPPED.md` records the safety decision, and `TRIAGE.md` keeps the focused interaction, vendor-byte-window, Object599, and selector evidence.

`research/packet-dumps/community-intake/20260607T024558Z-hdneo-public-mission-code-rajko-worldstate-sanitized-selected` contains 15 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` mission, code-storage, Rajko, and world-state logs imported as sanitized derivatives where needed. NUL bytes were stripped, auth/margin handshake blocks through `MS_EstablishUDPSessionReply` were removed, duplicate source hashes were skipped, and `TRIAGE.md` keeps focused vendor-byte-window, static interaction, Object599, movement, ability, and state-bundle evidence.

`research/packet-dumps/community-intake/20260607T030922Z-hdneo-public-luizoe-old-sniffer-selected` contains five reviewed public `hdneo/mxopackets1` Luizoe old-sniffer recursion/trade logs. They imported without redaction or skipped files, preserve public source paths and SHA-256 hashes in `SOURCE.md`, and keep focused vendor-sell, Object599, movement, static/dynamic interaction, friend-status, ability, equipment, and effect evidence in `TRIAGE.md`.

`research/packet-dumps/community-intake/20260607T032533Z-hdneo-public-movement-spawn-hardline-sanitized-selected` contains 23 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` movement, hyperjump, loading-area, hardline, door, spawn, and Rajko near-end logs imported as sanitized derivatives. NUL bytes were stripped, auth/margin handshake blocks through `MS_EstablishUDPSessionReply` were removed, the original raw sources remain outside Git, and `SOURCE.md` records derivative hashes and source paths.

`research/packet-dumps/community-intake/20260607T191353Z-hdneo-public-combat-pvp-rajko-state-selected` contains 12 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` combat, PvP, corrupted, Rajko Mara/Cyph, and state logs imported as sanitized derivatives. NUL bytes were stripped, auth/margin handshake blocks through `MS_EstablishUDPSessionReply` were removed, the original raw sources remain outside Git, and `SOURCE.md` records derivative hashes, source paths, and redaction context.

`research/packet-dumps/community-intake/20260607T200024Z-hdneo-public-remaining-small-ability-loot-market` contains seven reviewed public `hdneo/mxopackets1` ability-loadout, evade-combat, loot, and marketplace-lockpicks text logs. They imported without redaction or skipped files, preserve source paths and SHA-256 hashes in `SOURCE.md`, and keep focused vendor, Object599, movement, ability, and effect triage evidence. The two identical loot source hashes are intentionally preserved as separate source-path provenance rows.

`research/packet-dumps/community-intake/20260607T203107Z-hdneo-public-vector-state-static-selected` contains 12 reviewed public `hdneo/mxopackets1` Vector old-world, teleport, unknown-object, and disconnect logs. They imported without redaction or skipped files; `Vector35.log` was imported as a NUL-stripped derivative, and all other files were already text-clean. The intake preserves source paths and SHA-256 hashes in `SOURCE.md` and keeps focused vendor-window, CR1/CR2 static-interaction, Object599/profile, object-state, ability/effect, and selector-`ff` movement-boundary evidence in `TRIAGE.md`.

`research/packet-dumps/community-intake/20260607T205703Z-hdneo-public-vector-supplemental-state-selected` contains 16 reviewed public `hdneo/mxopackets1` Vector, login, Syntax, and old-world text logs. They imported without redaction or skipped files, preserve source paths and SHA-256 hashes in `SOURCE.md`, and keep focused vendor-window, CR1/CR2 static-interaction, Object599/profile, object-state, selector-`ff`, ability/effect, and marketplace evidence in `TRIAGE.md`.

`research/packet-dumps/community-intake/20260607T212233Z-hdneo-public-session-spawn-movement-sanitized-selected` contains 10 reviewed public `hdneo/mxopackets1` Luizoe recursion, unsorted proxy, create/session, idle, Vector4, and Unknown traces imported as sanitized derivatives. NUL bytes were stripped, auth/margin handshake blocks through `MS_EstablishUDPSessionReply` were removed, the original raw sources remain outside Git, `SOURCE.md` records derivative hashes and source paths, `SKIPPED.md` records that no files were skipped, and `TRIAGE.md` keeps focused vendor-window, static-interaction, Object599/profile, object-state, movement, selector-`ff`, and session/create evidence.

`research/packet-dumps/community-intake/20260608T065306Z-hdneo-public-luizoe-remaining-trade-sanitized-selected` contains three reviewed public `hdneo/mxopackets1` Luizoe recursion/Syntax trade-adjacent logs imported as sanitized derivatives. NUL bytes were stripped, connection-handshake text was removed before import, the already imported `recursion1` derivative and a delete-character source that did not add usable world-packet evidence were omitted, the original raw sources remain outside Git, and `TRIAGE.md` keeps focused buy/sell byte-window, static-interaction, Object599/profile, object-state, movement, and ability/effect evidence.

`research/packet-dumps/community-intake/20260608T120313Z-hdneo-public-create-spawn-session-sanitized-selected` contains eight reviewed public `hdneo/mxopackets1` create-character, login/session, jack-in, Morpheus, and Special Agent spawn logs imported as sanitized derivatives. NUL bytes were stripped, only GAME/WORLD packet blocks were retained to remove auth/margin handshake material, the original raw sources remain outside Git, and `TRIAGE.md` keeps focused vendor-window, static-interaction, Object599/profile, object-state, selector-`ff`, and create/session evidence.

`research/packet-dumps/community-intake/20260608T122812Z-hdneo-public-endgame-mission-sanitized-selected` contains four reviewed public `hdneo/mxopackets1` endgame, mission, duality, and high-signal unsorted packet logs imported as sanitized derivatives. NUL bytes were stripped, only GAME/WORLD packet blocks were retained to remove auth/margin handshake material, the original raw sources remain outside Git, and `TRIAGE.md` keeps focused vendor-window, static-interaction, Object599/profile, object-state, selector-`ff`, mode `06`, and effect evidence.

`research/packet-dumps/community-intake/20260608T130150Z-hdneo-public-warboy-world-game-sanitized-selected` contains six reviewed public `hdneo/mxopackets2` Warboy logs imported as WORLD/GAME-only sanitized derivatives. AUTH/MARGIN blocks and known session/key marker lines were dropped, the original raw sources and full archive remain outside Git, and `TRIAGE.md` keeps focused selector-`ff`, vendor-window, static-interaction, Object599/profile, object-state, and mode `06` evidence.

The selected intakes are now included by the default `scripts/packet-research.sh` run. The public hdneo intakes provide 44 checked-in `CLIENT_VENDOR_BUY` (`81 0e`) records, 11 checked-in `CLIENT_VENDOR_SELL` (`81 11`) records, 15 parsed `SERVER_VENDOR_OPEN` (`81 0d`) records, 69 `SERVER_LOAD_MARKETPLACE` (`81 25`) marketplace payloads, top-level static/dynamic object interactions across object types `0`, `1`, `2`, `3`, `4`, `5`, `6`, `8`, `9`, `13`, and `14`, Object599 / `NPC_BASE` creation samples in vendor/loot/near-end/two-person/mission/combat-state/vector-state/vector-supplemental/session-spawn/Luizoe-trade/create-spawn/endgame-mission/Warboy context, and equipment ids such as `3d b9 00 00` / 47421, `15 b9 00 00` / 47381, `0d ba 00 00` / 47629, `ef 1a 00 00` / 6895, `c2 1e 00 00` / 7874, `d7 1e 00 00` / 7895, `6a ba 00 00` / 47722, `63 9a 00 00` / 39523, and `99 95 00 00` / 38297. The generated report now groups 70 top-level vendor transaction-flow rows: many buys are followed by `5e -> 80 e7 -> 80 e4 -> 81 0f`, the Rajko Icy Snowball buys are followed by `69 -> 80 e7 -> 80 e4 -> 81 0f` with sequence `54` and candidate `u32 +5` step fields, the Code Analyzer Tool buy lead (`13 02 00 00 00`) currently has no observed cash/value match, and sells are followed by `80 e7 -> 80 e4 -> 5f -> 81 12` or adjacent sell-neighborhood rows. Embedded vendor byte-window leads, including the latest Luizoe trade/syntax, create/spawn/session, endgame/mission, and selected Warboy hits, are treated as boundary targets; only parsed top-level RPC rows are treated as decoded vendor transactions. The current default report covers 278 packet-like files and 1,276,069 packet-like lines, with 386 top-level object-interaction payloads, 4,382 Object599 creation records, 2,790 Object12 / `PlayerCharacter` creation records, and 6,282 broad mode `06` movement windows with 4,817 repeated `ff` continuation boundary targets. It also exports 10 Object599 post-separator vector probe rows covering 19 bounded-finite `be f64 x3` candidates at post-separator offset 9, splits their pre-vector bridges into 11 two-byte and eight four-byte candidates, adds 10 post-vector tail rows that split those candidates into bounded `be f32` tail shapes, no-tail controls, and raw/outlier controls, promotes 7 rows / 15 bounded vector-tail candidates to `review bounded vector-tail body` in the post-separator layout-action rollup, emits seven reviewed body-layout rows with concrete post-separator offsets for the promoted candidates, marks those 15 reviewed bodies complete under the guarded post-separator parser rule, classifies all 15 promoted vectors as non-position body fields after comparing them to decoded parent `Position`, groups the promoted tails into 11 planar direction-like `be f32 x3` rows, two padded planar triples, and two padded bounded quads, and keeps the four held finite-vector controls searchable as two no-tail candidates, one zero raw-tail candidate, and one outlier-tail candidate that also differ from decoded parent `Position` by more than 5k units.

The price joins now give packet-backed item/price anchors for both directions. Buy payloads decode normal weapon ids, `WinterSnowballIcy` / "Icy Snowball" (`01 a8 00 00 00`, six matched `-100` cash deltas and `100` buy values), plus high-bit `AwakenedAbility`, each matching the following cash delta and `81 0f` buy value. The new `CodeAnalyzerTool` / "Code Analyzer Tool" buy lead (`13 02 00 00 00`, expected vendor price 1000) is tracked as an unmatched price lead pending adjacent value evidence. Sell result payloads now decode `Shotgun2` / "Ragaini Combat Shotgun" (`00 55`, expected sell 900), `Shield_M_Shoes_A27_C3_1` / "Shielded White and Black Two tone Dada shoes" (`00 52`, expected sell 1560), `FM-13SK` (`00 58`, expected sell 5000), and `AwakenedAbility` (`00 58`, expected sell 10), each matching the following cash delta and `81 12 +9` sell value. The vendor-open decoder parses complete `81 0d` payloads as a 32-byte header with `u16 +28` list-start field (`20 00` / 32 in checked-in samples), `u16 +30` count, and `u32[] +32` ids; the checked-in shapes cover weapon, tool/disguise, ability, and clothing inventory lists with exact or near-exact public vendor-row joins.

The combat/ability intake adds 2,429 `80 bc` object/state bundle hits in triage, 84 Object599 / `NPC_BASE` creation-prefix hits, 24 static-object interaction byte windows, combat mob rows such as `Blackwood Mushfake`, `Silver Dragon Scale`, `Silver Dragon Horn`, and Bathary Boys variants, and additional equipment/effect observations.

The marketplace/vendor intake adds 760 `80 bc` object/state bundle hits in triage, 27 Object599 / `NPC_BASE` creation-prefix hits, 26 CR1 static-object interaction hits, two CR2 static-object interaction hits, 35 repeated mode `06` suffix leads, 12 ability-load hits, and 24 ability-unload hits. It does not add new parsed top-level vendor RPCs, but it does add marketplace/archive-vendor state-bundle context and new server payload shape labels such as `EventSlot1_Effect`, `EventSlot2_Effect`, `EventSlot3_Effect`, `FixedBinkIDOverride`, `PvPMaxSafeLevel`, `PvPServer`, and `WR_RezEvents`.

The redacted Trinity vendor intake adds 610 `80 bc` object/state bundle hits in triage, two `SERVER_VENDOR_OPEN` hits, five `CLIENT_VENDOR_SELL` hits, 24 ability-load hits, and 11 ability-unload hits. Its regenerated report rows add the new `00 52` and `00 55` sell-price joins and additional Trinity social/system setting variants without importing the password-bearing source lines.

The inventory/last-day/Rajko near-end intake adds 7,150 `80 bc` object/state bundle hits in triage, 29 `CLIENT_VENDOR_BUY` hits, seven `CLIENT_VENDOR_SELL` hits, four `SERVER_VENDOR_OPEN` hits, 96 static-object interaction hits across CR1/CR2 byte signatures, 18 selector-`ff` `00 41 00 ff` continuation-prefix hits, and 2,372 selector-`ff` `00 01 00 ff` continuation-prefix hits. Its regenerated report rows add the Rajko downtown `WinterSnowballIcy` buy sequence, including six `69` buy-ack candidates with sequence `54` and `u32 +5` step fields, plus near-end state/object rows for RajkoHaxor, entilsar, Warboy, and Corrupted contexts.

The two-person/player-interaction intake adds the second and third largest parsed packet-like sources in the checked-in corpus, five `SERVER_VENDOR_OPEN` byte-window hits, one `CLIENT_VENDOR_BUY` byte-window hit, five `CLIENT_VENDOR_SELL` byte-window hits, five top-level server `80 c8`/CR2 static-object interaction signatures, one client `80 c3`/CR1 static-object interaction signature, 1,814 `80 bc` bundle hits, 317 Object599 / `NPC_BASE` creation-prefix hits, a new dynamic `80 c7` object type `8` interaction for object id `10092570`, embedded `CLIENT_VENDOR_SELL` windows in protocol 03 object-state context, and the expanded mode `06` embedded-movement boundary queue.

The mission/code/Rajko world-state intake adds six `SERVER_VENDOR_OPEN` byte-window hits, three `CLIENT_VENDOR_BUY` byte-window hits, 8,661 `80 bc` object/state bundle hits, 479 ability-load hits, 515 ability-unload hits, 150 CR1 static-object interaction signatures, 14 CR2 static-object interaction signatures, 341 Object599 / `NPC_BASE` creation-prefix hits, selector-`ff` continuation-prefix evidence, and additional mission/code state-bundle context for `80 b2`, `80 b3`, `80 bd`, `80 d7`, `81 67`, `2e`, `3a`, and related server payloads.

The Luizoe old-sniffer intake adds one `SERVER_VENDOR_OPEN` byte-window hit, two `CLIENT_VENDOR_BUY` hits, 22 `CLIENT_VENDOR_SELL` hits, 2,444 `80 bc` object/state bundle hits, 580 Object599 / `NPC_BASE` creation-prefix hits, 68 CR2 and 67 CR1 static-object interaction signatures, 75 ability-load hits, 36 ability-unload hits, and dense mode `06` movement-selector evidence. It does not change the decoded top-level vendor transaction count, but it expands embedded vendor-window controls, clean Object599 creation coverage, and equipment/effect labels from the public standalone corpus.

The movement/spawn/hardline intake adds four `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_BUY` hits, five `CLIENT_VENDOR_SELL` hits, 10,696 `80 bc` object/state bundle hits, 340 Object599 / `NPC_BASE` creation-prefix hits, 333 adjacent Object599 boundary-prefix hits, 43 CR2 and 93 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, and new movement-state tail families. It does not change decoded top-level vendor transaction counts, but it expands movement-state, static door/object, Object599 creation/correlation, and equipment/effect labels such as Dual Stitchers, D-A Arms Stitcher, Borenov GL-35, Edson BR-19, Fast Healing, and active accelerated-spawn FX.

The combat/PvP/Rajko-state intake adds 20 `SERVER_VENDOR_OPEN` byte-window hits, 14 `CLIENT_VENDOR_BUY` hits, 13 `CLIENT_VENDOR_SELL` hits, 7,493 `80 bc` object/state bundle hits, 725 Object599 / `NPC_BASE` creation-prefix hits, 708 adjacent Object599 boundary-prefix hits, 14 CR2 and 61 CR1 static-object interaction signatures, 284 ability-load/player-attribute hits, 307 ability-unload hits, selector-`ff` continuation-prefix evidence, dense mode `06` movement-selector evidence, adrenaline effect rows, and accelerated-spawn FX rows. Its regenerated report adds one parsed top-level `SERVER_VENDOR_OPEN` and adds equipment/effect labels such as NH3000 Assault Rifle, Dual Zippers, Silenced Dual Grishev R95s, FM-11, Health Drain, Devastation Field, Plague Zone 2, Deflection, Code Nuke 3, and Repair RSI 4.

The remaining-small ability/loot/market intake adds four `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_BUY` hits, five `CLIENT_VENDOR_SELL` hits, 3,043 `80 bc` object/state bundle hits, 140 Object599 / `NPC_BASE` creation-prefix hits, 137 adjacent Object599 boundary-prefix hits, five CR2 and 54 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, 807 ability-load/player-attribute hits, 387 ability-unload hits, 724 adrenaline-booster effect-id hits, 17 active accelerated-spawn FX hits, and equipment labels such as Heavy Meson Rifle and System FM-1500 Magnum.

The Vector state/static intake adds 20 `SERVER_VENDOR_OPEN` byte-window hits, six `CLIENT_VENDOR_BUY` hits, four `CLIENT_VENDOR_SELL` hits, 15,180 `80 bc` object/state bundle hits, 1,028 Object599 / `NPC_BASE` creation-prefix hits, 998 adjacent Object599 boundary-prefix hits, 350 CR2 and 237 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, 3,355 ability-load/player-attribute hits, 4,141 ability-unload hits, and additional adrenaline, fast-healing, and accelerated-spawn FX evidence. Its regenerated report adds the unmatched Code Analyzer Tool buy lead.

The Vector supplemental state/static intake adds eight `SERVER_VENDOR_OPEN` byte-window hits, five `CLIENT_VENDOR_BUY` hits, six `CLIENT_VENDOR_SELL` hits, 12,508 `80 bc` object/state bundle hits, 896 Object599 / `NPC_BASE` creation-prefix hits, 876 adjacent Object599 boundary-prefix hits, 149 CR2 and 162 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, 419 ability-load/player-attribute hits, 335 ability-unload hits, 1,406 adrenaline-booster effect-id hits, 188 fast-healing effect-id hits, and one active accelerated-spawn FX hit.

The session/spawn/movement intake adds six `SERVER_VENDOR_OPEN` byte-window hits, 42 `CLIENT_VENDOR_BUY` hits, seven `CLIENT_VENDOR_SELL` hits, 16,249 `80 bc` object/state bundle hits, 14 CR2 and 122 CR1 static-object interaction signatures, 426 selector-`ff` `00 01 00 ff` continuation-prefix hits, five selector-`ff` `00 01 04 ff` hits, Object599/profile context, movement-state evidence, and session/create traces. It expanded session/create-specific evidence; later intakes carry the current corpus totals.

The Luizoe remaining trade/syntax intake adds one `CLIENT_VENDOR_BUY` byte-window hit, one `CLIENT_VENDOR_SELL` byte-window hit, 1,915 `80 bc` object/state bundle hits, 38 Object599 / `NPC_BASE` creation-prefix hits, 37 adjacent Object599 boundary-prefix hits, 3 CR2 and 23 CR1 static-object interaction signatures, 34 mode `06` movement selector hits, 81 ability-load/player-attribute hits, and 101 ability-unload hits. It does not add decoded top-level vendor transaction flows, but it expands embedded buy/sell boundary targets.

The create/spawn/session intake adds two `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_SELL` byte-window hits, 2,275 `80 bc` object/state bundle hits, 143 Object599 / `NPC_BASE` creation-prefix hits, 142 adjacent Object599 boundary-prefix hits, 5 CR2 and 9 CR1 static-object interaction signatures, and selector-`ff` continuation evidence. It expanded create/session-specific evidence; the following endgame/mission intake carries the current corpus totals while leaving decoded top-level vendor transaction flows at 70.

The endgame/mission intake adds 106 `SERVER_VENDOR_OPEN` byte-window hits, 156 `CLIENT_VENDOR_BUY` hits, four `CLIENT_VENDOR_SELL` hits, 14,239 `80 bc` object/state bundle hits, 749 Object599 / `NPC_BASE` creation-prefix hits, 733 adjacent Object599 boundary-prefix hits, 16 CR2 and 111 CR1 static-object interaction signatures, selector-`ff` continuation evidence, and additional mode `06` movement/effect context. It was the previous checked-in corpus high-water mark; the later selected Warboy derivative carries the current generated totals above.

The generated JSON also exports parsed `VendorOpenItemLists`, `VendorBuyTransactions`, `VendorSellTransactions`, `VendorBuyPriceLeadSummaries`, and `VendorSellPriceLeadSummaries` samples for those complete checked-in vendor flows, so downstream decoder tests can consume the open-header bytes, list-start field, count field, candidate float/raw fields, open-list id arrays, buy request item ids, adjacent cash deltas, buy/sell values, value-kind fields, sell response candidate item/ability fields, resource-table names, expected prices, buyback values, and price-match classifications without scraping Markdown.

Those transaction exports also include buy/sell ack details: `5e` buy sequence fields, echoed buy request prefixes, buy tail markers, `69` candidate buy ack fields, and `5f` sell item/flag fields, with summary-level ack sequence, ack-detail, item-value, and flag arrays for decoder tests.

Run `scripts/triage-packet-capture.sh` on newly received community logs before importing them. It accepts extracted folders plus `.zip`, `.tar`, `.tar.gz`, `.tgz`, `.tar.bz2`, `.tbz2`, `.tar.xz`, and `.txz` archives and reports current packet-signature leads without staging files in the corpus. The triage report now includes a selector-`ff` vendor-body section for Warboy-derived signatures such as `e0 2d 81 0d`, the longer `01 43 ... 22 89 20` vendor-body prefix, continuation prefixes `00 41 00 ff` / `00 01 00 ff` / `00 01 04 ff`, and resource-shaped continuations like `00 41 00 ff 00 00 00 00 b0 74`.

Use `scripts/import-packet-capture.sh --dry-run` after manual review to preview the staged corpus outside the repository, then rerun without `--dry-run` to import. It accepts the same folder and archive inputs, stages text-like logs under `research/packet-dumps/community-intake/<timestamp>-<label>/`, writes Markdown-safe `SOURCE.md` provenance with source-archive and imported-file SHA-256 hashes, flags duplicate hashes from earlier intake manifests, records skipped files in `SKIPPED.md`, normalizes ignored `.log` inputs to tracked `.txt` files, rejects unsafe archive paths, and generates `TRIAGE.md` with current packet-signature leads.

Run `scripts/packet-research.sh` after adding reviewed intake logs to regenerate the packet research summary.
