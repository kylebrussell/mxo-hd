# Packet Research

This project now has a repeatable packet-format research workflow instead of relying on manual notes.

## Source Inventory

| Source | Location | Use | Status |
| --- | --- | --- | --- |
| Local Hardline Dreams headers | `hds/servertype/*/NetworkProtocolHeaders.cs` | Current CR1/CR2 RPC request/response names and values | Tracked |
| Local packet logging | `hds/utils/output/Output.cs` | Runtime logs for new unknown RPC/client packets when `MXO_PACKET_LOG=1` | Tracked |
| Community packet dumps | `research/packet-dumps/mxopackets1` | 83 text and text-like `.log*` dumps imported from `Vibrantic/mxo-resurgence` | Tracked |
| Selected hdneo packet intake | `research/packet-dumps/community-intake/20260606T201844Z-hdneo-mxopackets1-vendor-marketplace-selected` | 11 reviewed public `hdneo/mxopackets1` vendor, marketplace, inventory, loot, and item-move logs with provenance, hashes, triage, and skipped-file evidence | Tracked |
| Selected hdneo sell/inventory intake | `research/packet-dumps/community-intake/20260606T202620Z-hdneo-mxopackets1-sell-inventory-selected` | 15 reviewed public `hdneo/mxopackets1` sell, vendor-use, inventory, loot, code upload, and item-move logs with provenance, hashes, triage, and skipped-file evidence | Tracked |
| Selected hdneo combat/ability intake | `research/packet-dumps/community-intake/20260607T003822Z-hdneo-mxopackets1-combat-ability-selected` | 12 reviewed public `hdneo/mxopackets1` combat, ability load/unload, hyperspeed, and ability-use logs with provenance, hashes, and triage evidence | Tracked |
| Selected hdneo marketplace/vendor intake | `research/packet-dumps/community-intake/20260607T013506Z-hdneo-mxopackets1-marketplace-vendor-selected` | 23 reviewed public `hdneo/mxopackets1` marketplace category, archive-vendor, and vendor-adjacent movement logs with provenance, hashes, triage, and skipped non-text vendor-session evidence | Tracked |
| Selected hdneo redacted Trinity vendor intake | `research/packet-dumps/community-intake/20260607T014317Z-hdneo-mxopackets1-trinity-vendor-redacted-selected` | 2 reviewed public `hdneo/mxopackets1` Trinity vendor-session logs imported as sanitized derivatives with password-bearing auth lines removed, provenance, hashes, and triage evidence | Tracked redacted derivative |
| Selected hdneo inventory/last-day/Rajko near-end intake | `research/packet-dumps/community-intake/20260607T015831Z-hdneo-public-inventory-lastday-rajko-nearend-selected` | 11 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` inventory, item-move, manageable last-day, and Rajko near-end logs imported as sanitized derivatives where needed with password-bearing auth lines removed, NUL bytes stripped, provenance, hashes, duplicate-hash leads, and triage evidence | Tracked sanitized derivative |
| Selected hdneo two-person/player-interaction intake | `research/packet-dumps/community-intake/20260607T022135Z-hdneo-public-two-person-actions-sanitized-selected` | 2 reviewed public `hdneo/mxopackets1` two-person/player-interaction logs imported as sanitized derivatives with NUL bytes stripped and auth/margin handshake blocks removed through `MS_EstablishUDPSessionReply` | Tracked sanitized derivative |
| Selected hdneo mission/code/Rajko world-state intake | `research/packet-dumps/community-intake/20260607T024558Z-hdneo-public-mission-code-rajko-worldstate-sanitized-selected` | 15 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` mission, code/ability-loadout, door, tutorial, spawn, zombie, hardline, and Rajko near-end world-state logs imported as sanitized derivatives with duplicate source hashes omitted, NUL bytes stripped, and auth/margin handshake blocks removed through `MS_EstablishUDPSessionReply` | Tracked sanitized derivative |
| Selected hdneo Luizoe old-sniffer intake | `research/packet-dumps/community-intake/20260607T030922Z-hdneo-public-luizoe-old-sniffer-selected` | 5 reviewed public `hdneo/mxopackets1` Luizoe old-sniffer recursion/trade logs with provenance, hashes, no skipped files, and focused vendor-sell, Object599, movement, static/dynamic interaction, friend-status, ability, equipment, and effect triage evidence | Tracked |
| Selected hdneo movement/spawn/hardline intake | `research/packet-dumps/community-intake/20260607T032533Z-hdneo-public-movement-spawn-hardline-sanitized-selected` | 23 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` movement, hyperjump, loading-area, hardline, door, spawn, and Rajko near-end logs imported as sanitized derivatives with NUL bytes stripped and auth/margin handshakes removed through `MS_EstablishUDPSessionReply` | Tracked sanitized derivative |
| Selected hdneo combat/PvP/Rajko-state intake | `research/packet-dumps/community-intake/20260607T191353Z-hdneo-public-combat-pvp-rajko-state-selected` | 12 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` combat, PvP, corrupted, Rajko Mara/Cyph, and state logs imported as sanitized derivatives with NUL bytes stripped and auth/margin handshakes removed through `MS_EstablishUDPSessionReply` | Tracked sanitized derivative |
| Selected hdneo remaining-small ability/loot/market intake | `research/packet-dumps/community-intake/20260607T200024Z-hdneo-public-remaining-small-ability-loot-market` | 7 reviewed public `hdneo/mxopackets1` ability-loadout, evade-combat, loot, and marketplace-lockpicks text logs with provenance, hashes, no skipped files, and focused vendor, Object599, movement, ability, and effect triage evidence | Tracked |
| Selected hdneo Vector state/static intake | `research/packet-dumps/community-intake/20260607T203107Z-hdneo-public-vector-state-static-selected` | 12 reviewed public `hdneo/mxopackets1` Vector old-world, teleport, unknown-object, and disconnect traces with provenance, hashes, no skipped files, NUL stripping on `Vector35`, and focused vendor, static-interaction, Object599, object-state, ability/effect, and selector-`ff` triage evidence | Tracked NUL-stripped derivative |
| Selected hdneo Vector supplemental state/static intake | `research/packet-dumps/community-intake/20260607T205703Z-hdneo-public-vector-supplemental-state-selected` | 16 reviewed public `hdneo/mxopackets1` Vector, login, Syntax, and old-world text logs with provenance, hashes, no skipped files, and focused vendor-window, static-interaction, Object599/profile, object-state, selector-`ff`, ability/effect, and marketplace triage evidence | Tracked |
| Selected hdneo session/spawn/movement intake | `research/packet-dumps/community-intake/20260607T212233Z-hdneo-public-session-spawn-movement-sanitized-selected` | 10 reviewed public `hdneo/mxopackets1` Luizoe recursion, unsorted proxy, create/session, idle, Vector4, and Unknown traces imported as sanitized derivatives with NUL stripping, auth/margin handshake removal, provenance, hashes, and focused vendor-window, static-interaction, Object599/profile, object-state, movement, selector-`ff`, and session/create triage evidence | Tracked sanitized derivative |
| Selected hdneo Luizoe remaining trade/syntax intake | `research/packet-dumps/community-intake/20260608T065306Z-hdneo-public-luizoe-remaining-trade-sanitized-selected` | 3 reviewed public `hdneo/mxopackets1` Luizoe recursion/Syntax trade-adjacent logs imported as sanitized derivatives with NUL stripping, connection-handshake text removed before import, provenance, hashes, and focused buy/sell, static-interaction, Object599, object-state, movement, ability, and effect triage evidence | Tracked sanitized derivative |
| Selected hdneo create/spawn/session intake | `research/packet-dumps/community-intake/20260608T120313Z-hdneo-public-create-spawn-session-sanitized-selected` | 8 reviewed public `hdneo/mxopackets1` create-character, login/session, jack-in, Morpheus, and Special Agent spawn logs imported as sanitized derivatives with NUL stripping, GAME/WORLD-only packet-block retention, provenance, hashes, and focused vendor-window, static-interaction, Object599/profile, object-state, selector-`ff`, and create/session triage evidence | Tracked sanitized derivative |
| Selected hdneo endgame/mission intake | `research/packet-dumps/community-intake/20260608T122812Z-hdneo-public-endgame-mission-sanitized-selected` | 4 reviewed public `hdneo/mxopackets1` endgame, mission, duality, and high-signal unsorted packet logs imported as sanitized derivatives with NUL stripping, GAME/WORLD-only packet-block retention, provenance, hashes, and focused vendor-window, static-interaction, Object599/profile, object-state, selector-`ff`, mode `06`, and effect evidence | Tracked sanitized derivative |
| Selected Warboy world/game intake | `research/packet-dumps/community-intake/20260608T130150Z-hdneo-public-warboy-world-game-sanitized-selected` | 6 reviewed public `hdneo/mxopackets2` Warboy logs imported as bounded WORLD/GAME-only sanitized derivatives after dropping AUTH/MARGIN blocks and known session/key marker lines; keeps high-signal selector-`ff`, static-object, Object599, and movement evidence | Tracked sanitized derivative |
| Public Warboy/Rajko packet archive | `research/packet-dumps/public-mxopackets2-warboy-triage.md` | Reproducible triage for public `Vibrantic/mxo-resurgence` `mxopackets2` `warboy.zip`; archive is referenced by URL and SHA-256 instead of unpacked into Git | Tracked triage |
| Warboy external-corpus runner | `scripts/run-warboy-packet-research.sh` | Downloads or reuses the public Warboy archive, verifies the SHA-256 when an archive is used, extracts safely outside Git, and writes scratch triage plus packet-research reports | Tracked |
| Capture request brief | `docs/PACKET-CAPTURE-REQUEST.md` | Exact logs, runtime flags, and action context to request from preservation contacts | Tracked |
| Outreach templates | `docs/PACKET-CAPTURE-OUTREACH.md` | Ready-to-send forum, Discord, and direct-message asks for packet-log holders and local testers | Tracked |
| Packet resource leads | `docs/PACKET-RESOURCE-LEADS.md` | Prioritized preservation-community leads, evidence, packet value, next outreach action, and intake route | Tracked |
| Discord lead mining | `scripts/mine-discord-resource-leads.sh`, `research/community-data/discord-emulation-packet-leads.md`, `research/community-data/discord-mara-central-packet-leads.md`, `research/community-data/discord-backup-channel-scan.md` | Repeatably mines public MxOEmu Discord exports for packet-log, collector, vendor, combat, replay, sniffer, mission-log, and source/archive leads with source hashes and message anchors | Tracked |
| Capture triage script | `scripts/triage-packet-capture.sh` | Scans received folders or common archives for current vendor, Object599, movement, ability, and effect packet signatures before import | Tracked |
| Capture import script | `scripts/import-packet-capture.sh` | Stages reviewed text logs from folders or common archives under `research/packet-dumps/community-intake/` with provenance, SHA-256 hashes, duplicate hash leads, skipped-file evidence, and triage output | Tracked |
| Packet tooling smoke tests | `scripts/test-packet-capture-tools.sh` | Verifies triage plus directory, zip, and tar.gz import behavior with hashes, duplicate hash leads, secret/binary skipping, `.log` normalization, and Discord lead-report generation | Tracked |
| MxOEmu FX lists | `research/community-data/fxlisthex.txt`, `data/fxlisthex.txt`, `data/fxlistreal.txt` | 5,941 imported public effect definitions for matching four-byte payload fields to symbolic FX names | Tracked |
| MxOEmu animation list | `data/animations.txt` | 5,442 animation definitions for matching two-byte packet fields to animation symbols | Tracked |
| Local gameobject, static object, and world entity data | `data/gameobjects.csv`, `data/staticObjects_*.csv`, `data/mob_parsed_untouched.csv`, `data/NPC_COLLECTION.xml` | Symbolic GoID, static object, mob spawn, and NPC handle references for packet field and string correlation | Tracked |
| MxOEmu public data host | `https://files.mxoemu.info/` | Public object, inventory, ability, FX, `goprops`, Python/resource data, and Discord exports; `research/community-data/MANIFEST.md` records imported small text resources and hashes; `goprops.7z` generated tracked vendor prices | Partially tracked |
| Rajko MxOEmu | `https://github.com/rajkosto/mxoemu` | External RPC dispatch map and sequence/encryption handling reference | External clone |
| TasteeWheat MxOEmu fork | `https://github.com/TasteeWheat/mxoemu1` | External door/hardline fork with hardcoded protocol 03 object/spawn packet constants and raw `HandleCommand` debug lines | External clone |
| MxOEmu forum thread | `https://mxoemu.info/forum/showthread.php?tid=2801` | Lead for source and packet-log requests from preservation community | External lead |
| MxOEmu captured-packet replay thread | `https://www.mxoemu.info/forum/showthread.php?pid=1059&tid=104` | Lead that HD_Morpheus had a Python server for replaying captured world-server packets | External lead |
| MxO client protocol forum | `https://mxoemu.info/forum/showthread.php?tid=7` | Auth/protocol research context, mostly margin/auth rather than world object payloads | External lead |
| RaGEZONE Hardline Dreams thread | `https://forum.ragezone.com/threads/the-matrix-online-server-emulator.1226514/?amp=1` | 2024 public lead that Hardline Dreams has combat packet logs, but object serialization/client research remains the hard part | External lead |
| Furious Angels MxO Emulation thread | `https://www.thefuriousangels.com/index.php?topic=5917.0` | 2009 historical lead that early Rajko proxy/SVN work and packet logs circulated in the veteran community; the old `svn2.xp-dev.com/svn/rajkosto-mxoemu/` host did not resolve during the current check, so this is a mirror/local-copy outreach lead rather than an acquired source | External lead |

The packet dump import intentionally includes text and text-like dumps only. Binary images, asset files, and client/key material were left out. The imported `.log*` files contain encrypted margin sections as well as decrypted game packet sections, so embedded header hits from those files must be treated as leads until the surrounding protocol context is decoded.

## Workflow

Generate the current evidence summary:

```bash
scripts/packet-research.sh
```

The script writes:

- `docs/generated/packet-research-summary.md`
- `docs/generated/packet-research-summary.json`

To rerun the public Warboy/Rajko external corpus pass without importing the raw 365 MB extraction into Git, use:

```bash
scripts/run-warboy-packet-research.sh
```

By default this downloads `warboy.zip` under `data/experiment_artifacts/packet-corpora/warboy/`, verifies SHA-256 `3dde9509d4f1b97e09938315dc591acfde2e32c5f4f1fb3153ce738f816f6341`, extracts safely, writes `reports/warboy-triage.md`, and writes Warboy-inclusive packet reports under the same ignored scratch workspace. To reuse an existing extraction instead of downloading:

```bash
scripts/run-warboy-packet-research.sh \
  --source-dir /tmp/mxo-warboy-extracted/warboy \
  --work-dir /tmp/mxo-warboy-script-test
```

If a Rajko MxOEmu clone is somewhere other than `/tmp/mxo-research/rajkosto-mxoemu`, set:

```bash
RAJKO_MXOEMU_ROOT=/path/to/mxoemu scripts/packet-research.sh
```

If the TasteeWheat MxOEmu fork is somewhere other than `/tmp/mxo-research/tasteewheat-mxoemu1`, set:

```bash
TASTEEWHEAT_MXOEMU_ROOT=/path/to/mxoemu1 scripts/packet-research.sh
```

To include large external packet corpora without committing them or overwriting the checked-in generated summary, point `PACKET_RESEARCH_EXTRA_DUMPS` at one or more extracted dump roots and redirect the outputs:

```bash
PACKET_RESEARCH_EXTRA_DUMPS=/tmp/mxo-warboy-extracted/warboy \
PACKET_RESEARCH_JSON=/tmp/packet-research-with-warboy.json \
PACKET_RESEARCH_MARKDOWN=/tmp/packet-research-with-warboy.md \
scripts/packet-research.sh
```

Separate multiple external dump roots with `:`. Keep large raw archives outside the repository unless a storage policy is chosen.
External packet samples are labeled by root name, such as `warboy (external)/ProxyM1/m1_packets.log`, so scratch reports are comparable across temp extraction directories.

## Current Findings

- Rajko's public `PlayerObject.cpp` dispatch map confirms 21 client RPC handlers against the local CR2/CR1 enum values.
- Exact command-byte matches include static/dynamic object interaction, jump, region loaded, hardline teleport, object selected, whereami, who, jackout, ready-for-world-change, and player background/detail RPCs.
- The imported packet dumps contain 1,276,069 packet-like lines across 278 parsed files.
- The selected public `hdneo` intakes add checked-in top-level vendor exchange evidence: 44 parsed `CLIENT_VENDOR_BUY` (`81 0e`) records, 11 parsed `CLIENT_VENDOR_SELL` (`81 11`) records, 15 parsed `SERVER_VENDOR_OPEN` (`81 0d`) records, 69 `SERVER_LOAD_MARKETPLACE` (`81 25`) marketplace payloads, and repeated buy/sell requests whose post-header fields now include buy suffixes `c1 09`, `c1 1e`, `c6 1e`, `cb 1e`, `01 a8 00 00 00`, `00 08 00 80 00`, and `13 02 00 00 00`, plus sell suffixes `00 45`, `00 46`, `00 4e`, `00 52`, `00 55`, `00 57`, and `00 58`. The selected two-person/player-interaction, mission/code/Rajko world-state, movement/spawn/hardline, combat/PvP/Rajko-state, remaining-small ability/loot/market, Vector state/static, Vector supplemental, session/spawn/movement, Luizoe remaining trade/syntax, create/spawn/session, endgame/mission, and Warboy world/game intakes add object-state/player-interaction/static-interaction context and embedded vendor byte-window leads; only parsed top-level RPC rows are treated as decoded vendor/open/buy/sell evidence.
- The report now groups these as 70 top-level vendor transaction-flow rows: many buy requests are followed by `5e -> 80 e7 -> 80 e4 -> 81 0f`, the Rajko Icy Snowball buys are followed by `69 -> 80 e7 -> 80 e4 -> 81 0f`, and sells are followed by `80 e7 -> 80 e4 -> 5f -> 81 12` or adjacent sell-neighborhood rows. The adjacent payload and field-lead tables expose transaction-local cash deltas, value-kind fields `8`/`7`, positive buy values at `81 0f +8`, positive sell values at `81 12 +9`, repeated buy item-prefix echoes, `69` candidate buy ack fields, and sell-side `5f` item/flag fields.
- The buy-price join decodes `81 0e` item ids for four 30000-price weapon rows, `WinterSnowballIcy` / "Icy Snowball" (`01 a8 00 00 00`, observed six times at price `100`), and high-bit `AwakenedAbility` at price `100`, with each checked row matching both the signed cash delta and positive `81 0f` buy value. The sell-price join now finds price-backed ids for `Shotgun2` / "Ragaini Combat Shotgun" (`00 55`, expected sell 900), `Shield_M_Shoes_A27_C3_1` / "Shielded White and Black Two tone Dada shoes" (`00 52`, expected sell 1560), `FM-13SK` (`00 58`, expected sell 5000), and `AwakenedAbility` (`00 58`, expected sell 10), each matching the following `80 e7` cash delta and `81 12 +9` sell value using the decoded one-tenth buyback rule.
- The vendor-open decoder treats complete `81 0d` payloads as a 32-byte header with a `u16 +28` list-start field (`20 00` / 32 in checked-in samples), a `u16 +30` count, and `u32[] +32` ids. The checked-in open shapes parse as weapon, tool/disguise, ability, and clothing inventory lists with exact or near-exact joins to `vendor_items.csv` rows and matching public price rows, while preserving candidate float/raw header fields as layout leads rather than final semantics.
- The selected intakes add 386 decoded top-level object-interaction payloads: static `80 c8` rows with object types `0`, `2`, `3`, and `4`, plus dynamic `80 c7` rows with object types `1`, `2`, `3`, `4`, `5`, `6`, `8`, `9`, `13`, and `14`. They also add Object599 / `NPC_BASE` creation samples tied to vendor/loot/state/create-session/endgame-mission/Warboy context, equipment IDs such as `3d b9 00 00` / 47421 resolving to `e50autorifle2003` / "Enhanced Kessay AR1", and the two-person dynamic interaction object id `10092570` / sector `154` / object type `8`. The skipped-file reports preserve the original safety/format-review decisions, and two Trinity vendor-session sources are now tracked separately as redacted derivatives.
- The selected public combat/ability intake imports 12 more standalone `hdneo/mxopackets1` logs covering combat, ability load/unload, hyperspeed, and individual ability-use sessions. Its triage report adds 2,429 `80 bc` object/state bundle hits, 84 Object599 / `NPC_BASE` creation-prefix hits, 22 CR1 static-object interaction hits, two CR2 static-object interaction hits, one `SERVER_VENDOR_OPEN` byte window, two `CLIENT_VENDOR_BUY` byte windows, and one `CLIENT_VENDOR_SELL` byte window. In the regenerated default report this intake contributes the top `80 bc x30` protocol 04 state-sequence sample, adds combat mobs such as `Blackwood Mushfake`, `Silver Dragon Scale`, `Silver Dragon Horn`, and Bathary Boys variants to the Object599 creation corpus, and expands the mode `06` embedded-movement boundary queue from a small lead into the next parser target.
- The selected public marketplace/vendor intake imports 23 more standalone `hdneo/mxopackets1` text logs covering marketplace category browsing, empty marketplace searches, archive-vendor teleport context, and vendor-adjacent movement/state. It keeps two larger Trinity vendor-session logs out of Git as non-text/manual-review targets. Its triage report adds 760 `80 bc` object/state bundle hits, 27 Object599 / `NPC_BASE` creation-prefix hits, 26 CR1 static-object interaction hits, two CR2 static-object interaction hits, 35 repeated mode `06` suffix leads, 12 ability-load hits, and 24 ability-unload hits. The regenerated report does not add new parsed top-level vendor RPCs from this intake, but it does add marketplace/archive-vendor state-bundle context and new server payload shape labels such as `EventSlot1_Effect`, `EventSlot2_Effect`, `EventSlot3_Effect`, `FixedBinkIDOverride`, `PvPMaxSafeLevel`, `PvPServer`, and `WR_RezEvents` from the archive-vendor log.
- The redacted Trinity vendor intake imports two previously skipped public vendor-session logs after dropping password-bearing auth lines and stripping a single NUL byte from each file. Its triage adds 2 `SERVER_VENDOR_OPEN` hits, 5 `CLIENT_VENDOR_SELL` hits, 610 `80 bc` object/state bundle hits, 24 ability-load hits, and 11 ability-unload hits; the regenerated report uses those rows for the new `00 52` and `00 55` sell-price evidence and for additional Trinity social/system setting payload variants.
- The sanitized inventory/last-day/Rajko near-end intake imports 11 public `hdneo/mxopackets1` and `hdneo/mxopackets2` logs after removing password-bearing auth lines from affected sources and stripping NUL bytes. Its triage adds 4 `SERVER_VENDOR_OPEN` hits, 29 `CLIENT_VENDOR_BUY` hits, 7 `CLIENT_VENDOR_SELL` hits, 30 CR2 static-object interaction hits, 66 CR1 static-object interaction hits, 7,150 `80 bc` object/state bundle hits, 18 selector-`ff` `00 41 00 ff` continuation-prefix hits, and 2,372 `00 01 00 ff` continuation-prefix hits. The regenerated report uses the Rajko downtown rows for the new `01 a8 00 00 00` / `WinterSnowballIcy` buy-price evidence, including six `69` buy-ack candidates with sequence field `36 00` / 54 and `u32 +5` step fields `4`, `8`, `12`, `20`, `24`, and `32`, and adds near-end Object599/state-bundle rows for RajkoHaxor, entilsar, Warboy, and Corrupted contexts.
- The sanitized two-person/player-interaction intake imports two public `hdneo/mxopackets1` logs after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds 5 `SERVER_VENDOR_OPEN`, 1 `CLIENT_VENDOR_BUY`, and 5 `CLIENT_VENDOR_SELL` byte-window leads, 4 CR2 `80 c8` and 1 CR1 `80 c3` static interaction windows, 1,814 `80 bc` object/state bundle hits, 317 Object599 / `NPC_BASE` creation-prefix hits, 29 selector-`ff` `00 01 10 ff` hits, and 2 selector-`ff` `00 01 00 ff` hits. The regenerated report does not turn these into new top-level vendor flows; it adds the two-person logs as the second and third largest parsed files, contributes embedded vendor-window context inside protocol 03/object-state bodies, adds the dynamic `80 c7` object id `10092570` / type `8` row, and broadens the mode `06` boundary queue.
- The sanitized mission/code/Rajko world-state intake imports 15 public `hdneo/mxopackets1` and `hdneo/mxopackets2` logs after omitting duplicate source hashes, stripping NUL bytes, and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds six `SERVER_VENDOR_OPEN` byte-window hits, three `CLIENT_VENDOR_BUY` byte-window hits, 14 CR2 static-object interaction hits, 150 CR1 static-object interaction hits, 8,661 `80 bc` object/state bundle hits, 341 Object599 / `NPC_BASE` creation-prefix hits, 479 ability-load/player-attribute hits, 515 ability-unload hits, and selector-`ff` continuation-prefix evidence. The regenerated report turns some of this intake into new parsed top-level vendor/open/marketplace rows and adds mission/code, tutorial, door, hardline, zombie, and Rajko near-end world-state samples to the movement, Object599, equipment, and state-bundle queues.
- The public Luizoe old-sniffer intake imports five public `hdneo/mxopackets1` recursion/trade logs without redaction or skipped files. Its triage adds one `SERVER_VENDOR_OPEN` byte-window hit, two `CLIENT_VENDOR_BUY` hits, 22 `CLIENT_VENDOR_SELL` hits, 68 CR2 static-object interaction hits, 67 CR1 static-object interaction hits, 2,444 `80 bc` object/state bundle hits, 580 Object599 / `NPC_BASE` creation-prefix hits, 568 adjacent Object599 boundary-prefix hits, 75 ability-load/player-attribute hits, 36 ability-unload hits, 4,238 mode `06/0e` movement selector hits, and 1,671 mode `06/0c` hits. The regenerated report keeps top-level vendor transaction counts unchanged, but it adds embedded vendor-window controls, new Object599 names such as Demon Army Andras, new equipment/effect rows including `b4 02 00 00` / AutomaticRifle1 and `cd 08 00 28` / Intense Attack, and more mode `06` movement-boundary samples from clean public logs.
- The sanitized movement/spawn/hardline intake imports 23 public `hdneo/mxopackets1` and `hdneo/mxopackets2` logs after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds four `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_BUY` hits, five `CLIENT_VENDOR_SELL` hits, 43 CR2 and 93 CR1 static-object interaction signatures, 10,696 `80 bc` object/state bundle hits, 340 Object599 / `NPC_BASE` creation-prefix hits, 333 adjacent Object599 boundary-prefix hits, and selector-`ff` continuation-prefix evidence. The regenerated report keeps top-level vendor transaction counts unchanged, but it expands mode `06` movement-boundary samples, movement-state tail families, Object599 creation/correlation rows, static door/object leads, and equipment/effect labels such as Dual Stitchers, D-A Arms Stitcher, Borenov GL-35, Edson BR-19, Fast Healing, and active accelerated-spawn FX.
- The sanitized combat/PvP/Rajko-state intake imports 12 public `hdneo/mxopackets1` and `hdneo/mxopackets2` logs after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds 20 `SERVER_VENDOR_OPEN` byte-window hits, 14 `CLIENT_VENDOR_BUY` hits, 13 `CLIENT_VENDOR_SELL` hits, 14 CR2 and 61 CR1 static-object interaction signatures, 7,493 `80 bc` object/state bundle hits, 725 Object599 / `NPC_BASE` creation-prefix hits, 708 adjacent Object599 boundary-prefix hits, 284 ability-load/player-attribute hits, 307 ability-unload hits, selector-`ff` continuation-prefix evidence, dense mode `06` movement-selector evidence, and adrenaline/accelerated-spawn FX leads. The regenerated report adds one parsed top-level `SERVER_VENDOR_OPEN`, expands combat-state equipment/effect labels, and moves mode `06` movement-boundary and Object599 creation work into a larger checked-in evidence set.
- The remaining-small ability/loot/market intake imports seven public `hdneo/mxopackets1` text logs without redaction or skipped files. Its triage adds four `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_BUY` hits, five `CLIENT_VENDOR_SELL` hits, five CR2 and 54 CR1 static-object interaction signatures, 3,043 `80 bc` object/state bundle hits, 140 Object599 / `NPC_BASE` creation-prefix hits, 137 adjacent Object599 boundary-prefix hits, selector-`ff` continuation-prefix evidence, 807 ability-load/player-attribute hits, 387 ability-unload hits, 724 adrenaline-booster effect-id hits, and 17 active accelerated-spawn FX hits. The regenerated report adds four parsed top-level `CLIENT_VENDOR_SELL` rows and new equipment labels including Heavy Meson Rifle and System FM-1500 Magnum.
- The Vector state/static intake imports 12 public `hdneo/mxopackets1` old-world, teleport, unknown-object, and disconnect traces after stripping NUL bytes from `Vector35` only. Its triage adds 20 `SERVER_VENDOR_OPEN` byte-window hits, six `CLIENT_VENDOR_BUY` hits, four `CLIENT_VENDOR_SELL` hits, 350 CR2 and 237 CR1 static-object interaction signatures, 15,180 `80 bc` object/state bundle hits, 1,028 Object599 / `NPC_BASE` creation-prefix hits, 998 adjacent Object599 boundary-prefix hits, 304 selector-`ff` `00 01 00 ff` continuation-prefix hits, 1,039 selector-`ff` `00 01 10 ff` hits, 3,355 ability-load/player-attribute hits, and 4,141 ability-unload hits. The regenerated report adds one parsed top-level `SERVER_VENDOR_OPEN`, one parsed `CLIENT_VENDOR_BUY` (`13 02 00 00 00` / `CodeAnalyzerTool`, currently no observed cash/value match), and one marketplace load row.
- The Vector supplemental state/static intake imports 16 additional public `hdneo/mxopackets1` Vector, login, Syntax, and old-world traces without redaction or skipped files. Its triage adds eight `SERVER_VENDOR_OPEN` byte-window hits, five `CLIENT_VENDOR_BUY` hits, six `CLIENT_VENDOR_SELL` hits, 149 CR2 and 162 CR1 static-object interaction signatures, 12,508 `80 bc` object/state bundle hits, 896 Object599 / `NPC_BASE` creation-prefix hits, 876 adjacent Object599 boundary-prefix hits, selector-`ff` continuation-prefix evidence, 419 ability-load/player-attribute hits, 335 ability-unload hits, 1,406 adrenaline-booster effect-id hits, 188 fast-healing effect-id hits, and a single active accelerated-spawn FX hit.
- The sanitized session/spawn/movement intake imports 10 public `hdneo/mxopackets1` Luizoe recursion, unsorted proxy, create/session, idle, Vector4, and Unknown traces after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds six `SERVER_VENDOR_OPEN` byte-window hits, 42 `CLIENT_VENDOR_BUY` hits, seven `CLIENT_VENDOR_SELL` hits, 14 CR2 and 122 CR1 static-object interaction signatures, 16,249 `80 bc` object/state bundle hits, Object599/profile context, movement-state evidence, 426 selector-`ff` `00 01 00 ff` continuation-prefix hits, and five selector-`ff` `00 01 04 ff` hits. It expanded session/create-specific evidence; later intakes carry the current corpus totals.
- The sanitized Luizoe remaining trade/syntax intake imports three missing public `hdneo/mxopackets1` Luizoe recursion/Syntax logs after stripping NUL bytes and removing connection-handshake text before import; it omits the already imported `recursion1` derivative and a delete-character source that did not add usable world-packet evidence. Its triage adds one `CLIENT_VENDOR_BUY` byte-window hit, one `CLIENT_VENDOR_SELL` hit, 1,915 `80 bc` object/state bundle hits, 38 Object599 / `NPC_BASE` creation-prefix hits, 37 adjacent Object599 boundary-prefix hits, 3 CR2 and 23 CR1 static-object interaction signatures, 34 mode `06` movement selector hits, 81 ability-load/player-attribute hits, and 101 ability-unload hits. It does not add decoded top-level vendor transaction flows, but it expands embedded buy/sell boundary targets.
- The create/spawn/session intake imports eight public `hdneo/mxopackets1` create-character, login/session, jack-in, Morpheus, and Special Agent spawn logs after stripping NUL bytes and retaining only GAME/WORLD packet blocks. Its triage adds two `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_SELL` hits, 2,275 `80 bc` object/state bundle hits, 143 Object599 / `NPC_BASE` creation-prefix hits, 142 adjacent Object599 boundary-prefix hits, 5 CR2 and 9 CR1 static-object interaction signatures, and selector-`ff` continuation evidence. It expanded create/session-specific evidence; the following endgame/mission intake carries the current corpus totals while keeping parsed top-level vendor transaction flows at 70.
- The endgame/mission intake imports four public `hdneo/mxopackets1` endgame, mission, duality, and high-signal unsorted packet logs after stripping NUL bytes and retaining only GAME/WORLD packet blocks. Its triage adds 106 `SERVER_VENDOR_OPEN` byte-window hits, 156 `CLIENT_VENDOR_BUY` hits, four `CLIENT_VENDOR_SELL` hits, 14,239 `80 bc` object/state bundle hits, 749 Object599 / `NPC_BASE` creation-prefix hits, 733 adjacent Object599 boundary-prefix hits, 16 CR2 and 111 CR1 static-object interaction signatures, selector-`ff` continuation evidence, and additional mode `06` movement/effect context. That intake expanded the checked-in corpus; the later selected Warboy derivative now carries the current corpus totals while parsed top-level vendor transaction flows remain 70.
- The selected Warboy world/game intake imports six public `hdneo/mxopackets2` Warboy logs after retaining only WORLD/GAME packet blocks and dropping AUTH/MARGIN blocks plus known session/key marker lines. Its triage adds 22 `SERVER_VENDOR_OPEN`, five `CLIENT_VENDOR_BUY`, five `CLIENT_VENDOR_SELL`, 6,124 `80 bc` object/state bundle hits, 24 CR2 static-object signatures, 125 CR1 static-object signatures, 16 selector-`ff` `e0 2d 81 0d` field hits, and continuation-prefix evidence including 2,608 `00 01 00 ff`, 757 `00 01 10 ff`, and 70 `00 01 04 ff` hits. The regenerated report expands the checked-in corpus to 278 packet-like files and 1,276,069 packet-like lines, raises Object599 creation records to 4,382, post-creation object-view boundary leads to 3,279, Object12 / `PlayerCharacter` creation records to 2,790, and broad mode `06` movement to 6,282 windows with 4,817 repeated `ff` continuation boundary targets, while parsed top-level vendor transaction flows remain 70.
- The generated JSON now also exports parsed `VendorOpenItemLists`, `VendorBuyTransactions`, `VendorSellTransactions`, `VendorBuyPriceLeadSummaries`, and `VendorSellPriceLeadSummaries`, preserving the `81 0d` header bytes, list-start field, count field, candidate float/raw header fields, open-list item/ability ids, buy request item ids, following packet cash deltas, buy/sell values, value-kind fields, sell response candidate item/ability fields, resource-table names, expected prices, buyback values, and price-match classifications for downstream decoder tests.
- The same transaction exports now preserve buy/sell ack payload details: `5e` buy ack sequence fields, echoed buy request prefixes, buy tail markers, `69` candidate buy ack fields, and `5f` sell ack item/flag fields. The price-summary rows roll those into searchable ack sequence, ack-detail, item-value, and flag arrays.
- The public `Vibrantic/mxo-resurgence` packet source also contains `mxopackets2-master/mxopackets2-master/warboy.zip`, described upstream as MxO packets from Rajko and Warboy with Rajkosto sniffer credit. The archive is tracked here by URL, Git blob SHA `e19d7f56cb13e1cc6301ab359dbd321cefb2620e`, SHA-256 `3dde9509d4f1b97e09938315dc591acfde2e32c5f4f1fb3153ce738f816f6341`, and `research/packet-dumps/public-mxopackets2-warboy-triage.md` rather than by unpacking 365 MB of logs into Git. The triage report confirms high-value leads: 207 vendor-open hits, 105 vendor-buy hits, 112 vendor-sell hits, 3,321 Object599 creation-prefix hits, 31,339 object/state bundle hits, movement selector/tail coverage, 1,058 ability-load hits, 1,036 ability-unload hits, and effect-id evidence for adrenaline booster, fast healing, and accelerated spawns. It now also records selector-`ff` vendor-body search counts: 80 `e0 2d 81 0d` packed-field hits, 42 fixed `01 43 ... e0 2d 81 0d 22 89 20` body-prefix hits, 377 `00 41 00 ff` continuation-prefix hits, 54 `00 41 00 ff 00 00 00 00 b0 74` vendor-open resource-shape hits, and 13 `00 00 00 ff 00 00 00 00 1b ee` vendor-sell resource-shape hits. A scratch `tools/PacketResearch` run over the committed corpus plus an external extraction of this archive produced 1,712,918 packet-like lines across 95 files after treating Warboy `Client->WORLD` / `WORLD->Client` frames as normal game client/server traffic. That pass extracted 607,278 protocol 03 object views, 100 top-level protocol 04 interaction payloads, 1,187 additional protocol 04 server payload shape samples, and 282 static object correlations after including selector-`ff` `u32 +9` field candidates in the static-object candidate load. Warboy adds three framed static interaction ids that also resolve to tracked vendor inventory rows: `277874480` (`ElevatorPanel`, 14 interactions, `data/vendor_items.csv:2`), `277874441` (`Door Hell Club Elevator`, 11 interactions, `data/vendor_items.csv:3`), and `959447528` (`Stance Site Invis Bench`, two interactions, `data/vendor_items.csv:8`). These are vendor-path leads from static-object and vendor-list correlation, not decoded vendor window semantics.
- A 2026-06-08 rerun of `scripts/run-warboy-packet-research.sh` refreshed the ignored Warboy-inclusive scratch report against the then-current checked-in corpus before the selected Warboy derivative was imported. That scratch report covers 272 packet-like files, 2,681,011 packet-like lines, 857,696 protocol 03 object views, 429 protocol 04 interaction payloads, and 2,466 protocol 04 server payload shape samples. Its selector-`ff` repeat-list sample set preserves 86 vendor-header windows: 50 `SERVER_VENDOR_OPEN`, 29 `CLIENT_VENDOR_SELL`, and seven `CLIENT_VENDOR_BUY`, split by region into 28 `field +9 u32`, 30 `continuation body`, 24 `pre-position body`, and four `repeat entry 1` windows. The full-archive scratch priority queue is deep-body boundary candidates with 28 targets / 29 windows, mid-body boundary candidates with 13 targets / 15 windows, one short-span boundary candidate, modeled field overlaps with five targets / 32 windows, and near-position overlaps with nine targets / nine windows. The parser-action split suppresses all 50 continuation/pre-position probe rows as packed resource-field, lower-confidence low-half resource, or high-half header-byte collisions unless new capture context contradicts that model.
- The same Warboy-inclusive scratch run surfaced embedded vendor packet signals through the normal report path: 105 `CLIENT_VENDOR_BUY`, 113 `CLIENT_VENDOR_SELL`, and 210 `SERVER_VENDOR_OPEN` byte-window hits, but zero parsed top-level vendor RPC hits for all three. Context grouping shows those bytes are overwhelmingly inside server protocol 03 object/state frames or unknown packet bodies, with only a few line-level protocol hints. These are strong Warboy corpus targets for boundary work, not final decoded vendor exchanges.
- The Warboy-inclusive report now groups immediate embedded-vendor byte windows and joins them to the containing protocol 03 object-view segment when packet offsets are available. The strongest repeated `SERVER_VENDOR_OPEN` window is not top-level protocol 04: it appears in server protocol 03 unknown-line contexts with prefixes such as `00 00 00 00 e0 2d` before `81 0d` and a stable suffix `22 89 20 00 0b 0c 11 79 80 00`. Its top exact containment is a count-3, first-selector `0c` object view whose parsed position segment is followed by selector `ff` as an unresolved payload containing the repeated window at object-relative offset `+29`. Other repeated rows land in unresolved appearance/attribute or selector `2a`/`2e` tails. Treat these as protocol 03 object/state boundary families to decode, not as vendor-open payloads.
- The same Warboy-inclusive report now groups unresolved non-`80` protocol 03 variable selector tails and scans each tail for local two-byte RPC header windows. The top vendor-bearing tail family is selector `ff` in count-3 first-selector `0c` object views with lead prefix `01 43 00 00 00 00 00 00 00 e0 2d 81 0d 22 89 20`: 21 records contain `SERVER_VENDOR_OPEN` at selector-payload offset `+11` / object-relative offset `+29`. The nearby-window column shows those bytes are consistently inside the four-byte window `e0 2d 81 0d` at payload offset `+9`, which makes this look like a packed selector-field value or body data rather than a clean nested RPC boundary. The selector-`ff` packed-field table then splits the same prefix into concrete field-slice families and now checks the `u32 +9` value against imported FX, game object, ability, item, static object, and vendor inventory tables. The repeated Warboy `e0 2d 81 0d / 226569696` value has no imported resource-table reference, as do the other top selector-`ff` field families. The largest subfamily has 18 count-3/first-`0c` records carrying `u32 +9 = e0 2d 81 0d / 226569696`, bytes `+13 = 22 89 20`, bytes `+16 = 00 0b 0c 11 79 80 00 00`, bytes `+24 = 01 00 00 00 00 12 01 00`, and bytes `+31 = 00 02 ff 00 00 00 00 ba`; three more first-`0c` records keep the same fields but switch bytes `+24` to `01 00 00 00 00 1b 01 00`, and first-`0e` rows show the same split. A new selector-`ff` position-like pass scans for conservative 24-byte little-endian xyz triples after the fixed prefix. In the Warboy scratch report, 21,434 of 31,765 selector-`ff` tails expose such a triple; the vendor-bearing `e0 2d 81 0d` family surfaces at payload offset `+51`, with 16 first-`0c` records at `17595.543,495,2447.504`, three more first-`0c` records at `17615.635,495,1816.967`, and matching first-`0e` rows. The new nearest decoded-position hint column ties those two clusters to nearby decoded player/world coordinates: the `17595.543,495,2447.504` cluster is 85.4 units from player `Se7` and 102.2 units from world row `Zion Duelist`, while the `17615.635,495,1816.967` cluster is 43.7 units from player `ricky303` and 96.9 units from player `Tsurai`. The post-position tail table adds the next structural lead: all 27,716 Warboy selector-`ff` position leads with bytes after the 24-byte xyz have a suffix, and the top groups start at post-position `+0 = ff`, then ten zero bytes, then first nonzero offset `+11` as a little-endian `u16`. The largest first-`0c` group has `ba 13 / 5050` with 112 records, while the vendor-bearing coordinate clusters keep `a0 13 / 5024`, `b9 13 / 5049`, and `ba 13 / 5050`; bytes after that `u16` often include `00 00 00 00 00 ff ff 00` or `00 00 00 00 00 ff 00 00`. The repeat-stride scan shows 25,393 of those tails expose at least two `ff` markers at a consistent stride, with stride `18` dominating the visible top groups, and the second-entry aggregate shows 25,245 of those repeat tails expose a local field inside entry 2. Large first-`0c` groups include `ba 13 / 5050` followed by second-entry zero-run 5, local field `+6 = 66 00 / 102` across 1,133 records, plus a broader zero-run 0, local field `+1 = ff 00 / 255` family across 708 records that includes the previous `ff ff 00 00 00 00 66 00` second-lead subgroup. This broader `ff` family explains the repeated embedded bytes while preserving the stricter immediate-window evidence above and gives the next decoder concrete pre-position, position, and post-position body anchors.
- The repeat-entry transition table and matrix now check the first local field in each consecutive `ff` entry instead of only the second entry. In the Warboy scratch report, the transition table pairs entry 1 and entry 2 for all 25,393 repeat-stride tails, and the matrix yields 50,786 field observations across those first two consecutive entries; entry 1 is stable around post-position zero-run 10 / local field `+11` matching the first record `u16`, while entry 2 separates the large `ba 13 / 5050` first-record family into zero-run 5 / `+6 = 66 00 / 102`, zero-run 0 / `+1 = ff 00 / 255`, and smaller variant buckets. That gives the next parser a concrete state-list-entry hypothesis to test without treating embedded `81 0d` bytes as decoded vendor RPCs.
- The repeat-continuation table now checks what follows those paired entries. In the Warboy scratch report, all 25,393 paired repeat-stride tails have bytes after the repeated records at continuation offset `36`, so the observed pair is not terminal in the buffer. The visible continuation prefixes are not a third `ff` record; high-signal groups start with shapes such as `00 41 00 ff`, `00 00 00 ff`, `00 01 00 ff`, or `00 01 04 ff`, followed by zero padding and small little-endian-looking fields. The continuation-field table groups those bodies by local bytes `+0..+3` and `u16` windows at `+4`, `+6`, `+8`, `+10`, `+12`, and `+14`: all 25,393 continuation bodies with at least four bytes have byte `+3 = ff`. Top Warboy groups expose `00 41 00 ff` with `u16 +8 = b0 74 / 29872` and `u16 +14 = 08 00 / 8`, `00 00 00 ff` families that often stay zero until `u16 +14 = 08 00 / 8`, `00 01 00 ff` with variant nonzero windows such as `fd 0f / 4093`, `00 04 / 1024`, and `3d 19 / 6461`, and `00 01 04 ff` with `f7 0f / 4087`, `00 04 / 1024`, and `d0 ee / 61136`. The marker-tail table then treats that stable local `ff` as a candidate marker: all 25,393 bodies have it, and all 25,393 expose a post-marker u16 after full-continuation parsing. High-signal post-marker shapes split by zero run: `00 41 00` usually has four zero bytes then first field `+8 = b0 74 / 29872`; `00 00 00` can run ten zero bytes to `+14 = 08 00 / 8` or four zero bytes to fields like `da 22 / 8922`; `00 01 00` splits between immediate `+4` fields such as `fd 0f / 4093`, `76 0c / 3190`, and `37 03 / 823`, and four-zero `+8` fields such as `b0 74 / 29872`; `00 01 04` starts immediately with `f7 0f / 4087`. The prefix-family table collapses the detailed rows into 532 global prefix/post-marker families: the largest are `00 01 00` with immediate `+4 = f0 0f / 4080` (912 bodies), `00 01 00` with immediate `+4 = fd 0f / 4093` (714 and 692 bodies across different post-marker leads), `00 01 04` with four-zero `+8 = f2 22 / 8946` (666), `00 01 10` with four-zero `+8 = 12 c9 / 51474` (598), and `00 01 04` with immediate `+4 = f7 0f / 4087` (548). The repeat-list candidate table makes that model explicit: all 25,393 marker continuations have exactly two stride-18 entries before continuation offset `36`, all 25,393 expose a post-marker u16, and the largest rows combine entry fields with continuation fields: `ba 13 / 5050 -> ff 00 / 255 -> 00 01 00 ff +4 f0 0f / 4080` (912 bodies), `ba 13 / 5050 -> 66 00 / 102 -> 00 01 04 ff +8 f2 22 / 8946` (666), `01 00 / 1 -> ff 33 / 13311 -> 00 01 00 ff +4 fd 0f / 4093` (644), `7a 14 / 5242 -> 66 00 / 102 -> 00 01 04 ff +4 f7 0f / 4087` (444), and `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff +8 3f 24 / 9279` (436). The field-value summary then collapses 76,179 role observations across those list candidates: entry 1 is led by `ba 13 / 5050` (7,739) and `01 00 / 1` (3,009), entry 2 by `ff 00 / 255` (7,724) and `66 00 / 102` (5,064), and post-marker by `fd 0f / 4093` (2,036), `f2 22 / 8946` (946), `3f 24 / 9279` (943), and `f0 0f / 4080` (912). That shifts the next decoder step from "find a third record" to "model a two-record stride-18 list followed by a separate continuation body whose local `+3` byte is a stable `ff` marker or flag."
- The generated JSON now exports these modeled leads as top-level `Protocol03SelectorFfRepeatListCandidates` records instead of leaving them only in Markdown. Against the expanded checked-in corpus, the report now exports 4,938 two-entry stride-18 candidates before continuation offset `36`; use the generated entry-pair, layout, marker, and header-summary tables for current top-row rankings. The JSON also exports `Protocol03SelectorFfRepeatListFieldSummaries`: the expanded checked-in corpus has 1,505 groups split into 994 entry-1, 148 primary entry-2, 189 effective entry-2, and 174 post-marker groups, with resource references on 977, 124, 162, and 153 groups respectively; the older full Warboy scratch JSON had 2,621 field-summary groups and 2,422 with resource references. The highest-count entry-1 values now resolve as item/game-object clothing IDs plus animation-symbol collisions, led by `ba 13 / 5050` (`Fclothing_Shirt_A13_C7`, "Orange/Yellow Sif Leather Fighting Dress", and `NPCDJAtTurnTable_Idle4`) and `7a 14 / 5242` (`Fclothing_Shirt_A19_C26`, "Deep Magenta Knight Leather V-Neck Top", and `AD_ADLb_Aggro`). Primary entry-2 top values `ff 00 / 255` and `66 00 / 102` resolve only to animation symbols in the imported tables, while post-marker leaders split between animation refs (`fd 0f / 4093`, `f0 0f / 4080`) and clothing item/game-object refs (`f2 22 / 8946`, `3f 24 / 9279`). The new `Protocol03SelectorFfRepeatListShapeSummaries` export groups entry 1, entry 2, continuation prefix, and post-marker field together for downstream parser work. The expanded checked-in corpus has 1,393 shape summaries; the older full Warboy scratch run had 3,567, all with at least one resource reference. The largest Warboy combined shapes are `ba 13 / 5050 -> ff 00 / 255 -> 00 01 00 ff -> f0 0f / 4080` (912 candidates), `ba 13 / 5050 -> 66 00 / 102 -> 00 01 04 ff -> f2 22 / 8946` (666), `01 00 / 1 -> ff 33 / 13311 -> 00 01 00 ff -> fd 0f / 4093` (644), `7a 14 / 5242 -> 66 00 / 102 -> 00 01 04 ff -> f7 0f / 4087` (444), and `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff -> 3f 24 / 9279` (436). `Protocol03SelectorFfRepeatListEffectiveShapeSummaries` then groups by normalized effective entry-2 value: the expanded checked-in corpus has 1,404 effective-shape groups; the older full Warboy scratch run had 3,662. The top Warboy normalized shapes are `ba 13 / 5050 -> 66 00 / 102 -> 00 01 00 ff -> f0 0f / 4080` (878 candidates, all secondary-sourced from primary `ff 00`), `ba 13 / 5050 -> 66 00 / 102 -> 00 01 04 ff -> f2 22 / 8946` (666 primary-sourced), `7a 14 / 5242 -> 66 00 / 102 -> 00 01 04 ff -> f7 0f / 4087` (444 primary-sourced), and `ba 13 / 5050 -> 66 00 / 102 -> 00 01 00 ff -> 3f 24 / 9279` (438 mixed: 346 primary, 92 secondary). The new `Protocol03SelectorFfRepeatListEffectiveShapeHeaderSummaries` export then joins those normalized shapes to local two-byte RPC-header byte windows without bloating every candidate: the expanded checked-in corpus has 304 known-header shape rows covering 725 candidate instances, including 10 vendor-header candidate instances; the older full Warboy scratch run had 892 known-header shape groups covering 2,843 candidate instances, including 81 vendor-header candidate instances. The strongest Warboy vendor-correlated shape is `ba 13 / 5050 -> 21 0a / 2593 -> 00 41 00 ff -> b0 74 / 29872`, where all 12 candidates carry `SERVER_VENDOR_OPEN` at selector-payload `+11` / object `+29` inside `e0 2d 81 0d`; another high-priority shape is `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff -> 1b ee / 60955`, with all nine candidates carrying vendor windows led by `CLIENT_VENDOR_SELL` at `81 11`. `Protocol03SelectorFfRepeatListHeaderNameSummaries` now inverts those correlations by header name: the expanded checked-in corpus has 57 header-name rows, while the older full Warboy scratch run had 89 rows and three vendor rows. In Warboy, `SERVER_VENDOR_OPEN` covers 49 candidates, 49 de-duplicated windows, and 15 normalized shapes; `CLIENT_VENDOR_SELL` covers 27 candidates, 27 windows, and 11 shapes; `CLIENT_VENDOR_BUY` covers five candidates, five windows, and five shapes. These are byte-window correlations for boundary work, not nested-RPC proof. The new `FieldLayoutKind` split shows the expanded checked-in corpus now spans long-zero and immediate entry-1 rows plus zero-padded, marker-adjacent, immediate, and no-field entry-2 rows; the older full Warboy scratch run separated entry 2 into 11,997 zero-padded u16 candidates, 10,754 marker-adjacent u16 candidates, 2,494 immediate u16 candidates, and 148 no-field candidates. `Protocol03SelectorFfRepeatListLayoutSummaries` rolls that into 70 expanded checked-in layout-prefix groups; the older full Warboy scratch run had 99 groups, led by long-zero entry 1 / marker-adjacent entry 2 / `00 01 00 ff` / immediate post-marker (3,308 candidates, 235 shapes), long-zero entry 1 / zero-padded entry 2 / `00 01 00 ff` / four-zero post-marker (3,028, 540), long-zero entry 1 / zero-padded entry 2 / `00 01 00 ff` / immediate post-marker (2,716, 288), and long-zero entry 1 / marker-adjacent entry 2 / `00 01 00 ff` / four-zero post-marker (1,899, 335). The new secondary-slot export shows 15,047 Warboy entry-2 records carry a nonzero local `+6` field in addition to their primary field; the strongest primary/secondary pairs are `ff 00 -> 66 00` (3,179), `ff 22 -> 66 00` (843), `ff 00 -> 5a 0a` (710), and `ff 00 -> 44 00` (686). In the top layout group, `66 00 / 102` appears as the entry-2 secondary field in 2,158 of 3,308 candidates. The effective-field export now normalizes marker-adjacent entry-2 rows to that secondary slot when present; in Warboy, 10,754 candidates use the secondary source, making `66 00 / 102` the dominant normalized entry-2 value with 9,743 total observations, including 4,679 marker-adjacent observations. The expanded checked-in corpus now splits normalized entry-2 sources almost evenly: 2,502 primary-sourced and 2,436 secondary-sourced candidates. The new `entry 2 effective` field-summary role keeps that normalized value queryable beside the original primary marker evidence. These are candidate resource matches and structural shape groups, not proven field semantics.
- `Protocol03SelectorFfRepeatListDecodedRows` now exports a normalized parser-facing row for each exact selector-`ff` two-entry stride-18 repeat-list candidate. The expanded checked-in corpus has 4,938 decoded rows; all 4,938 carry parser action `decode two-entry stride-18 list before continuation marker` and all 4,938 mark continuation-local offset `+3` as the `ff` marker/flag candidate. The rows keep entry-1, primary entry-2, secondary/effective entry-2, continuation prefix, post-marker field, position anchor, and sample file/line together so downstream decoder tests can consume the two-record list plus separate continuation body directly instead of reconstructing it from aggregate summaries. The normalized entry-2 source split stays 2,502 primary-sourced versus 2,436 secondary-sourced rows, and the largest continuation prefix-before-marker families remain `00 01 00` (2,425), `00 01 10` (1,609), `00 00 00` (376), `00 01 11` (155), and `00 00 10` (86).
- `Protocol03SelectorFfRepeatListEntryPairSummaries` now groups the current checked-in repeat-list candidates by entry-1 field, normalized entry-2 field, and continuation prefix before considering post-marker spread. The expanded checked-in corpus exports 1,353 entry-pair rows. The largest rows are `7f 14 / 5247 -> 44 00 / 68 -> 00 01 10 ff` with 120 combat/ability candidates and stable post-marker `12 c9`, `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff` with 106 mixed primary/secondary candidates and post-marker split `08 00` / `26 a6` / `18 00`, `ba 13 / 5050 -> 66 00 / 102 -> 00 01 00 ff` with 101 mixed candidates and nine post-marker values led by `3f 24` and `54 01`, `01 00 / 1 -> 21 0a / 2593 -> 00 01 00 ff` with 77 secondary-sourced combat/ability candidates, and `01 00 / 1 -> ff 06 / 1791 -> 00 00 00 ff` with 70 primary-sourced combat/ability candidates. This makes the immediate parser question narrower: determine whether primary versus secondary effective entry-2 sources are layout variants for the same repeated state-list field, and whether post-marker spread is a continuation-body discriminator rather than a third list record.
- `Protocol03SelectorFfRepeatListContinuationMarkerSummaries` now exports the continuation-marker invariant as a compact JSON table. The expanded checked-in corpus has 264 marker rows covering 4,938 candidates and 1,404 effective shapes; every row uses marker byte `ff`. Its largest prefix families are `00 01 00` (2,425 candidates), `00 01 10` (1,609), `00 00 00` (376), `00 01 11` (155), and `00 00 10` (86). Top concrete marker buckets include `00 01 00 ff` immediate `fd 0f` (1,849 candidates / 85 shapes), `00 01 00 ff` immediate `f0 0f` (912 / 2, all secondary-sourced entry-2 values), `00 01 04 ff` four-zero `f2 22` (666 / 1), and `00 01 10 ff` four-zero `12 c9` (598 / 234). The same export now carries known-header and vendor-header correlations: the expanded checked-in corpus has 99 marker rows with known-header evidence covering 725 candidates and six marker rows with vendor-window evidence covering 10 vendor-window candidate instances; the older full Warboy scratch run had 232 known-header marker rows and 22 vendor-header marker rows covering 81 vendor windows. The marker rows now also carry weighted resource-reference labels for the entry-1, effective entry-2, and post-marker fields: in Warboy, 384 rows have entry-1 references, 386 have effective entry-2 references, and 339 have post-marker references. A stricter vendor-only split is exported too: 21 of the 22 vendor-bearing marker rows have vendor-only entry-1 refs, 21 have vendor-only effective entry-2 refs, and 16 have vendor-only post-marker refs. The strongest vendor-bearing marker buckets are `00 41 00 ff -> b0 74` with 19 `SERVER_VENDOR_OPEN` windows, vendor-only entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5` / "Shielded Green Farrel Cinch Jacket Highcollar"; `00 01 00 ff -> b0 74` with 16 vendor windows, vendor-only entry-1 refs led by `NPCVendorArchStandOutdoor_GatherRound` / `Fclothing_Shirt_A12_C13`, and the same post-marker refs; `00 00 00 ff -> 1b ee` with nine `CLIENT_VENDOR_SELL` windows and vendor-only entry refs `ba 13 / 5050 -> 66 00 / 102`; and `00 01 00 ff -> fd 0f` with six vendor windows plus the `Aggro_StandMovementDisruption_WalkF` post-marker animation ref. This gives downstream parser tests a direct way to verify the local `+3 = ff` marker/flag hypothesis and prioritize marker families with vendor-byte evidence before assigning field semantics.
- `Protocol03SelectorFfRepeatListContinuationParserActionSummaries` now converts those marker rows into explicit parser actions. The expanded checked-in corpus has three action rows covering all 264 marker rows / 4,938 decoded candidates. The largest action models `+3 ff` as a continuation marker while keeping known-header windows as byte-window controls: 93 marker rows, 3,028 candidates, 855 shapes, 667 known-header candidates, and 945 header windows with zero vendor windows. The ordinary marker action covers 165 rows / 1,561 candidates / 456 shapes with no embedded header-window evidence. The vendor-correlated control action covers six rows / 349 candidates / 93 shapes, including 58 known-header candidates, 10 vendor-header candidates, 91 known-header windows, and 10 vendor windows; it keeps those vendor-looking windows in collision-control handling rather than promoting them to nested RPC decodes. No continuation action row currently asks the parser to treat the stable `+3 ff` byte as a third repeat-list record or nested vendor RPC boundary.
- `Protocol03SelectorFfRepeatListParserCoverageSummaries` now compacts the continuation actions into parser-case coverage buckets. The generated report keeps the same three buckets as a decoder checklist: 3,028 candidates covered with known-header byte-window controls, 1,561 ordinary marker/post-marker candidates, and 349 vendor-window collision-control candidates. Each coverage row preserves parser actions/reasons, prefix families, post-marker layouts/fields, entry-1/effective-entry-2 fields, source split, header regions, vendor windows, selectors, and sample anchors so parser fixtures can assert the two-entry list plus `+3 ff` continuation-marker model while keeping header-looking windows as suppression controls.
- `Protocol03SelectorFfRepeatListHeaderSamples` now exports bounded selector-`ff` byte samples for known-header windows inside repeat-list candidates. The expanded checked-in corpus now fills the 120-sample cap and includes 10 vendor-flagged samples; the older full Warboy scratch JSON was also capped at 120 samples and included all 81 vendor de-duplicated windows: 49 `SERVER_VENDOR_OPEN`, 27 `CLIENT_VENDOR_SELL`, and five `CLIENT_VENDOR_BUY`. Each sample keeps source file/line, payload and object offsets, `HeaderRegion`, parser interpretation, local target offset, header context bytes, repeat-list context bytes, normalized shape fields, and decoded position anchors for parser work. The Warboy vendor region split is 28 `field +9 u32`, 27 `continuation body`, 22 `pre-position body`, and four `repeat entry 1`, and the sample rows now label examples directly as targets such as `continuation +54 / post-marker +50` or `pre-position +212`. This is stronger evidence that these are selector-body boundary leads rather than clean nested RPCs.
- `Protocol03SelectorFfRepeatListHeaderRegionSummaries` now rolls those samples up by local header name, encoded header, and region. The expanded checked-in corpus has 105 region rows including six vendor rows, while the older full Warboy scratch JSON had 207 region rows including 10 vendor rows: `SERVER_VENDOR_OPEN` splits into `field +9 u32` (28), `continuation body` (12), `pre-position body` (8), and `repeat entry 1` (1); `CLIENT_VENDOR_SELL` splits into `pre-position body` (13), `continuation body` (12), and `repeat entry 1` (2); `CLIENT_VENDOR_BUY` splits into `continuation body` (3), `pre-position body` (1), and `repeat entry 1` (1). The region rows now carry weighted resource-reference labels for entry 1, effective entry 2, and post-marker fields; all 10 vendor rows have entry-1 and effective entry-2 refs, and 9 have post-marker refs. Top resource-labeled vendor regions include `SERVER_VENDOR_OPEN` in `field +9 u32` with 28 windows, entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5`; `CLIENT_VENDOR_SELL` in `pre-position body` with 13 windows and entry refs `ba 13 / 5050 -> 66 00 / 102`; and `CLIENT_VENDOR_BUY` in `continuation body` with three windows and entry-2 refs led by `21 0a / 2593`. This keeps the boundary evidence queryable without copying every byte sample.
- `Protocol03SelectorFfRepeatListVendorBoundaryTargetSummaries` now narrows those same vendor windows to exact parser targets by header name, region, selector-payload offset, object-relative offset, candidate-local offset, and position anchor. The expanded checked-in corpus has seven vendor target rows covering 10 candidate instances, while the older full Warboy scratch JSON had 50 target rows covering all 81 vendor windows and 60 normalized shapes: 49 `SERVER_VENDOR_OPEN` candidates across 21 target rows, 27 `CLIENT_VENDOR_SELL` candidates across 24 target rows, and five `CLIENT_VENDOR_BUY` candidates across five target rows. `Protocol03SelectorFfRepeatListVendorBoundaryInterpretationSummaries` then collapses those exact targets into eight parser-priority rows. Continuation body-length work now splits into deep-body unresolved targets with 21 target rows / 22 windows and mid-body unresolved targets with three target rows / four windows; pre-position body-length work now splits into mid-body unresolved targets with nine target rows / 10 windows and deep-body unresolved targets with three target rows / three windows. The continuation local-offset roll-up is led by deep-body offsets `continuation +107 / post-marker +103` and `+81 / +77`, plus mid-body offsets `+39 / +35`, `+116 / +112`, and `+54 / +50`; pre-position targets are led by mid-body offsets `pre-position +137`, `+152`, `+162`, `+170`, and `+180`, plus deep-body offsets `+176`, `+150`, and `+212`. The JSON and Markdown now carry `TargetBodySpanBytes` and `TargetBodyHexes` for exact target rows and roll-ups, so mixed exact targets such as `CLIENT_VENDOR_SELL` at payload `+189` expose both `pre-position gap 182` and `pre-position gap 2` while preserving local body byte slices for parser fixtures. The likely-collision or short-span rows are separated from those body-length targets: continuation short-tail body covers one target row / one window at `continuation +155 / post-marker +151`, packed `field +9 u32` overlap covers two target rows / 28 windows, pre-position near-position overlap covers nine target rows / nine windows with only zero to two bytes between the matched header and xyz anchor, and repeat-entry-1 primary-field overlap covers three target rows / four windows at `repeat entry 1 +11`. The strongest exact target remains `SERVER_VENDOR_OPEN` at selector payload `+11`, split by object offset: object `+29` has 22 windows and seven shapes, while object `+30` has six windows and four shapes. Both are inside the packed `e0 2d 81 0d` field rather than a clean RPC boundary; their top resource-labeled shape fields include entry-1 clothing/game-object ids such as `ba 13 / 5050` and `a0 13 / 5024`, effective entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5`. This gives the next decoder a ranked target table for proving which offsets are packed-field collisions, repeat-entry bytes, near-position byte-window collisions, short continuation tails, pre-position bodies, or continuation-body boundaries.
- `Protocol03SelectorFfRepeatListVendorBoundaryPrioritySummaries` now groups those exact target rows by parser priority before the detailed interpretation split. The expanded checked-in corpus has three priority rows covering 10 candidate instances, while the refreshed full Warboy scratch report had five priority groups: deep-body boundary candidates with 24 targets / 25 windows, mid-body boundary candidates with 12 targets / 14 windows, one short-span boundary candidate, modeled field overlaps with five targets / 32 windows, and near-position overlaps with nine targets / nine windows. This makes the next parser order explicit: prove or reject the deep/mid continuation and pre-position body-length hypotheses first, keep the short continuation tail as a bounded sanity check, and treat packed `field +9`, repeat-entry, and near-position rows as collision controls unless future captures show otherwise.
- `Protocol03SelectorFfRepeatListVendorBoundaryDecisionRows` now turns the exact checked-in selector-`ff` vendor-window targets into parser decisions. The current generated report has seven decision rows and all are suppression fixtures rather than positive nested-RPC decodes: two `SERVER_VENDOR_OPEN` `e0 2d 81 0d` rows are modeled `field +9 u32` overlaps, one `SERVER_VENDOR_OPEN` continuation-body row is a low-half item/gameobject collision, two `CLIENT_VENDOR_BUY` rows are lower-confidence low-half resource collisions, and two `CLIENT_VENDOR_SELL` rows are high-half header-byte collisions. The export keeps each row's exact payload/object offsets, local target offset, body span, window bytes, resource-collision evidence, shape fields, and sample anchor. Because no current checked-in selector-`ff` vendor target has nearby body-length evidence, the parser should suppress these as false nested vendor RPCs unless new before/after captures contradict the model.
- `Protocol03SelectorFfRepeatListVendorBodyWindowProbeSummaries` now removes the modeled packed-field and repeat-entry overlaps from that priority queue and groups only vendor windows that land inside continuation or pre-position bodies. The expanded checked-in corpus has five probe rows covering five candidate instances, while the older full Warboy scratch JSON had 45 rows: 25 continuation-body rows covering 27 windows and 20 pre-position rows covering 22 windows. By priority, those probe rows cover 24 deep-body rows / 25 windows, 12 mid-body rows / 14 windows, one short-span row / one window, and eight near-position rows / nine windows. The table adds body-local offsets, `mod 2` / `mod 4` alignment, bytes before and after the matched two-byte header, full body byte counts, clamped body bytes, payload/object offsets, the four-byte little-endian window, the header `u16`, half-window `u16` resource references, nearby `u16` windows, and simple nearby length-match checks. The strongest repeated probes are continuation `+39 / post-marker +35` with two `SERVER_VENDOR_OPEN` windows and 51 bytes after the header, continuation `+81 / +77` with two `SERVER_VENDOR_OPEN` windows and 81 bytes after it, and pre-position `+137` with two `CLIENT_VENDOR_SELL` windows and a 53-byte gap before xyz. None of the 45 Warboy probe rows has a nearby `u16` matching bytes-after-header, header-plus-tail, body offset, offset-plus-header, or full body bytes, so the next parser should not expect an obvious adjacent little-endian length field at these vendor-like byte windows. All 45 rows have at least one half-window resource-reference collision, but high-half references are mostly the header-looking `81 xx` bytes colliding with animation ids (`81 11`, `81 0d`, `81 0e`). The useful collision signal is the low half: 28 rows / 31 windows resolve to imported resource ids, including 23 rows / 25 windows with item refs. `Protocol03SelectorFfRepeatListVendorBodyWindowEvidenceSummaries` now buckets all 45 Warboy rows by the strongest evidence: 23 probe rows / 25 windows are low-half item/gameobject collision controls, five rows / six windows are lower-confidence low-half resource collisions, 17 rows / 18 windows are high-half-only header-byte collisions, and zero rows are nearby length-match candidates. `Protocol03SelectorFfRepeatListVendorBodyWindowParserActionSummaries` turns that into three Warboy parser actions and no active body-length-test rows: suppress 23 rows / 25 windows as packed item/gameobject field collisions, suppress five rows / six windows unless corroborated by new capture context, and suppress 17 rows / 18 windows as high-half header-byte collisions. `Protocol03SelectorFfRepeatListVendorBodyWindowResourceCollisionSummaries` rolls the labels up into 19 Warboy collision rows, 16 low-half and three high-half, keyed by half, `u16` value, resource kind, and resource reference. Repeated low-half examples include `e0 2d / 11744` resolving to `Armor_F_Shirt_A14_C2_1` item/gameobject rows across 15 rows and `00 28 / 10240` resolving to `Armor_F_Shirt_A4_C24_6` across four rows, plus one-off clothing ids such as `c5 38 / 14533` and `c5 a0 / 41157`. These remain the most concrete body-length fixtures for distinguishing true continuation/pre-position subrecords from aligned clothing/state-field byte-window collisions.
- `Protocol03SelectorFfRepeatListVendorContinuationBodyOffsetSummaries` now splits continuation-body vendor windows into local continuation-offset parser targets. The expanded checked-in corpus has three continuation-body offset rows covering three candidate instances, while the older full Warboy scratch JSON had 23 offset rows covering all 27 continuation-body vendor windows. All 27 remain anchored on candidate position `+51`; the repeated rows are continuation `+39` / post-marker `+35` with two `SERVER_VENDOR_OPEN` windows, `+54` / `+50` with `CLIENT_VENDOR_BUY` plus `SERVER_VENDOR_OPEN`, `+81` / `+77` with two `SERVER_VENDOR_OPEN` windows, and `+107` / `+103` with `CLIENT_VENDOR_SELL` plus `SERVER_VENDOR_OPEN`. Each row preserves bounded context bytes, continuation-body byte counts, bytes remaining after the suspect two-byte window, continuation prefixes, entry-1 fields, effective entry-2 fields, post-marker fields, first selectors, and sample file/line. The tail classes summarize the 27 windows as 22 deep body tails, four mid-body tails, and one short tail after the matched header bytes; the boundary interpretation roll-up now mirrors that split as 21 deep-body targets / 22 windows, three mid-body targets / four windows, and one short-span target / one window. The top repeated Warboy rows show this is usually not a near-terminal header-like collision: continuation `+39` has 51 bytes after the window, `+81` has 81, and `+107` has 125/130, while `+54` splits between 30 and 125. That gives body-length parser work concrete trailing spans instead of only raw payload offsets.
- `Protocol03SelectorFfRepeatListVendorPrePositionBodyOffsetSummaries` does the same for bytes before the decoded xyz anchor. The expanded checked-in corpus has two pre-position offset rows covering two candidate instances, while the older full Warboy scratch JSON had 18 offset rows covering all 22 pre-position vendor windows. The repeated rows are pre-position `+34` with two `CLIENT_VENDOR_SELL` windows four bytes before the position anchor, `+137` with two `CLIENT_VENDOR_SELL` windows 55 bytes before position, `+176` with two `CLIENT_VENDOR_SELL` windows that split between four and 184 bytes before position, and `+205` with one `CLIENT_VENDOR_SELL` plus one `SERVER_VENDOR_OPEN` window split between four and 40 bytes before position. The position-gap classes split the 22 windows into nine near-position overlaps, 10 mid-body gaps, and three deep pre-position gaps; the boundary interpretation roll-up now mirrors that split as nine mid-body pre-position targets / 10 windows, three deep-body pre-position targets / three windows, and nine near-position overlap targets / nine windows. This separates likely near-position byte-window collisions, such as `+34` with only two bytes after the window before xyz, from earlier body bytes like `+137` with 53 bytes remaining and `+176` with 182 bytes remaining, giving the next parser explicit local offsets and span classes for the second unresolved priority row.
- Protocol 04 parsing now assembles marked multi-line packet bodies and conservative unmarked wrapped packet blocks, then walks every top-level RPC block instead of only the first apparent header.
- The generated report cross-references 5,201 local gameobject attribute definitions, 5,941 imported FX definitions from the public MxOEmu FX lists, and 5,442 animation definitions from `data/animations.txt`.
- Top-level protocol 04 hits include confirmed samples for static object interaction, dynamic object interaction, vendor open, vendor buy, vendor sell, marketplace load, item move slot, region loaded, hardline teleport, target/object selected, whereami, jump, ability handler, loot accept, jackout, `SERVER_PLAYER_ATTRIBUTE` / `SERVER_ABILITY_LOAD`, `SERVER_ABILITY_UNLOAD`, `SERVER_FRIENDLIST_STATUS`, `SERVER_CODER_ATTRIBUTE_UNKNOWN`, and `SERVER_MANAGE_BONUS`.
- The MxOEmu public data host exposes `goprops.7z`; its expanded object properties include per-item `VendorPrice` and `NonSellable` fields. `scripts/import-mxoemu-community-data.sh` now generates `data/vendor_prices.csv` from that source, and the runtime vendor price lookup uses it with a default fallback.
- The same public data host exposes `pythonMXO.zip`; `resource/Python/Monolith.ltp/rshooks.py` defines `DEFAULT_VENDOR_PRICE = 1000`, normal vendor buyback as `VendorPrice / 10`, recycle info as `VendorPrice / 20`, and special ability-code buyback handling. Runtime sell pricing now follows the decoded normal-item rule plus high-bit ability-code buyback, including level premiums and the mapped full-price exception list.
- The public data import now tracks a manifest plus small text resources from `files.mxoemu.info`: compact ability names, item names, item command names, FX lists, and a normalized public gameobject reference. The packet research report loads 1,404 ability definitions, but current decoded PlayerCharacter creation `TitleAbility`/combat-exclusive fields remain small raw values such as `356`, `17`, and `4096` rather than direct public high-bit ability GoIDs.
- Protocol 04 object-interaction payload parsing now decodes the local handler shape: four-byte object id, derived sector, and one-byte object type. The current imported corpus has 72 top-level interaction payloads: static `80 c8` rows with object types `0`, `2`, and `3`, and dynamic `80 c7` rows with object types `2`, `3`, `4`, `5`, and the new two-person type `8` row for object id `10092570` in sector `154`. Static vendor-path interactions still anchor the top-level vendor-open samples, but before/after inventory and cash state are still needed for final vendor semantics.
- External source/log mining now parses raw `HandleCommand:` debug lines. TasteeWheat's `Reality.log` contributes two raw static interaction commands (`80 c8 02 00 20 4e 03 00`) for object id `1310720002`, sector `20000`, object type `3`. These are not full packet captures, but they independently confirm the object-interaction command payload shape outside the imported packet dump corpus.
- The generated report now joins decoded object-interaction ids against tracked `data/staticObjects_*.csv` rows using the same little-endian `mxoId` interpretation as the runtime loader. Static interaction ids now resolve across door, environment, elevator-panel, and human-NPC/vendor-path rows, including repeated objects such as `888143880`, `277874426`, `277874441`, `277874480`, and the external raw-command object `1310720002`. This confirms the packet object ids are real static table objects while preserving the need for vendor-open captures before assigning final vendor semantics.
- The generated report now joins decoded object-interaction ids against tracked `data/vendor_items.csv`. Vendor-row matches now include `888143880` from packet-dump door interactions, `277874426`/`277874441`/`277874480` from static object type `0` environment interactions, and `1310720002` from the two TasteeWheat raw command examples. Because HD_Neo's vendor list was parsed from incomplete old logs, this is a vendor-related lead and a warning that local object type labels such as `0 = environment` and `3 = door` should not be treated as final protocol semantics without a vendor-open capture.
- Public MxO Discord export mining is now repeatable through `scripts/mine-discord-resource-leads.sh`. The tracked reports at `research/community-data/discord-emulation-packet-leads.md` and `research/community-data/discord-mara-central-packet-leads.md` cover 131,486 public Matrix Online Discord messages with source hashes, packet-resource term counts, message anchors, and no copied message bodies. `discord-emulation` remains the targeted collector/vendor/code-storage source: collector RPC packets `8113`, `8114`, and `8115`; no known logs for code-storage-to-inventory transfer; `vendor_items.csv` provenance from incomplete opened-vendor logs; Neo packet-log archive searches; `.codejunky` combat packet-log material; old sniffer code; and `MxOSource`/`reality.exe` replay inputs. `discord-mara-central` now has curated probes for the public GitHub packet corpus, Rajkosto sniffer provenance, observed-only NPC spawn/log limitations, final-shutdown-window coverage, last-day log holders, combat packet decoding notes, server-resource priority over packet logs for server-only templates, and a tutorial packet-log parser/document lead. Its broader candidate table adds 26 `packet log` hits, six `packet capture` hits, three `captured packet` hits, one `sniffer` hit, 20 `mission logs` hits, 15 `torrent` hits, 20 `MxOSource` hits, and one code-storage/inventory lead. `research/community-data/discord-backup-channel-scan.md` records the smaller `mxo-faq`, `cmnty-mish-chat`, and `matrix-games` checks and why they were not retained as full reports.
- The generated report now records packet-context summaries for known vendor header byte hits, separates parsed top-level RPC hits from embedded byte windows, and exports top-level vendor transaction-flow rows with adjacent protocol 04 packet sequences, retained per-block payload prefixes, and fixed-offset field leads for vendor-adjacent headers. The checked-in selected hdneo intakes now show top-level `CLIENT_VENDOR_BUY` (`81 0e`), `CLIENT_VENDOR_SELL` (`81 11`), and `SERVER_VENDOR_OPEN` (`81 0d`) samples.
- The 11 parsed top-level sell records come from explicit `sellawakened`, `sell_item_FM13sk_saikungoperative`, redacted Trinity vendor-session, and remaining-small ability-loadout logs. They are client protocol 04 blocks with header offset 7 and suffixes `00 45`, `00 46`, `00 4e`, `00 52`, `00 55`, `00 57`, and `00 58`; the older embedded `81 11` candidate in `Vector.log22` remains MRGN-body noise until proven otherwise.
- Earlier embedded `81 0d` candidates in Shirabaka West and `Vector.log22` remain payload-field evidence: two Shirabaka West samples are confirmed as the second two-byte field in top-level `80 bc` server payloads, and the `Vector.log22` sample is inside an MRGN packet body. The selected hdneo intakes are different evidence: 12 parsed top-level vendor-open records have stable payloads beginning `7c ad d9 43` or a closely related world-state prefix, with observed payload lengths 60, 72, 100, 200, 208, 480, and 760 bytes.
- The generated report now groups top-level `80 bc` payloads. The repeated structure is consistently 20 payload bytes after the `80 bc` header, with field families such as `56 00 <field> 00 c3...` and `45 03 <field> ...`, so the next step is semantic mapping to object attributes or bonus state.
- The generated report now groups `80 bc` internal layout by leading field. The largest families are `56 00` with 27,000 payloads, `15 00` with 17,017 payloads, `55 00` with 14,034 payloads, `45 00` with 3,527 payloads, and `45 03` with 3,410 payloads. A fixed-offset schema table separates the strongest repeated 20-byte shapes, led by `56 00` / `b3 00 00` / byte-11 `21 00` / byte-15 `01 00` with 2,334 records, `56 00` / `b3 00 00` / `21 00` / `00 00` with 2,161, `56 00` / `b3 00 00` / `02 00` / `01 00` with 1,585, `56 00` / `b3 00 00` / `02 00` / `00 00` with 1,462, and `55 00` / `01 00 00` / `05 00` / `01 00` with 1,400. Their capture scopes are mostly teleport plus selected combat, two-person, and Vector state traffic, and their top packet sequences are large `80 bc` state bundles such as `80 bc x30` or runs interleaved with `80 bd`, `80 b2`, `80 b3`, `47`, and social/faction responses, so these shapes are world-state/state-bundle targets rather than strong vendor leads.
- The generated report now ties manual `80 bc` constants back to corpus layout families. The strongest provenance-backed family is `45 03`: three hardcoded examples now have three corpus-backed field groups and 743 total field-group hits. The `45 00` family has 17 hardcoded examples with 17 corpus-backed field groups and 507 total field-group hits; `15 00` has seven hardcoded examples with six corpus-backed field groups and 118 total hits.
- `80 bc` field 1 values now produce candidate matches for interaction-oriented attributes such as `BuyActiveTracker`, `TalkActiveTracker`, `GiverActiveTracker`, `TakerActiveTracker`, `PutActiveTracker`, `SecureTradeFlag`, and `CurrentState`. These are priority capture targets, not decoded names yet.
- The interaction-oriented `80 bc` candidates now include layout-strength and sequence-context evidence. The strongest candidate is field 1 `11 00` / 17 as `SecureTradeFlag`: 941 payloads match that candidate, 839 repeat field 1 at payload byte offset 9, 36 have same-packet vendor/market-header context, and 44 vendor/market capture contexts are present in the broader filename/context grouping. Candidate `TalkActiveTracker` and `BuyActiveTracker` rows remain important capture targets, but they currently do not show the byte-9 repeat pattern.
- The generated report now scores `80 bc` field-0 families with semantic evidence instead of only shape counts. `45 03` is the strongest state-id long-form candidate: all 3,410 payloads repeat field 1 at byte offset 9, with 1,511 same-packet `80 b2` matches, 2,341 same-packet `80 b3` matches, 61 vendor/market co-packet contexts, 839 interaction-candidate payloads, and field-1 references that include `11 00` / `SecureTradeFlag` plus clothing GoID rows. `46 03` repeats the same pattern at low count for `d4 03` / `Mclothing_Glasses_A3_C2`, `05 03` is a small field-1 self-repeat schema, and `11 00` is a field2 mirror schema with 511 payloads and 334 byte-11 repeats of field 2. The high-volume `56 00`, `15 00`, `55 00`, and `45 00` families still look like broad state-bundle carriers rather than final decoded action fields.
- The generated report now splits the `80 bc` long-form state-id candidates by individual field value and exports them in JSON as `ManageBonusStateIdLongFormFieldLeadSummaries`; it also exports `ManageBonusStateIdLongFormSemanticFamilySummaries` so action/state rows are not buried under item-catalog ids. The semantic rollup currently separates one `trade state` row (`45 03` / `11 00`) with 839 payloads, 392 same-packet `80 b2` matches, 389 `80 b2` value mirrors, 579 same-packet `80 b3` matches, and 21 vendor/market co-packet contexts; 32 clothing item-id rows with 2,624 payloads and strong short-state mirrors; and six low-count unresolved state ids with eight payloads and no local references yet. In the `45 03` family, `11 00` / 17 has the fixed field-2 value `00 02` plus candidate references to `SecureTradeFlag`, `HalfExtents`, `HeavyLuggableID`, and `MasterCharacterID`; clothing GoID rows such as `35 04`, `3d 04`, `3e 04`, `3f 04`, and `40 04` each keep byte-11 `00 00`. `46 03` / `d4 03` / `Mclothing_Glasses_A3_C2` has 42 long-form rows with 30 `80 b2` mirrors and 33 `80 b3` matches. This turns the family-level decode lead into concrete, machine-readable semantic and per-field before/after capture targets.
- The generated report now exports `ManageBonusTradeStateValueSummaries` to narrow the `45 03` / `11 00` trade-state candidate by the remaining fixed-width value fields. All 839 rows keep field 2 `00 02`, now annotated as `go data node`; byte 11 splits into `32 00` / 50 with 748 payloads, `01 00` / 1 with 61, `02 00` / 2 with 14, `11 00` / 17 with 14, and singleton `04 00` / 4 and `05 00` / 5 rows. Byte 15 is a binary `01 00` / `00 00` split. The two dominant `32 00` rows divide into 433 byte-15-on payloads with 383 same-packet `80 b3` matches and 315 byte-15-off payloads with 175 same-packet `80 b2` value mirrors plus nine vendor/market co-packet contexts. The enriched export now records linked `80 b2` value fields and local value references: dominant `32 00` maps to DebuffState/IsDuelDeath candidates, `01 00` maps to UseCount/CurrentState-style candidates, `02 00` maps to ActiveTracker/CurrentState-style candidates, and the `11 00` rows retain SecureTradeFlag/HalfExtents-style references. The rarer `11 00` byte-11 rows are concentrated in Trinity vendor/inventory files, while `02 00` rows include Luizoe trade/syntax captures, making these value groups the next before/after trade-state capture targets.
- The generated report now also exports `ManageBonusTradeStateByte15PairSummaries`, which compacts the `45 03` / `11 00` value rows into four byte-15 off/on pairs by byte-11 value. The dominant `32 00` / 50 pair has 315 off payloads, 433 on payloads, 153 same-packet paired rows, 175 off-side and 163 on-side linked `80 b2` field-1 mirrors, linked `80 b2` field 2 fixed at `08 02`, and capture coverage across static interaction, vendor/market, inventory, NPC/mission, trade, combat/ability, and general state bundles. Smaller paired groups remain actionable controls: `01 00` / 1 has 23 off and 38 on payloads with 12 paired rows, `11 00` / 17 has seven off and seven on payloads with five paired rows in Trinity vendor/inventory captures, and `02 00` / 2 has six off and eight on payloads with three paired rows in Luizoe trade/syntax and create/spawn captures.
- The generated report now adds `ManageBonusTradeStateByte15TransitionSummaries`, which mines the actual same-packet off/on row relationships instead of only row-level aggregates. In every paired packet currently observed, the byte-15 `01 00` row is immediately followed by the `00 00` row for the same byte-11 value when considering filtered trade-state rows; row distance is always one and there are zero off-before-on pairs. The dominant `32 00` / 50 pair appears in 153 paired packets, all with same-packet `80 b2`, 133 with same-packet `80 b3`, and none with top-level vendor/market or object-interaction headers in that exact paired packet. The dominant immediate window is `80 b3 -> 80 bc x2 -> 80 b2 -> 80 bc x2` in 130 of 153 `32 00` pairs, with a fixed protocol block distance of three across all current pair groups. Every current paired group has an intervening `80 b2` tuple `11 00/<byte-11>/08 02`, and the nearest preceding `80 b3` value is `11 00` whenever present. For the `32 00` group this means 153/153 between-pair `80 b2` mirrors, 131 complete refresh pairs with a matching preceding `80 b3`, and 22 `80 b2`-only refresh pairs with no preceding `80 b3`; there are zero unmatched pairs. The smaller `01 00`, `11 00`, and `02 00` groups contribute 20/20 complete refresh pairs with the same on-before-off order and intervening `80 b2` mirror.
- The same export now records immediate non-trade `80 bc` companion rows after each on/off trade row. The dominant `32 00` group has 153 on companions, 152 off companions, 152 complete companion pairs, and every observed companion byte-15 word matches the adjacent trade row state. Its complete companion pairs keep companion byte 11 fixed as `0c 00 -> 0c 00`; companion field 0 splits into `55 00 -> 55 00` for 107 pairs and `45 00 -> 55 00` for 45 pairs. Companion field 1 is not stable in any current companion pair: the dominant group has 45 on-side `02 00` companion values and top off-side values `63 00`, `70 00`, `49 00`, `71 00`, and `81 00`, while the smaller groups mostly pair `02 00` with `09 00` or `50 00`. The companion map now annotates both companion byte-11 and companion field-1 values with local attribute/gameobject candidates. The dominant `0c 00 -> 0c 00` byte-11 row links to CurrentState/DetectTrapFX-style creation indexes, `JumpEndTime` / `OrganizationID` update indexes, and the tracked `PlayerCharacter` GoID; the smaller `00 00 -> 00 00` and `04 00 -> 04 00` rows link to CurrentState/use-count/talk/defuse-style attribute candidates. The dominant row has only two distinct field-0 pairs but 82 distinct field-1 pairs, and the displayed top field-1-pair list covers 37 of 152 complete companion pairs. This is strong layout/order evidence for the trade-state candidate, but it is still packet serialization order rather than proven gameplay-time semantics.
- The companion-map repeat-bucket export now proves that the dominant `32 00` / `0c 00 -> 0c 00` companion field-1 tail is broad rather than only hidden repeated clusters: its 82 distinct field-1 pairs split into 35 singletons, 37 two-hit pairs, five three-hit pairs, two four-hit pairs, one five-hit pair, one six-hit pair, and one nine-hit pair.
- The generated report now exports `ManageBonusTradeStateParserActionSummaries` and a Markdown `80 bc` Trade-State Parser Action Leads section. This promotes one serialized trade-state transition candidate for parser work: the `32 00` / 50 row has 153 transition pairs, 152 companion pairs, 152 stable companion byte-11 pairs, 131 complete `80 b3`/`80 b2` refresh pairs, 22 `80 b2`-only refresh pairs, zero unmatched pairs, and 82 distinct companion field-1 pairs. The smaller `01 00`, `11 00`, and `02 00` groups stay grouped as low-count controls with 20 transition pairs, 20 companion pairs, 20 complete refreshes, and zero unmatched pairs. This narrows the next implementation target to a serialized trade-state transition parser and fixture tests, while keeping final gameplay names as semantic leads until before/after captures prove timing.
- The generated report now also exports `ManageBonusTradeStateSerializedTransitionSummaries`, a compact parser-facing layout table. The promoted row has trade shapes `45 03/11 00/00 02/32 00/01 00` to `45 03/11 00/00 02/32 00/00 00`, companion byte-11 `0c 00 -> 0c 00`, between-pair `80 b2` value `11 00/32 00/08 02` in all 153 transition pairs, and preceding `80 b3` value `11 00` in 131 complete refreshes. The low-count control shapes are the same layout with byte-11 `01 00`, `11 00`, and `02 00`; all 20 control transitions have complete short-state refreshes. This is now a machine-readable parser target rather than only a narrative decode lead.
- `ManageBonusTradeStateSerializedTransitionSamples` now preserves representative packet-level fixtures for those layouts. The current report emits five sample rows: the promoted `32 00` complete-refresh path with 130 observed pairs, the promoted `32 00` `80 b2`-only path with 22 observed pairs, and one complete-refresh control each for byte-11 values `01 00`, `11 00`, and `02 00`. Each sample carries the exact on/off `80 bc` payloads, immediate companion shapes, row/block distances, pair header window, between-pair `80 b2` tuple, optional preceding `80 b3`, packet sequence, capture scope, and file/line anchor for parser fixtures.
- `ManageBonusTradeStateSerializedTransitionParserCoverageSummaries` now rolls those samples into three parser cases weighted by observed pairs: promoted complete short-state refresh (`32 00`, 130 pairs), promoted `80 b2`-only refresh (`32 00`, 22 pairs), and low-count complete-refresh controls (`01 00`, `11 00`, and `02 00`, 20 pairs). This is the implementation checklist for a serialized trade-state decoder: support the dominant complete path, support the no-preceding-`80 b3` path, and keep the smaller byte-11 controls as fixtures rather than final gameplay labels.
- `ManageBonusTradeStateSerializedTransitionDecoderRows` now exports five normalized parser rows from those fixtures, so decoder code can consume typed fields instead of parsing shape strings. The rows expose fixed trade fields `45 03`, `11 00` / 17, and `00 02` / 512; byte-11 variants `32 00`, `01 00`, `11 00`, and `02 00`; byte-15 `01 00 -> 00 00`; typed between-pair `80 b2` tuples with field 2 fixed at `08 02` / 520; optional preceding `80 b3` (`11 00` / 17 or null); immediate companion field 0, byte 11, and byte 15 transitions; block distance; packet window; and fixture sample anchors.
- `ManageBonusTradeStateSerializedTransitionDetectionActionSummaries` now rolls the full packet-level detection set into corpus-wide parser action rows. The current detector has four action groups: 130 dominant `32 00` complete-refresh detections with companion shapes and same-packet `80 b2`/`80 b3`, 22 dominant `32 00` `80 b2`-only detections with companion shapes and same-packet `80 b2`, 20 low-count complete-refresh control detections for `01 00`, `11 00`, and `02 00`, and one review candidate where the dominant trade on/off rows and short-state mirror exist but the off-side companion row is missing. The summary rows keep variant roles, trade shape pairs, top companion shape pairs, short-state tuples, packet windows, capture scopes, and sample anchors for parser test planning.
- `ManageBonusTradeStateSerializedTransitionDetections` and the Markdown `80 bc` Serialized Trade-State Transition Detections section now export packet-level parser-safe transition rows. The detector currently emits 173 rows: 152 dominant `32 00` variant detections split into 130 complete short-state refreshes and 22 `80 b2`-only refreshes, 20 low-count control detections, and one complete-refresh review candidate that keeps the dominant trade rows but lacks the off-side companion row. Every detection keeps the exact on/off payloads, companion shapes when present, the mirrored between-pair `80 b2` tuple, optional preceding `80 b3`, packet sequence, capture scope, and sample file/line. This turns the serialized layout evidence into concrete packet-level parser fixtures while preserving final gameplay timing as unresolved.
- `ManageBonusTradeStatePayloadDecodeSummaries` and the Markdown `80 bc` Trade-State Payload Decoder section now decode individual `45 03/11 00/00 02` manage-bonus rows after field 1 repeats at byte offset 9. The detector finds 839 current payload rows split into 10 variant/state groups: dominant `32 00` has 433 on and 315 off rows, low-count controls include `01 00` with 38 on / 23 off, `02 00` with 8 on / 6 off, and `11 00` with 7 on / 7 off, while unpaired `04 00` on and `05 00` off singleton leads remain separate. This is a direct per-payload decoder; same-packet adjacency and companion semantics still come from the transition tables.
- `ManageBonusTradeStateSerializedTransitionFieldRoleSummaries` now turns those three parser cases into an explicit field-role schema across 172 observed transition pairs. Trade field 0 is fixed `45 03`, trade field 1 is fixed `11 00 / 17`, trade field 2 is fixed `00 02 / 512`, trade byte 11 carries the variant (`32 00` in 152 pairs plus `01 00`, `11 00`, and `02 00` controls), trade byte 15 serializes `01 00 -> 00 00`, between-pair `80 b2` mirrors `11 00/<byte11>/08 02`, and preceding `80 b3` is `11 00` for 150 pairs with 22 `none` cases. Companion roles use aggregate companion-map evidence: companion byte 11 is stable as `0c 00 -> 0c 00` for 152 pairs plus low-count `00 00` and `04 00` controls, companion field 0 splits `55 00 -> 55 00` (112) and `45 00 -> 55 00` (60), companion field 1 remains a broad repeat-bucket tail, and companion byte 15 mirrors `01 00 -> 00 00`.
- The generated report now mines 59 hardcoded command examples from Rajko and local queued RPC constants, including 37 `80 bc` startup/world-state examples. The strongest field-group overlap is `45 03`, which appears 112 times in community dumps even though exact payload values differ. One local `80 bc` payload (`15 00 / 45 00 / 00 f7`) remains a high-confidence manual constant with exact corpus hits.
- The generated report now groups top-level `80 b2` payloads, which the local protocol map aliases as both `SERVER_PLAYER_ATTRIBUTE` and `SERVER_ABILITY_LOAD`. The current corpus has 1,359 parsed `80 b2` payloads; the selected combat/ability, Rajko state, remaining-small ability-loadout, and Vector state/static intakes provide the strongest ability-load/state-bundle shapes, while the two-person intake adds more state-bundle contexts.
- The generated report now groups protocol 04 server-state header sequences. It finds 3,054 state-related protocol 04 packets containing `80 b2` and/or `80 bc`, including 514 packets that carry both families in the same top-level packet. Repeated sequence shapes show `80 bc` traveling in large state bundles, often interleaved with `80 b2`, `80 b3`, `80 bd`, `47`, and social/faction responses.
- The generated report now groups top-level `80 b3` payloads, which the local protocol map names `SERVER_ABILITY_UNLOAD`. The current corpus has 2,143 samples, all with 2 payload bytes. The dominant fields still account for most samples and each has same-packet `80 bc` field-1 matches, tying `80 b3` into the same short/long state identifier family already observed for `80 b2`.
- The generated report now groups and decodes top-level `80 d7` payloads, which the local protocol map aliases as `SERVER_FRIENDLIST_STATUS` / `SERVER_FRIENDLIST_STATUS_ADD`. The current corpus has 3,056 samples with a stable leading layout of `08 00`, status field `3c 00` or rare `3b 00`, and `00 8e`, followed by a two-byte little-endian text length and ASCII `SOE+MXO+Vector-Hostile+...` handle. These arrive mostly as repeated `80 d7` runs in login/other captures, making them friend/roster status data rather than a strong `80 bc` state-bundle lead.
- The generated report now groups top-level `80 bd` payloads, which the local protocol map names `SERVER_CODER_ATTRIBUTE_UNKNOWN`. The current corpus has 1,418 samples, all with 9 payload bytes. The dominant shape remains `05 11 00 00 00 00 00 00 00`, mostly in teleport and repeated `80 bc` state bundles. The selected combat/ability, marketplace/archive-vendor, redacted Trinity vendor, Rajko, two-person, mission/code, combat-state, remaining-small, Vector state/static, Vector supplemental, and endgame/mission intakes add smaller `01 xx` attribute-like rows, so they remain state-bundle leads rather than generic `80 bc` payloads.
- The generated report now decodes top-level unknown `81 67` protocol 04 server payloads as a dedicated structural group. The current corpus has 717 samples, mostly 63-byte payloads with stable prefix fields `17 00`, `27 00`, and `1c 22`; Rajko, Trinity, entilsar, Corrupted, two-person, mission/code, movement/spawn, combat-state, remaining-small, Vector, session/spawn, and endgame/mission variants add shorter text/tail combinations. The local protocol map does not name this header yet, so the report keeps positional field names.
- The generated report now decodes the teleport startup `06 -> 80 e5 -> 80 e4` sequence as specialized protocol 04 payloads. The current corpus has 289 `SERVER_LOAD_WORLD` payloads with marker `0e 00`, district ids, simulation-time bytes, flag `01`, an environment-string offset, a sized world path, and a sized environment option string. It also has 804 specialized player value payloads across `SERVER_PLAYER_EXP`, `SERVER_PLAYER_INFO`, and related adjacent player-value rows.
- The generated report now decodes two more protocol 04 server setting families. `81 a9` / `SERVER_FLASH_TRAFFIC` has 177 payloads with fixed leading fields and a sized URL slot containing `http://thematrixonline.station.sony.com/processFlashTraffic.vm`. `3a` / `SERVER_FEATURE_EVENT` has 1,396 payloads with prefix `05 00`, declared byte counts, sized setting keys, and typed values for `PvPServer`, `PvPMaxSafeLevel`, `FixedBinkIDOverride`, `EventSlot*_Effect`, and `WR_RezEvents`.
- The generated report now decodes protocol 04 social/faction payloads. `7c` / `SERVER_FACTION_PLAYER_INFO` has 167 samples split between mode 1 faction summaries and mode 2 fixed-width crew records; mode 1 decodes faction names, and mode 2 decodes crew names plus leader handles. `80 86` / `SERVER_CREW_MEMBERS_LIST` has 134 samples, and `80 de` / `SERVER_FRIEND_ONLINE` has 88 samples with sized `SOE+MXO+Vector-Hostile+...` handles.
- The generated report now isolates unknown protocol 04 `47` as a stable structural group. The current corpus has 568 fixed 46-byte samples with leading words `00 00`, `04 01`, `00 00`, mid words `1a 00` and `06 00`, and a repeated 21-byte tail. These packets appear after `80 bc` state bundles and sometimes immediately before `2e`, `81 a9`, and social/faction responses, so they are no longer mixed into generic triage.
- The generated report now decodes named protocol 04 chat/faction response payloads. `2e` / `SERVER_CHAT_MESSAGE_RESPONSE` has 690 system-style response samples with type byte `07`; `81 54` / `SERVER_CHAT_WHEREAMI_RESPONSE` has 22 coordinate samples, and `80 f5` / `SERVER_FACTION_NAME_RESPONSE` has 31 samples.
- The generated report now isolates unknown protocol 04 `6d` and `80 c1` payloads as dedicated structural groups. The 223 `6d` samples include mode `01 00`, mode `02 00`, and mode `03 00` list-bearing payloads; they use packet offset `08 00`, list flag `01`, little-endian entry counts, and six-byte records with a prefix byte, entry id at record offset +1, and a three-byte tail. The top-level `Unknown6dListRecordShapeSummaries` JSON export keeps the three dominant exact rows at 67 mode-1 payloads / 3,484 records, 58 mode-2 payloads / 3,654 records, and 48 mode-3 payloads / 1,104 records. The new `Unknown6dPacketContextSummaries` roll-up groups all `6d` rows into three mode-level contexts: mode `01 00` has 98 payloads / 4,334 records / 54 distinct ids, mode `02 00` has 70 payloads / 4,015 records / 63 ids, and mode `03 00` has 55 payloads / 1,127 records / 23 ids. Most payloads start or end their packet sequence rather than carrying a same-packet `80 c1`; the same-packet `80 c1` count is zero for all three modes, so `80 c1` is adjacent state-bundle context rather than an embedded `6d` sub-block in the current corpus. All records in the dominant rows use prefix `00`; most tails are `00 00 00`, with the nonzero signal concentrated in final-byte flags such as `00 00 01`, `00 00 02`, or `00 00 03` and therefore in the tail-u16 `+1` projection. Mode `01 00` and `02 00` rows line up with contiguous animation ids such as `100 -> AD_ADLb_ALb`, `101 -> AD_ADLb_iLb`, `102 -> AD_Arc_L40`, and `111 -> AD_A_SR_ADLbShoulderLockMakiOtoshi_F50`; mode `03 00` rows expose low-number game-object/item collisions such as `12 -> PlayerCharacter`, `4 -> Lock_Pick`, and `7 -> Secret_Plan` plus broad attribute-index overlaps. Attribute-only ids such as 49 and 72 remain better treated as field-index candidates, while id 55 collides with `ILCombatHandler` plus CurrentState/EffectID/IsEscortable attributes. Treat these as semantic leads for the next decoder pass, not proof that every `6d` id is an animation, game object, item, or attribute. The new `Unknown80c1PacketContextSummaries` export splits the 53 fixed four-byte `80 c1` samples into 11 field-pair rows. The dominant `00 00 -> c8 00` row has 23 payloads / 23 packets, 626 same-packet `80 bc` payloads, and 19 same-packet `80 b3` payloads; `19 00 -> c8 00` has 6 payloads / 152 same-packet `80 bc`, and `00 00 -> 8c 01` has 6 payloads / 138 same-packet `80 bc` plus 8 same-packet `80 b3`. Its local-reference columns are collision evidence only: field 0 mostly hits low attribute indexes, with noisy `00 00`/`01 00` rows and more specific `19 00`, `1a 00`, `2b 00`, and `32 00` overlaps such as NoiseLevel/StealthAwareness, ExclusiveLocator/JumpPeakHeight, GuardType/Demeanor, and DebuffState/IsDuelDeath; field 1 is much tighter around `c8 00`, `14 01`, and `8c 01` local animation ids, with `8c 01` also colliding with game object 396 `bullet_trail_component`. No current `80 c1` row has same-packet `6d`, and only the singleton `01 00 -> 14 01` row has same-packet `80 b2`, so treat `80 c1` as a short state-bundle marker until before/after gameplay captures prove a field meaning.
- The generated report still exports 1,624 additional protocol 04 server payload shape samples for unresolved families after the specialized structural groups are applied.
- Cross-family state links now connect `80 b2` field 0 to `80 bc` field 1 within the same top-level packet. The strongest shared fields are `11 00` in 205 packets / 383 pairs, `35 04` in 81 packets / 136 pairs, `3d 04` in 60 packets / 111 pairs, `3f 04` in 57 packets / 105 pairs, and `3e 04` in 55 packets / 103 pairs, which is concrete evidence that the short `80 b2` form and long `80 bc` form share an attribute or state identifier.
- The linked short/long forms now expose a repeatable field-value invariant. Across 1,474 same-packet `80 b2`/`80 bc` links, 1,463 repeat the linked field at `80 bc` payload byte offset 9, and 1,460 also repeat the `80 b2` field 1 value at `80 bc` payload byte offset 11.
- The generated report now loads 44,406 tracked gameobject symbol entries and cross-references linked `80 b2`/`80 bc` state fields against them. Five repeated shared fields map to clothing GoIDs, including `3d 04` / 1085 as `Mclothing_Pants_A8_C1`, `35 04` / 1077 as `Mclothing_Pants_A6_C8`, and `3e 04` through `40 04` as adjacent `Mclothing_Pants_A8_C2` through `Mclothing_Pants_A8_C4`. These are symbolic leads, not proof that every linked state field is a GoID.
- FX matches inside `80 bc` payload windows are low-frequency and may be coincidental, but repeated hits now have symbolic names and source lines in the generated report.
- Protocol dump ingestion now accepts compact and label-prefixed compact hex packet lines as well as spaced byte dumps, and it stitches conservative unmarked multi-line packet blocks when a line begins with a top-level protocol wrapper and following short/hex lines continue it. Compact ingestion now surfaces 43 additional packet-like lines from existing community dumps, while loose stitching recovers full manual door-answer bodies without changing packet-like line totals.
- Protocol 03 parsing now segments top-level object packets beyond the first signature. The current corpus yields 293,946 parsed object messages, 182,285 fully segmented messages, 404,417 known selector segments, 26,843 secondary movement wrapper segments, and 35,879 adjacent movement wrapper segments. The live split keeps 151 guarded 57-byte marker-24/marker-25 object-view bodies, 58 guarded 57-byte marker-16 object-view bodies, 115 guarded 57-byte marker-16/marker-17 object-view bodies, 12 guarded 57-byte marker-15 object-view bodies, 9 guarded 57-byte marker-19 object-view bodies, 2 guarded 57-byte marker-1a object-view bodies, 8 alternate guarded 57-byte marker-19 object-view bodies, 11 guarded 57-byte marker-25 object-view bodies, 11 guarded 57-byte marker-4f object-view bodies, 2 guarded 57-byte marker-64 object-view bodies, 5 guarded 57-byte marker-2e object-view bodies, 3 alternate guarded 57-byte marker-2e object-view bodies, 2 guarded 57-byte marker-2e-60 object-view bodies, 5 guarded 57-byte marker-2f object-view bodies, 5 guarded 57-byte marker-34 object-view bodies, 2 alternate guarded 57-byte marker-34 object-view bodies, 4 guarded 57-byte marker-34-9c object-view bodies, 4 guarded 57-byte marker-31 object-view bodies, 3 guarded 57-byte marker-28 object-view bodies, 2 alternate guarded 57-byte marker-28 object-view bodies, 14 alternate guarded 57-byte marker-1f/marker-1e object-view bodies, 41 guarded 57-byte marker-24/marker-26 object-view bodies, 10 alternate guarded 57-byte marker-24/marker-26 object-view bodies, 28 guarded 57-byte marker-29/marker-2a object-view bodies, 2 alternate guarded 57-byte marker-29/marker-2a object-view bodies, 17 alternate guarded 57-byte marker-24 object-view bodies, 15 guarded 57-byte marker-1e/marker-1f object-view bodies, 2 guarded 57-byte marker-1e-b9 object-view bodies, 13 guarded 57-byte marker-1d object-view bodies, 3 alternate guarded 57-byte marker-1d object-view bodies, 12 guarded 57-byte marker-1d/marker-1e object-view bodies, 15 guarded 94-byte movement marker-34 object-view bodies, four guarded 158-byte movement marker-34 object-view bodies, three guarded 158-byte movement marker-16 object-view bodies, four guarded 159-byte movement marker-16/marker-18 object-view bodies, five guarded 49-byte movement marker-0e object-view bodies, four guarded 53-byte marker-34-46 object-view bodies, two guarded 53-byte marker-31 object-view bodies, 10 non-dominant 53-byte marker-32 object-view bodies, two non-dominant 70-byte marker-32 object-view evidence windows, 25 guarded 94-byte zero-update bodies, two guarded 94-byte marker-04 zero-update bodies, two guarded 95-byte marker-04 zero-update bodies, four guarded 237-byte marker-04 zero-update bodies, two guarded 261-byte marker-2a delete-view object-view bodies, 12 guarded 49-byte movement marker-02 zero-update bodies, two guarded 49-byte movement marker-03 zero-update bodies, two guarded 94-byte residual exact object-view bodies, two guarded 94-byte long embedded movement marker-16 trailer bodies, seven terminal long embedded movement marker-34 trailer bodies, two terminal long embedded movement marker-34-46 trailer bodies, six terminal long embedded movement marker-2a trailer bodies, three terminal long embedded movement marker-04 trailer bodies, eight terminal long embedded movement marker-24 trailer bodies, two guarded 57-byte long embedded movement marker-24 trailer bodies, and four guarded 57-byte long embedded movement marker-25 trailer bodies on top of the previously live zero-update, guarded object-view, non-dominant, residual, and terminal 96-byte mode `06` families. Known selector payload sizes now cover one-byte state markers (`00`), empty markers (`02`), position (`08`), selector `09` two-byte-prefixed movement positions, position plus flags (`0a`, `0c`, `0e`), rotation (`04`, `06`), delete/animation (`01`), single emote payloads (`28`), compact terminal selector `28` attribute/effect markers, selector `80` self-view attribute mask/value blocks, primary movement wrappers, terminated secondary movement wrappers, adjacent movement wrappers, mode `06` zero-update, guarded mode `06` object-view bodies, residual guarded mode `06` bodies, non-dominant mode `06` bodies, terminal 96-byte mode `06` tuple bodies, terminal long embedded mode `06` marker-34/marker-34-46/marker-2a/marker-04/marker-24 trailer bodies, guarded long embedded mode `06` marker-16/marker-24/marker-25 trailer bodies, guarded 158-byte mode `06` marker-34 object-view bodies, guarded 158-byte mode `06` marker-16 object-view bodies, guarded 159-byte mode `06` marker-16/marker-18 object-view bodies, guarded 49-byte mode `06` marker-0e object-view bodies, guarded 49-byte mode `06` marker-02 zero-update bodies, guarded 49-byte mode `06` marker-03 zero-update bodies, and one-update selector `9b` state blocks.
- Treating selector `00` as a one-byte state marker remains part of the complete segmentation path for common `0e ... 00 00` object-update tails.
- Primary movement wrappers are now decoded for two-update object packets whose first selector is `12`, `13`, or `14` and whose payload starts `00 <id> 00 <mode> <movement selector>`. The generated embedded-movement rollup now finds 2,287 bounded primary wrappers.
- Secondary movement wrappers are now decoded when the second segment in a two-update object packet starts with `00 02`, embeds a known movement selector (`08`, `0a`, `0c`, or `0e`), and ends with `00 00`. The current generated report finds 26,843 such terminated wrapper segments, including rows exposed after adjacent object-view parsing.
- Adjacent movement wrappers are now decoded when the second segment in a two-update object packet starts with `00 02 <movement selector>`, carries a plausible movement position, lacks the `00 00` terminator, and is immediately followed by another plausible object-view header. The generated report finds 35,879 such boundary-checked segments, which unlocks many adjacent object messages that were previously hidden behind unresolved tails.
- Protocol 03 selector `09` movement-state bundles now parse the first 14-byte segment as a two-byte prefix plus xyz position. Update-count-3 movement-state tails after first selectors `08` and `09` now stay unresolved when their variable tail bytes collide with generic selectors such as `0c`, `0e`, `02`, `06`, or `80`.
- Movement-state post-position tails are now grouped as lead families. Across 8,873 tails, the strongest families are first-selector `08` with `ff 01 64 01` (3,689 records), first-selector `08` with `ff 01 4e 01` (1,319), first-selector `08` with `ff 01 aa 01` (1,107), first-selector `08` with `80 80`-prefixed tail data (627), first-selector `08` with `ff 01 02 00` (508), first-selector `09` with `ff 01 02 00` (373), first-selector `09` with `ff 01 64 01` (264), first-selector `08` with `ff 01 0e 01` (247), first-selector `08` with `ff 01 01 01` (231), and first-selector `09` with `ff 01 aa 01` (206). The tail selector byte plus the next three payload bytes decode as a little-endian tag; adjacent captures commonly step by 124-126 or 250-251, which makes this a timestamp/sequence lead. The report now separates tag bytes, marker, and post-marker prefix; `ff 01` families mostly show zero-prefixed post-marker bytes, while `80 80` families expose variable continuation leads. No final body length is claimed yet.
- Protocol 03 one-update selector `9b` packets now parse as a fixed 10-byte state block. The generated report finds 22 current records, all with payload `01 08 80 fd 00 00 00 00 00 00`; longer selector `9b` contexts remain unresolved.
- Protocol 03 selector payloads now expose embedded movement-window leads plus live bounded mode `06` zero-update, guarded object-view, residual, non-dominant, terminal 96-byte, and exact long embedded trailer-body segment families. The extractor recognizes `00 <mode> <movement selector>` windows instead of only the `00 02` form. The generated action rollup now separates 6,282 unbounded mode `06` object-state movement leads, led by inner selectors `0e` (3,359), `0c` (2,621), `08` (235), and `0a` (67), while the stricter boundary-candidate table keeps 4,817 repeated `ff 00 00 00 10 00 00 00` continuation targets with repeated continuation `00 00 01 ff 00 00 00 00`; after preserving lookahead through consumed bodies, the tuple-body exports keep the boundary and trailer evidence that proves each live body rule. The live parser now consumes the repeated terminal long embedded marker-34/marker-34-46/marker-2a/marker-04/marker-24 trailer shapes and the repeated guarded marker-16/marker-24/marker-25 trailer shapes plus the 78-byte marker-ff non-object, 119-byte marker-04 zero-update, 138-byte marker-4f object-view, and terminal 96-byte marker-04 zero-update tuple bodies under exact guards while preserving lookahead evidence for the report. The generated JSON exports this as `Protocol03NestedMovementBoundaryEvidenceSummaries`, `Protocol03NestedMovementMode06BoundaryCandidateSummaries`, `Protocol03NestedMovementMode06FfContinuationSummaries`, `Protocol03NestedMovementMode06PostContinuationBoundarySummaries`, `Protocol03NestedMovementMode06PostContinuationBodySummaries`, `Protocol03NestedMovementMode06PostContinuationPrefixSummaries`, `Protocol03NestedMovementMode06PostContinuationRemainderSummaries`, `Protocol03NestedMovementMode06PostContinuationTupleSummaries`, `Protocol03NestedMovementMode06PostContinuationTupleBoundarySummaries`, `Protocol03NestedMovementMode06PostContinuationTupleBodySummaries`, `Protocol03NestedMovementMode06PostContinuationTerminalTupleBodySummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementTrailerShapeSummaries`, `Protocol03NestedMovementMode06UnsupportedEmbeddedControlSelectorSummaries`, `Protocol03NestedMovementMode06HeldTupleBodySummaries`, `Protocol03NestedMovementMode06Marker34LongBodyFixtureSummaries`, `Protocol03NestedMovementMode06HeldTupleBodyPrioritySummaries`, `Protocol03NestedMovementMode06TupleBodyParserActionSummaries`, and `Protocol03NestedMovementParserActionSummaries`.
- The generated mode `06` post-prefix tuple body-shape export now groups bytes after the eight-byte tuple and before the next parser-accepted object-view header, and the aggregate parser-action table now includes terminal tuple-body rows. `Protocol03NestedMovementMode06TupleBodyParserActionSummaries` records 151 exact guarded 57-byte marker-24/marker-25 object-view tuple bodies, 58 exact guarded 57-byte marker-16 object-view tuple bodies, 115 exact guarded 57-byte marker-16/marker-17 object-view tuple bodies, 12 exact guarded 57-byte marker-15 object-view tuple bodies, 9 exact guarded 57-byte marker-19 object-view tuple bodies, 2 exact guarded 57-byte marker-1a object-view tuple bodies, 8 exact alternate guarded 57-byte marker-19 object-view tuple bodies, 41 exact guarded 57-byte marker-24/marker-26 object-view tuple bodies, 10 exact alternate guarded 57-byte marker-24/marker-26 object-view tuple bodies, 28 exact guarded 57-byte marker-29/marker-2a object-view tuple bodies, 2 exact alternate guarded 57-byte marker-29/marker-2a object-view tuple bodies, 17 exact alternate guarded 57-byte marker-24 object-view tuple bodies, 15 exact guarded 57-byte marker-1e/marker-1f object-view tuple bodies, 2 exact guarded 57-byte marker-1e-b9 object-view tuple bodies, 8 exact guarded 57-byte marker-33 object-view tuple bodies, 2 exact alternate guarded 57-byte marker-35 object-view tuple bodies, 4 exact guarded 57-byte marker-13 object-view tuple bodies, 13 exact guarded 57-byte marker-1d object-view tuple bodies, 3 exact alternate guarded 57-byte marker-1d object-view tuple bodies, 12 exact guarded 57-byte marker-1d/marker-1e object-view tuple bodies, 11 exact guarded 57-byte marker-25 object-view tuple bodies, 11 exact guarded 57-byte marker-4f object-view tuple bodies, 2 exact guarded 57-byte marker-64 object-view tuple bodies, 5 exact guarded 57-byte marker-2e object-view tuple bodies, 3 exact alternate guarded 57-byte marker-2e object-view tuple bodies, 2 exact guarded 57-byte marker-2e-60 object-view tuple bodies, 7 exact residual guarded 57-byte object-view tuple bodies, 2 exact residual guarded 94-byte object-view tuple bodies, 5 exact guarded 57-byte marker-2f object-view tuple bodies, 5 exact guarded 57-byte marker-34 object-view tuple bodies, 2 exact alternate guarded 57-byte marker-34 object-view tuple bodies, 4 exact guarded 57-byte marker-34-9c object-view tuple bodies, 4 exact guarded 57-byte marker-31 object-view tuple bodies, 3 exact guarded 57-byte marker-28 object-view tuple bodies, 2 exact alternate guarded 57-byte marker-28 object-view tuple bodies, 14 exact alternate guarded 57-byte marker-1f/marker-1e object-view tuple bodies, 31 remaining generic guarded 57-byte object-view tuple bodies, 3 exact alternate guarded 53-byte marker-34 object-view tuple bodies, 4 exact guarded 53-byte marker-34-46 object-view tuple bodies, 2 exact guarded 53-byte marker-31 object-view tuple bodies, 10 exact non-dominant 53-byte marker-32 object-view tuple bodies, 2 exact non-dominant 70-byte marker-32 object-view tuple bodies, 6 exact guarded 78-byte marker-ff non-object tuple bodies, 2 exact guarded 119-byte marker-04 zero-update tuple bodies, 2 exact guarded 138-byte marker-4f object-view tuple bodies, 2 exact terminal 96-byte marker-04 zero-update tuple bodies, 2 exact guarded marker-34 effect/scaffold object-view tuple bodies, 1 exact guarded marker-34 nested replay object-view tuple body, 2 exact guarded 123-byte marker-34 embedded movement object-view tuple bodies, 4 exact guarded 158-byte marker-34 object-view tuple bodies, 3 exact guarded 158-byte marker-16 object-view tuple bodies, 4 exact guarded 159-byte marker-16/marker-18 object-view tuple bodies, 5 exact guarded 49-byte marker-0e object-view tuple bodies, 25 exact guarded 94-byte zero-update tuple bodies, 2 exact guarded 94-byte marker-04 zero-update tuple bodies, 2 exact guarded 95-byte marker-04 zero-update tuple bodies, 4 exact guarded 237-byte marker-04 zero-update tuple bodies, 2 exact guarded 261-byte marker-2a delete-view object-view tuple bodies, 12 exact guarded 49-byte marker-02 zero-update tuple bodies, 2 exact guarded 49-byte marker-03 zero-update tuple bodies, 235 live terminal 96-byte tuple bodies, 200 generic guarded 94-byte object-view tuple bodies, 15 exact guarded 94-byte marker-34 object-view tuple bodies, two live guarded marker-16 long embedded trailer bodies, two live guarded marker-24 long embedded trailer bodies, seven live terminal marker-34 long embedded trailer bodies, two live terminal marker-34-46 long embedded trailer bodies, six live terminal marker-2a long embedded trailer bodies, three live terminal marker-04 long embedded trailer bodies, eight live terminal marker-24 long embedded trailer bodies, and four live guarded marker-25 long embedded trailer bodies. `Protocol03NestedMovementMode06HeldTupleBodySummaries` now lists the residual queue directly: 70 held rows / 70 windows. `Protocol03NestedMovementMode06HeldTupleBodyPrioritySummaries` now contains only 70 low-count/non-dominant control rows / 70 windows, and `Protocol03NestedMovementMode06Marker34LongBodyFixtureSummaries` preserves full `BodyHex` plus body/boundary offsets for the five marker-34 fixtures, including two promoted effect/scaffold rows, two promoted 123-byte embedded-movement rows, and one promoted 265-byte nested-replay row; terminal tail tables keep 35 grouped terminal rows / 218 windows for comparison and carry pre-tail field offsets/bytes.
- The marker-34 fixture follow-up exports now add `Protocol03NestedMovementMode06Marker34LongBodyOffsetSummaries`, `Protocol03NestedMovementMode06Marker34LongBodyInteriorWindowSummaries`, `Protocol03NestedMovementMode06Marker34LongBodyKnownFxWindowSummaries`, `Protocol03NestedMovementMode06Marker34LongBodyFxFieldRoleSummaries`, `Protocol03NestedMovementMode06Marker34LongBodyStructuralRoleSummaries`, `Protocol03NestedMovementMode06Marker34LongBodyLayoutSummaries`, `Protocol03NestedMovementMode06Marker34LongBodyParserReadinessSummaries`, `Protocol03NestedMovementMode06Marker34LongBodyNestedReplaySummaries`, `Protocol03NestedMovementMode06Marker34LongBodyFieldWindowSummaries`, and `Protocol03NestedMovementMode06Marker34LongBodyEmbeddedMovementVectorSummaries`. The offset scaffold has 79 nonzero body-offset rows; across all five full fixtures, stable nonzero positions include body offsets `6=ff`, `37=01`, `41=ff`, `58=ff`, `72=0c`, `74=58`, `75=37`, `76=08`, `79=1f`, `80=06`, `81=08`, and `88=22`. The field-window export consolidates that into nine decoder-facing rows: primary marker lead offset 6 across all five fixtures, tag window offset 37 across all five fixtures with tag-le values 1744/1748/1781/1791/1913, effect-style offset 54 across three fixtures, stable scaffold offset 71 across all five fixtures, embedded movement payload offset 94 split by selector-specific payload length, replay prefix offset 148, nested tag offset 203, nested effect offset 220, and nested stable scaffold offset 237. The embedded-vector export decodes the body-offset 95 movement windows with the same little-endian float rules as the live nested movement parser: selector `0c` in the 265-byte fixture decodes `-47888.32,70.326,-31265` and is near the outer position at distance 92.157, while selector `0e` in the two 123-byte fixtures decodes `50485.15,-1105,-48460.01` and is a same-zone movement delta from both outer positions. The interior-window export exposes embedded mode `06` movement windows at body offset 95 (`00 06 0e` in the two 123-byte fixtures and `00 06 0c` in the 265-byte fixture) plus the 265-byte fixture's body-offset 148 `00 02 ff` marker-prefix lead. The known-FX export resolves effect-style windows at body offset 54: `30 04 00 28` / `FX_CHARACTER_HEALTH_DRAIN` appears in two fixtures with follow-up bytes `ff 09` and `ff 14`, while `9c 05 00 28` / `FX_CHARACTER_DEFLECTION` appears once with `ff 0e`; the 265-byte fixture also repeats `FX_CHARACTER_DEFLECTION` at body offset 220. The FX-role export resolves all three known FX windows to marker-34-relative offset 54: the body-offset 54 rows are outer marker-34 effect fields, and body offset 220 is the same effect field inside the nested marker-34 replay body that starts at body offset 166. The structural-role export normalizes marker lead, tag, effect, and scaffold fields across scopes: the outer marker-34 body has relative offsets `6`, `37`, `54`, and `71`, while the nested replay body at offset 166 repeats the same relative fields at absolute offsets `172`, `203`, `220`, and `237`. The layout export composes those fields per fixture: body lengths 110 and 119 are effect/scaffold layouts, the two 123-byte rows are embedded-movement layouts with selector `0e` and decoded position `50485.15,-1105,-48460.01`, and the 265-byte row combines `FX_CHARACTER_HEALTH_DRAIN`, selector `0c` movement near the outer position, and a nested marker-34 replay body containing `FX_CHARACTER_DEFLECTION`. The parser-readiness export groups those layouts into promotion targets and reports parser state: the two effect/scaffold fixtures are promoted under the guarded parser action, the two 123-byte embedded-movement fixtures are promoted under the guarded parser action, and the 265-byte nested-replay fixture is promoted under the exact guarded nested-replay parser action. The nested-replay export proves that the 265-byte fixture's body-offset 148 prefix `00 02 ff cd cc 4c be 00 00 00 00 e9 07 00 00 00 00 34` starts another marker-34 body at offset 166: the nested body has tag window `01 00 83 07 ff`, repeats `FX_CHARACTER_DEFLECTION` with follow-up `ff 0e`, and matches all 12 stable scaffold probes through relative offset 88.
- The marker-34 parser-readiness rows now carry classifier-visible guard signatures: body suffixes, shared post-prefix lead `00 e9 07 00 00 00 00 34`, marker byte `34`, and the boundary/header samples for each layout bucket.
- `Protocol03NestedMovementMode06LongTupleBodyTailAnchorSummaries` now keeps the broader long bounded/terminal queue at 79 grouped rows. The embedded-trailer exports narrow that to repeated shared-trailer shapes plus evidence-only unsupported selector controls (`28` has two windows; `06`, `2a`, and `2e` have one each).
- The exact embedded trailer-shape export keeps live parser promotion strict. Live exact shapes are `02` + `ff cd cc 4c be` + `00 e9 ... 34` (seven terminal-96 windows), `03` + `ff cd cc 4c be` + `00 46 ... 34` (two terminal-96 windows), `01` + zero pre-lead + `00 b0 ... 16` (two guarded 94-byte windows), `03` + zero pre-lead + `00 4c ... 2a` with matching outer post-prefix (six terminal-96 windows), `03` + zero pre-lead + `00 b6 ... 24` (eight terminal-96 windows plus two guarded 57-byte windows), `03` + zero pre-lead + `00 cf ... 25` (four guarded 57-byte windows), and `03` + zero pre-lead + `00 96 ... 04` (three terminal-96 windows). One same-pre-anchor marker-2a row with outer post-prefix `00 66 ... 29`, remaining marker-24 guarded-state/object-view pre-anchor rows, singleton decoded controls such as `00 f0 ... 36`, `00 72 ... 2a`, `00 91 ... 24`, `00 dc ... 26`, `00 39 ... 1f`, `00 af ... 05`, and `00 f5 ... 3b`, plus unsupported selector controls remain evidence-only until more captures corroborate them. `Protocol03NestedMovementMode06UnsupportedEmbeddedControlSelectorSummaries` groups the residual unsupported controls by selector byte.
- `Protocol03NestedMovementMode06LongTupleBodyPreAnchorLeadSummaries` and body/offset exports keep the exact pre-anchor scaffold as evidence while the live parser consumes only repeated trailer shapes satisfying the stricter embedded-movement rule. Offset tables still expose recurring header-like collisions at 70, 74, 78, 79, and 94, so those remain collision evidence, not accepted object-view boundaries.
- Protocol 03 variable-block string extraction now surfaces printable profile/object names inside unresolved object-view packets. The strongest new family initially appeared as update count `12`, selector `57`; creation-aware parsing now identifies it as a fully dynamic Object599 / `NPC_BASE` creation record with prefix `0c 57 02 <spawn counter> cd ab`.
- Protocol 03 update count `12`, selector `0c` player spawn/profile records now parse as Object12 / `PlayerCharacter` creation records. Across 2,790 PlayerCharacter creation records, 2,723 include a decoded `CharacterName` attribute, and the Object12 fixed-width creation table exposes `RealLastName`, `RealFirstName`, `CharacterName`, `Level`, `Health`/`MaxHealth`, `RSIDescription`, `CombatantMode`, title/combat-exclusive fields, cancel/exclusive fields where present, and `Position` values. The larger Rajko, movement/spawn, combat/PvP, remaining-small, Vector state/static, Vector supplemental, session/spawn/movement, Luizoe remaining trade/syntax, create/spawn/session, endgame/mission, and Warboy world/game intakes add many live-player profile variants beyond the original `AfterWhoruNeo`/`platinumfox` pair.
- The generated report now loads 14,642 tracked mob/NPC entity rows and exact-matches protocol 03 string leads against them. Top matches include `A.S.P. Major` with 54 packet strings across 175 spawn rows, `Gold Blood Champion` with 25 packet strings across 313 spawn rows, and `Sisters of Fate Student` with 22 packet strings across 77 spawn rows, including level, zone, RSI samples, and bounded prefix/suffix bytes around each packet string.
- Object599 / `NPC_BASE` creation strings are now extracted directly from observed `0c 57 02 <spawn counter> cd ab <attribute count> 8e <name>` record-prefix candidates. The generated report maps `57 02` to tracked `NPC_BASE` GoID 599, identifies `cd ab` as the local creation separator, and treats the formerly ambiguous byte after `cd ab` as the creation attribute count. Top groups now include `Chopper Runner` with 494 records, `Blackwood Goof` with 245, `Assassin Blade` with 181, `A.S.P. Master Sergeant` with 176, `A.S.P. Major` with 141, `Blackwood Mushfake` with 133, `Chopper Jumper` with 129, `Gold Blood Enthusiast` with 127, `A.S.P. Trainee` with 123, and `A.S.P. Warrant` with 117.
- All 4,382 directly extracted Object599 / `NPC_BASE` creation records in the current corpus have a 32-byte fixed `CharacterName` attribute after first mask `8e`. The previous 33-byte zero-run result was a heuristic artifact: it counted the first zero byte of the following `TitleAbility` attribute.
- The directly extracted Object599 / `NPC_BASE` creation records share `TitleAbility = 00 10 00 00` and `CombatantMode = 22`, matching the local mob creation defaults in `Mob.getCreationData()`. This turns the unresolved named NPC/profile block into a creation-attribute layout anchored to local code and corpus evidence.
- Object599 / `NPC_BASE` creation masks now parse cleanly with local fixed-width attribute definitions. Every directly extracted record has matching declared and parsed creation attribute counts, ranging from 12 through 27 attributes, and the generated report groups raw values for `CharacterName`, `TitleAbility`, `CombatantMode`, `StopFollowActiveTracker`, `Description`, `Position`, `IsEnemy`, `NPCRank`, `Level`, `Health`, `MaxHealth`, `CurrentState`, and other observed fields. The report now also records bytes consumed by each declared attribute stream; all 4,382 Object599 records have post-attribute bytes, 1,787 expose `00 0c 57 02` within the first eight suffix bytes, and 3,279 expose a plausible next object-view header inside the first eight suffix bytes with 269 distinct bounded pre-header byte sequences. Of those boundary leads, 2,944 use a three-byte `<value> 00 00` pre-header. Selector-length plus dynamic-creation boundary parsing now accounts for 2,750 follow-up object-view headers without consuming them into the parent record. The remaining nested dynamic creation blocker is a focused machine-readable queue: seven tail-summary groups, 253 header-like lead summaries, 34 body-anchor summaries, 49 extended-boundary summaries, four parser-action rollups, 25 top body-shape rollups, 10 post-separator field rollups, 10 post-separator vector probe rollups, 10 post-vector tail rollups, four post-separator layout action rollups, seven reviewed body-layout rows, one vector-semantic rollup, three tail-semantic rollups, and three control-semantic rollups exported for parser work. The body-anchor, extended-boundary, parser-action, body-shape, post-separator field, vector-probe, vector-tail, layout-action, reviewed-layout, vector-semantic, tail-semantic, and control-semantic tables split the pre-separator body bytes, post-`cd ab` lead bytes, separator-collision candidates, later candidate headers, variable pre-vector bridge bytes, conservative `be f64 x3` probe values, post-vector `be f32` tail shapes, promotion/rejection labels, concrete field offsets, parent-position mismatch evidence, bounded tail direction/padding classes, and held-control layouts, while 15 reviewed post-separator bodies are now consumed by the guarded parser rule.
- The Object599 / `NPC_BASE` dynamic-creation tail evidence is now exported as machine-readable JSON via `Protocol03NpcBaseDynamicCreationTailSummaries`, `Protocol03NpcBaseDynamicCreationHeaderLikeLeadSummaries`, `Protocol03NpcBaseDynamicCreationBodyAnchorSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundarySummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryParserActionSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryBodyShapeSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorFieldSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorVectorProbeSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorVectorTailSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorLayoutActionSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorReviewedBodyLayoutSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorVectorSemanticSummaries`, `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorTailSemanticSummaries`, and `Protocol03NpcBaseDynamicCreationExtendedBoundaryPostSeparatorControlSemanticSummaries`. The committed corpus now keeps seven dynamic-tail summary groups, 253 header-like lead rows, 34 body-anchor rows, and 49 extended-boundary rows after promoting common view-1 selector `50`/`67`/`68`/`69`/`76`/`90`/`a3`/`d0` follow-up headers into bounded Object599 boundary parsing; across 1,761 measured nested dynamic creations, 127 still parse the Object599 body without exposing another object-view header inside the current eight-byte search window, and 15 are bounded by the reviewed post-separator body rule. The extended-boundary action rollup still preserves 142 total evidence rows: 3 rows / 6 candidates of repeated later-header evidence, 14 rows / 19 candidates of post-separator later-header evidence, 23 rows / 23 candidates of singleton later-header evidence, and 9 rows / 10 candidates rejected as `cd ab` separator collisions. The body-shape rollup makes separator placement explicit: the strongest post-separator shape has 5 rows / 8 candidates with 60 body bytes before the later header, `cd ab` at byte 13, and 45 post-separator bytes. The post-separator field rollup splits that 60-byte body into lead byte `02`, second byte `05` for 3 rows / 5 candidates and `09` for 2 rows / 3 candidates; both variants have five zero bytes after the second byte and first nonzero data at post-separator offset 7. The conservative vector probe reads three big-endian doubles at post-separator offset 9 and finds 10 rows / 19 candidates, all bounded finite triples; 11 candidates have a two-byte pre-vector bridge at offset 7, and eight candidates have a four-byte bridge at offset 5. The post-vector tail probe starts at offset 33 and keeps 10 rows / 19 candidates: 11 candidates have bounded `be f32 x3` tails, four have bounded `be f32` tails plus one zero pad byte, two have no post-vector tail, one has a zero raw two-byte tail, and one remains an outlier tail. The layout-action rollup promotes seven rows / 15 candidates to `review bounded vector-tail body` and keeps the four remaining candidates as controls: two no-tail, one raw-tail, and one outlier-tail. The reviewed body-layout export maps those 15 candidates into seven parser-ready offset rows: lead/subtype at `post[0]`/`post[1]`, a three- or five-byte zero run, a two- or four-byte bridge, `be f64 x3` at `post[9..32]`, and bounded `be f32` tails starting at `post[33]`; those 15 candidates are now complete under the guarded reviewed post-separator parser rule. The vector-semantic rollup compares those 15 `be f64 x3` values to the decoded parent `Position` attribute and labels all 15 as `treat as non-position vector body field`: every vector differs from the parent position and every vector-parent distance falls in the `>5k` bucket. The tail-semantic rollup classifies the promoted `be f32` tails into 11 planar direction-like triples, two padded planar triples, and two padded bounded quads; all 13 triple rows have a zero middle axis, and all 15 promoted tails stay in the `<=10` magnitude bucket. The control-semantic rollup keeps the four unpromoted finite-vector candidates as held controls: two no-tail Object599 follow-up candidates, one zero raw two-byte tail before an object-state update, and one outlier `be f32 x2` tail before a delete-view candidate. All four held controls also differ from decoded parent `Position` and fall in the `>5k` distance bucket, which keeps them useful as negative controls rather than promoted parser rows. The earlier Warboy-inclusive scratch report remains useful as a larger external comparison set for body-length work.
- Typed Object599 / `NPC_BASE` creation-attribute correlation now confirms several field semantics against tracked mob rows. Across 4,363 creation records with tracked same-name mob rows, decoded `Level` matches 4,361/4,363, `Health` plus `MaxHealth` matches 4,267/4,284, `Description`/RSI-like ids match 4,345/4,363, `NPCRank` matches 4,363/4,363, `DebuffState` matches 4,206/4,206, and `CurrentState` matches 4,333/4,354. Decoded 24-byte `Position` values exact-match 44/4,363 old mob rows, while nearest same-name row distances place 3,931/4,363 within 1k units, 4,157/4,363 within 2.5k, and 4,280/4,363 within 5k, which supports the vector decode while showing exact static-row equality is too brittle.
- Object599 / `NPC_BASE` equipment and effect fields now have symbolic leads. The current report resolves additional `EquippedItemID` rows through little-endian gameobject ids and imported MxOEmu `&gib` command names. The mission/code/Rajko world-state intake promotes repeated public weapon rows such as `b5 02 00 00` / 693 (`AutomaticRifle2` / "Reit-Tek BR") and `b9 02 00 00` / 697 (`Shotgun2` / "Ragaini Combat Shotgun"), while selected hdneo intakes add `3d b9 00 00` / 47421, `15 b9 00 00` / 47381 (`e50autorifle1001`), `0d ba 00 00` / 47629 (`e50shotgun2002`), `ef 1a 00 00` / 6895, the Luizoe old-sniffer `b4 02 00 00` / 692 (`AutomaticRifle1` / "Bormann 5000") row, movement/spawn rows such as `c7 09 00 00` / 2503 (`DualSubmachinegun2` / "Dual Stitchers"), `ec 1a 00 00` / 6892 (`Submachinegun3` / "D-A Arms Stitcher"), `f0 1a 00 00` / 6896 (`AutomaticRifle3` / "Borenov GL-35"), combat/PvP/Rajko-state rows such as `c2 1e 00 00` / 7874 (`AutomaticRifle8` / "NH3000 Assault Rifle"), `d7 1e 00 00` / 7895 (`DualSubmachinegun6` / "Dual Zippers"), `6a ba 00 00` / 47722 (`e50submachinegun2dlsl001` / "Silenced Dual Grishev R95s"), and `d5 1e 00 00` / 7893 (`Submachinegun9` / "FM-11"), plus remaining-small rows `63 9a 00 00` / 39523 (`MesonRifle2B` / "Meson Rifle Mk II"), `99 95 00 00` / 38297 (`MesonRifle3` / "Heavy Meson Rifle"), and `22 b6 00 00` / 46626 (`SemiAutoPistolSystem` / "System FM-1500 Magnum"). The corpus also has resolved `EffectID` records including `cd 08 00 28` / Intense Attack, `b5 07 00 28` / Fast Healing, `66 00 00 04` / active accelerated-spawn FX, and new combat-state/remaining-small FX rows such as `30 04 00 28` / Health Drain, `03 08 00 28` / Devastation Field, `62 01 00 28` / Plague Zone 2, `9c 05 00 28` / Deflection, `11 0a 00 28` / Code Nuke 3, and `2e 0a 00 28` / Repair RSI 4.
- The generated report now imports 41,696 MxOEmu `&gib` item command rows from `invitems_command.txt`. Vendor inventory correlations and Object599 equipment leads now show command symbols and display names beside raw item ids, which makes capture targets easier to validate without changing packet semantics.
- The generated report now imports 1,404 ability definitions from the public compact ability list plus the local legacy ability table. It keeps unmatched decoded ability-style fields as raw values instead of forcing names.
- Protocol 03 selector `28` effect/emote payloads now have a conservative prefix decode wherever the selector appears in a protocol 03 object-view message. The current corpus has 5,925 selector `28` payloads, including 4,622 first-selector records; 5,624 expose a 12-byte little-endian float position prefix, and 3,960 contain at least one imported FX ID window. The strongest ability-context rows remain FX-window and position leads, while selector `80` self-view attribute decoding exposes additional attribute/effect bundle `28` payloads after the mask terminator.
- Protocol 03 selectors `2a` and `2e` now have conservative position-like lead extraction. The generated report scans 17,110 selector `2a`/`2e` variable tails and finds 6,747 known position-like leads with plausible little-endian xyz triples. These remain prefix/body-boundary leads; no full variable-tail body length is claimed yet.
- Protocol 03 selector `80` payload starts now parse against the local Object12 self-view attribute mask/value layout used by `PlayerCharacter.GetSelfUpdateAttributes(..., false)`. The current corpus has 34,284 decoded selector `80` self-view blocks containing 45,038 fixed-width Object12 self-view attributes; another 1,742 selector `80` tails remain layout leads, with 122 containing at least one imported FX ID window. The largest groups now cover `SelectionRangeDebuff`, `MissionTypeFlags`, `IsDuelDeath`, `JumpEndTime`, `DuelID`, `Health`, and related self-view attributes; the previous `4c 0a 00 28` FX-looking window is one bounded `Health = 0a4c` case followed by the next selector `28`, not a standalone effect id.
- Protocol 03 selectors `3c`, `9e`, `9f`, and `a0` now have conservative static object/door spawn prefix leads. The generated report finds 37 `cd ab` static object leads across 11 static object ids after compact, label-prefixed compact, loose manual door-answer blocks, movement/spawn/hardline, Vector state/static, Vector supplemental, session/spawn/movement, create/spawn/session, and endgame/mission intakes are included. Full payload rows still use protocol object type `3`, expose bounded quaternion windows, and surface post-quaternion marker/vector/tail fields. Those post-quaternion fields are bounded layout evidence, not final field semantics yet.
- The generated report now mines 34 hardcoded protocol 03 examples from local code and external MxOEmu sources, including TasteeWheat's door/hardline fork. These examples are provenance hints for spawn/profile, static object, and movement layouts; live capture evidence is still required before treating them as decoded client behavior.
- Variable-length protocol 03 families remain intentionally partial. The generated report now calls out unresolved spawn/profile, named NPC/profile, appearance/attribute, longer effect/emote bundle tails, residual selector `80` tails, static object/door post-quaternion body semantics, and unknown selector tails instead of pretending those payload sizes are fully decoded, while extracting embedded movement windows where the evidence is local and repeatable.

## Immediate Research Targets

1. Capture or acquire more vendor buy/sell sessions with a known character cash value, inventory slot, item GoID, vendor GoID, static object id, object interaction payload, and before/after inventory state. The checked-in selected hdneo intakes now give buy, sell, open, and static vendor-interaction anchors, but cash/inventory delta semantics are still missing.
2. Use the validated `80 bc` sequence-context leads, especially `11 00` / `SecureTradeFlag`, to pair NPC talk, vendor open, buy, sell, secure-trade, inventory, and loot actions with before/after object state and assign field semantics.
3. Re-run local clients with `MXO_PACKET_LOG=1` and preserve `PacketLog.txt`, `UnknownRPC.txt`, and `UnknownClient03Request.txt` after vendor open, buy, sell, marketplace, inventory, loot, and door actions.
4. Ask MxO preservation contacts specifically for packet logs containing repeated static human-NPC/vendor interactions (`80 c8` or CR1 `80 c3` with object type `0`, `2`, or `3`), vendor-open, vendor-buy, vendor-sell, marketplace listings, inventory drag/drop, hardline workflows, and Object599 / `NPC_BASE` spawn/profile tails with selectors `50`, `67`, `68`, `69`, `76`, `90`, `a3`, and `d0`.
5. Capture before/after vendor-open evidence for static object type `0`, `2`, and `3` interactions, and compare it against the new checked-in `80 c8` static type `2` vendor-path interactions, the checked-in `80 c7` dynamic object type `2` vendor-session interactions, the existing object ids `888143880` and `1310720002`, and the Warboy static ids `277874480`, `277874441`, and `959447528` that resolve to static object rows and vendor inventory rows.
6. Implement recycle-info pricing (`VendorPrice / 20`) only if a decoded packet path needs it.
7. Semantically decode `80 bc` server payload fields by mapping the grouped field families to local object attributes, bonus/state classes, and before/after gameplay actions.
8. Follow up on the prioritized leads in `docs/PACKET-RESOURCE-LEADS.md`, starting with the acquired Warboy/Rajko `mxopackets2` triage report and its now-framed `WORLD` packet analysis, the RaGEZONE/Hardline Dreams combat-log lead, the Discord curated probes in `research/community-data/discord-emulation-packet-leads.md`, the curated and broader packet-log/archive rows in `research/community-data/discord-mara-central-packet-leads.md`, the HD_Morpheus world-packet replay lead, and the Furious Angels historical Rajko proxy/SVN lead. Prioritize logs that include object serialization, ability state, FX, and before/after attribute context.
9. Continue resolving variable-length protocol 03 creation/profile selectors by correlating Object12 / `PlayerCharacter` and Object599 / `NPC_BASE` post-attribute suffix boundaries and before/after gameplay captures, then apply the same approach to appearance/attribute, selector `2a`, `2e`, `28`, and `80` tails, nested-movement unknown selector bodies, plus the unresolved static object/door selector tail semantics after the extracted `3c`/`9e`/`9f`/`a0` prefix and post-quaternion tail fields. For Object599 / `NPC_BASE`, prioritize captures that preserve at least 600 bytes after `0c 57 02 <spawn counter> cd ab <attribute count> 8e <CharacterName>` and include the header-like `view 1` selectors `50`, `67`, `68`, `69`, `76`, `90`, `a3`, and `d0`; for nested movement, keep preserving before/after object state around `00 06 <movement selector>` payloads plus the repeated `ff 00 00 00 10 00 00 00 00 00 01 ff 00 00 00 00` boundary lead. The zero-update, guarded object-view, guarded exact 57-byte marker-family object-view paths including marker-1a, marker-1d-alt, marker-1f-1e-alt, marker-1e-b9, marker-13, marker-24-26-alt, marker-29-2a, marker-29-2a-alt, marker-33, marker-35-alt, marker-64, marker-2e, marker-2e-alt, marker-2e-60, marker-2f, marker-28, marker-28-alt, marker-31, marker-34, marker-34-alt, marker-34-9c, and marker-4f, residual exact guarded 57-byte object-view, residual exact guarded 94-byte object-view, guarded exact 53-byte marker-34, marker-34-46, and marker-31 object-view, non-dominant exact 53-byte marker-32 object-view, non-dominant exact 70-byte marker-32 object-view evidence row, guarded exact 158-byte marker-34 object-view, guarded exact 158-byte marker-16 object-view, guarded exact 159-byte marker-16/marker-18 object-view, guarded exact 49-byte marker-0e object-view, guarded exact marker-34 effect/scaffold object-view, guarded exact marker-34 nested replay object-view, guarded exact 123-byte marker-34 embedded movement object-view, guarded exact 94-byte marker-04 zero-update, guarded exact 95-byte marker-04 zero-update, guarded exact 237-byte marker-04 zero-update, guarded exact 261-byte marker-2a delete-view object-view, guarded exact 49-byte marker-02 zero-update, guarded exact 49-byte marker-03 zero-update, guarded exact 78-byte marker-ff non-object tuple body, guarded exact 119-byte marker-04 zero-update tuple body, guarded exact 138-byte marker-4f object-view tuple body, exact terminal 96-byte marker-04 zero-update tuple body, residual guarded, non-dominant, terminal 96-byte, terminal long embedded marker-34/marker-34-46/marker-2a/marker-04/marker-24, and guarded long embedded marker-16/marker-24/marker-25 mode `06` paths are now live parser rules or exact parser-action evidence rows. The next nested-movement decoder test should start from the priority rollup and full marker-34 fixtures: the 70 lower-priority low-count/non-dominant control rows / 70 windows, the marker-2a outer-post-prefix mismatch row, remaining marker-24 guarded-state/object-view pre-anchor rows, singleton long-embedded controls (`00 f0`, `00 72`, `00 91`, `00 dc`, `00 39`, `00 af`, `00 f5`), and the unsupported selector controls now grouped as selector `28` (two windows) plus singleton selectors `06`, `2a`, and `2e`, while capture requests should focus on proving semantics for the consumed mode `06` bodies rather than only proving body length.
   Use the full marker-34 fixtures plus the layout, parser-readiness, field-window, structural-role, FX-role, vector, offset/interior/known-FX/nested-replay exports as the immediate nested-movement decoder queue: start with the three promoted parser-readiness buckets, then test marker-34-relative structural fields at offsets 6, 37, 54, and 71 for both outer and nested replay bodies, marker-34-relative offset 54 effect-style fields (`30 04 00 28` and `9c 05 00 28`), body offset 95 decoded movement vectors (`00 06 0c` near outer position and `00 06 0e` same-zone movement delta), the body-offset 148 marker-34 replay prefix, its nested body start at offset 166, and the 265-byte fixture's nested replay-relative offset 54 `9c 05 00 28` effect field against before/after object-state captures.
10. Use `Protocol03NestedMovementMode06LongTupleBodyPreAnchorLeadSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorBodySummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorOffsetSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorHeaderCandidateSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementTrailerSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementTrailerShapeSummaries`, and `Protocol03NestedMovementMode06UnsupportedEmbeddedControlSelectorSummaries` as the first nested-movement queue for remaining long-body work. The live repeated trailer families (`02`/`ff cd cc 4c be`/`00 e9 ... 34`, `03`/`ff cd cc 4c be`/`00 46 ... 34`, `01`/zero pre-lead/`00 b0 ... 16`, `03`/zero pre-lead/`00 4c ... 2a` with matching outer post-prefix, `03`/zero pre-lead/`00 b6 ... 24`, `03`/zero pre-lead/`00 96 ... 04`, and `03`/zero pre-lead/`00 cf ... 25`) are now promoted under exact embedded-movement and trailer guards. Keep the marker-2a outer-post-prefix mismatch row, repeated marker-24 guarded-state/object-view pre-anchor rows, singleton rows, and unsupported selector rows as collision controls until more captures corroborate them.
11. Diff the local packet handlers against Rajko's `GameClient.cpp`, `MessageTypes.cpp`, and `PlayerObject.cpp` behavior before changing protocol parsing semantics.

Use `docs/PACKET-RESOURCE-LEADS.md` to choose the right preservation contact, `docs/PACKET-CAPTURE-REQUEST.md` as the current request brief, `docs/PACKET-CAPTURE-OUTREACH.md` for ready-to-send outreach text, and `scripts/collect-packet-capture.sh` to bundle local `PacketLog.txt`, `UnknownRPC.txt`, `UnknownClient03Request.txt`, `DebugLog.txt`, and `ServerLog.txt` files with structured capture context, SHA-256 file hashes, skipped-file evidence for possible secret markers, and an initial triage report.

Run `scripts/triage-packet-capture.sh --source /path/to/extracted/logs-or-capture.zip --out data/experiment_artifacts/packet-captures/triage.md` on received logs before importing them. The script accepts directories plus `.zip`, `.tar`, `.tar.gz`, `.tgz`, `.tar.bz2`, `.tbz2`, `.tar.xz`, and `.txz` archives. The report matches both spaced and compact hex signatures, but it is a triage layer only; it should direct manual inspection and later parser/report updates, not replace packet context review.

After review, run `scripts/import-packet-capture.sh --source /path/to/extracted/logs-or-capture.zip --label contact-or-thread-name --sender "who sent it" --provenance "old live-server, MxOEmu, Hardline Dreams, or unknown" --context "short action notes" --dry-run` to preview the import outside the repository, then re-run without `--dry-run` to stage safe text logs under `research/packet-dumps/community-intake/`. The import script accepts directories plus `.zip`, `.tar`, `.tar.gz`, `.tgz`, `.tar.bz2`, `.tbz2`, `.tar.xz`, and `.txz` archives, rejects unsafe archive paths, writes Markdown-safe provenance, source-archive and imported-file SHA-256 hashes, flags duplicate hashes from earlier intake manifests, writes skipped-file reports, normalizes ignored `.log` inputs to tracked `.txt` files, and generates a triage report for the staged corpus.

Use `scripts/test-packet-capture-tools.sh` after changing capture intake scripts. It creates synthetic packet-log folders and archives under `/tmp`, then verifies compact/spaced triage, archive triage, text import, dry-run preview import, Markdown escaping for table-sensitive values, source/archive hashes, duplicate hash leads, skipped secret/binary evidence, `.log` normalization, and archive extraction.

## Notes

Protocol 04 request headers are encoded differently from the local enum values for two-byte commands. For example, local `CLIENT_OBJECTINTERACTION_STATIC = 0xc8` appears on the wire as `80 c8`, while `CLIENT_TELEPORT_HL = 0x18e` appears as `81 8e`. The packet research tool normalizes those forms before comparison.
