# Packet Resource Leads

This matrix tracks public preservation-community leads for packet-format research. It is for outreach and acquisition planning only; import received data through `scripts/triage-packet-capture.sh` and `scripts/import-packet-capture.sh` after manual review for provenance, secrets, and relevance.

## Priority Leads

| Priority | Lead | Evidence | Packet value | Next action |
| --- | --- | --- | --- | --- |
| P0 | RaGEZONE Hardline Dreams thread: `https://forum.ragezone.com/threads/the-matrix-online-server-emulator.1226514/?amp=1` | A 2024 Hardline Dreams contributor says they have combat packet logs, while object serialization, state, attributes, abilities, and FX remain the hard parts. | Best public lead for combat, ability, effect, equipment, health, inner-strength, and before/after protocol 03 object-state captures. | Use the Hardline Dreams combat template in `docs/PACKET-CAPTURE-OUTREACH.md`. Ask for raw text packet logs plus action context, especially object serialization before, during, and after ability use. |
| P0 | Public `Vibrantic/mxo-resurgence` packet archives: `https://github.com/Vibrantic/mxo-resurgence/tree/master/packets_and_assets` | `research/packet-dumps/mxopackets1` already imports 83 text/text-like logs. `research/packet-dumps/public-mxopackets2-warboy-triage.md` records the public `mxopackets2` Warboy/Rajko archive, SHA-256 `3dde9509d4f1b97e09938315dc591acfde2e32c5f4f1fb3153ce738f816f6341`, and triage evidence without committing 365 MB of extracted logs. A direction-fixed scratch decode now treats Warboy `Client->WORLD` / `WORLD->Client` labels as normal game directions and extracts 607,278 protocol 03 object views, 100 protocol 04 interactions, and 1,187 additional protocol 04 server payload shape samples from the external archive. It also finds Warboy static interaction ids `277874480`, `277874441`, and `959447528` correlated with tracked vendor inventory rows, plus selector-`ff` position clusters near decoded players/world rows around the repeated vendor-byte family. | Acquired public corpus evidence for vendor open/buy/sell byte signatures, CR1/CR2 static interactions, Object599 / `NPC_BASE` creation prefixes, movement wrappers/tails, ability load/unload signatures, and FX/effect ids. Vendor byte contexts are mostly embedded in protocol 03 object/state frames or unknown packet bodies, so they remain boundary targets rather than decoded top-level RPCs. The repeated static/vendor correlations and selector-`ff` coordinate anchors are stronger vendor-path/object-state leads, but still need before/after vendor-window context. | Use the tracked triage report and source URL before asking for duplicate copies. Decide on a large-corpus storage policy before importing the full extracted Warboy logs. Continue boundary work against the external extraction when focusing on vendor or object-state decoding. |
| P0 | Standalone public `hdneo/mxopackets1` and `hdneo/mxopackets2` archives: `https://github.com/hdneo/mxopackets1`, `https://github.com/hdneo/mxopackets2` | The standalone trees expose vendor, marketplace, inventory, loot, item-move, combat, ability, last-day, endgame, Rajko near-end, two-person/player-interaction, mission, code-storage, world-state, old-sniffer recursion/trade, movement, hyperjump, loading-area, hardline, door, spawn, PvP, corrupted, Mara/Cyph, combat-state, Vector, static-interaction, unknown-object, session, and create-character logs that were not in the current tracked `Vibrantic` subset. Fifteen reviewed intakes now import 176 public text or sanitized-text logs under `research/packet-dumps/community-intake/`, preserve hashes/provenance, and keep skipped safety/format-review rows in the original intake manifests; the latest session/spawn/movement intake imported 10 sanitized public text logs after NUL stripping and auth/margin handshake removal. | Checked-in top-level vendor evidence now covers 44 `CLIENT_VENDOR_BUY` records, 11 `CLIENT_VENDOR_SELL` records, 15 `SERVER_VENDOR_OPEN` records, 65 marketplace load payloads, static/dynamic object interactions, and Object599 / `NPC_BASE` vendor/loot/near-end/two-person/mission/combat/ability-loadout/Vector/session samples. The redacted Trinity vendor intake adds price-backed `00 52`/`00 55` sell IDs; the Rajko near-end intake adds `01 a8 00 00 00` / `WinterSnowballIcy` buy evidence; the remaining-small intake adds four more parsed top-level sell rows and ability-loadout/loot state context; the Vector state/static intake adds the `CodeAnalyzerTool` buy lead; the newest session/spawn/movement intake raises static/state/Object599/movement evidence without adding new parsed top-level vendor transactions. The latest checked-in report has 5,477 mode `06` boundary targets, 6,905 nested-movement suffix leads, 3,684 Object599 creation records, 2,813 post-creation object-view boundary leads, and expanded equipment/effect/state labels from the latest public slices. | Do not ask for duplicate copies of already imported standalone public logs. Next public-archive work should focus on remaining trade, manageable endgame slices, and safe non-text conversion only after secret redaction; keep raw binary/non-text logs and very large last-day files outside Git until a storage and review policy exists. |
| P0 | MxOEmu captured-packet replay thread: `https://www.mxoemu.info/forum/showthread.php?pid=1059&tid=104` | HD_Morpheus described replaying world-server packet logs through a custom Python server connected with `reality.exe`. | Potential old live-server world packet corpus or replay tooling, especially spawn/profile, player appearance, movement, and object-state packets. | Ask whether any replay logs, source, Python server archives, or raw packet text survived. Prioritize files that preserve world-server payloads around NPC/player spawns. |
| P0 | MxOEmu forum source/log request thread: `https://mxoemu.info/forum/showthread.php?tid=2801` | Public thread asks for source code and packet logs and links `fxlisthex.txt`, showing the forum has packet/source/resource holders. | Broadest public routing point for old sniffer logs, local source drops, and people who may know where old packet dumps moved. | Post a narrow request using the Object599 and vendor templates; include selector/search strings so archive holders can search without understanding the decoder. |
| P1 | Public Matrix Discord referenced from MxOEmu: `https://mxoemu.info/index.html`; public Discord backup: `https://files.mxoemu.info/MxoDiscordBackup/` | `research/community-data/discord-emulation-packet-leads.md` and `research/community-data/discord-mara-central-packet-leads.md` mine the two highest-signal Matrix Online JSON exports with `scripts/mine-discord-resource-leads.sh`. `research/community-data/discord-backup-channel-scan.md` records the channel triage and explains why smaller channels were not retained as full reports. | Good route for current maintainers, old log holders, and clarifying `vendor_items.csv`, collector RPC `8113`/`8114`/`8115`, code-storage-to-inventory gaps, Neo packet-log archive leads, combat packet-log material, packet-capture/sniffer mentions, mission-log torrents, and old replay/source artifacts. | Start with the emulation curated probes for collector/vendor/code-storage leads, then use the mara-central packet-log/capture/sniffer/mission-log rows for broader archive-holder follow-up. Ask for raw logs and source archives, not summaries. |
| P1 | MxOEmu live/public server and command guide: `https://mxoemu.info/index.html`, `https://mxoemu.info/forum/showthread.php?pid=19509&tid=2678` | Current public status supports exploration, doors, clothing vendors, item trading, animations/effects, movement abilities, and command-based item/FX/animation setup. | Practical source for new controlled captures of vendor/trade/movement/effect/door paths, even though live combat is not implemented there. | Recruit testers to run short one-action capture sessions and bundle logs with `scripts/collect-packet-capture.sh`. Use `&gib`, `&playfx`, movement, doors, hardlines, and clothing vendors to make captures reproducible. |
| P1 | MxOEmu public data host: `https://files.mxoemu.info/` | `research/community-data/MANIFEST.md` now tracks imported small text resources and hashes for public abilities, items, FX, normalized gameobjects, item commands, and derived vendor prices. The host also exposes larger full-goprops and Python/resource archives. | Semantic references for item ids, vendor prices, abilities, FX ids, object ids, and script-side vendor/collector behavior. These are not packet captures but help label payload fields and prove when decoded packet fields are not direct public IDs. | Keep imported runtime data small and tracked. Treat larger archives such as `pythonMXO.zip` and `fullgoprops.7z` as external references unless a parser needs a compact derived artifact. |
| P2 | Furious Angels MxO Emulation thread: `https://www.thefuriousangels.com/index.php?topic=5917.0` | A 2009 veteran-community thread discusses early Rajko MxOEmu/proxy work, an old `svn2.xp-dev.com/svn/rajkosto-mxoemu/` checkout, compiled RealityD-era files, and packet-log interpretation. The old XP-Dev SVN host did not resolve during the current check, so this is not an acquired public source. | Historical archive-holder lead for mirrors or local copies of early Rajko proxy source, packet logs, and packet-analysis notes that may predate the current public GitHub mirrors. Could be useful for confirming sniffer/proxy assumptions and recovering packet logs that did not make it into `mxopackets1`/`mxopackets2`. | Ask Furious Angels admins or old thread participants whether any SVN mirror, proxy source, compiled package notes, or packet-log archives survived. Request raw text logs and source archives only after provenance review. |
| P2 | MxO client protocol forum thread: `https://mxoemu.info/forum/showthread.php?tid=7` | HD_Morpheus documented login/auth challenge-response concepts and certificate/session-key strings from the client. | Useful background for margin/auth/encryption context, but less directly useful for decoded world object payloads. | Preserve as context. Do not let it displace world-server packet acquisition unless login/auth work becomes a blocker. |
| P2 | Public source references: `https://github.com/rajkosto/mxoemu`, `https://github.com/TasteeWheat/mxoemu1` | Current research already mines Rajko RPC dispatch/sequence handling and TasteeWheat hardcoded door/hardline protocol 03 examples. | Good for validating header maps, local queued-command constants, hardcoded object examples, and script semantics. | Diff focused source areas before parser changes: `GameClient.cpp`, `MessageTypes.cpp`, `PlayerObject.cpp`, hardline/door command examples, and Python/resource hooks. |

Current Warboy selector-`ff` resource correlation: the scratch JSON now gives candidate item, game-object, animation, and ability names for 2,422 repeat-list field-summary groups, 3,567 full entry-1 -> entry-2 -> continuation -> post-marker shape summaries, and 3,662 normalized effective-shape summaries. The high-count entry-1 families are clothing item/game-object IDs with animation-symbol collisions, while the largest primary entry-2 families (`ff 00 / 255`, `66 00 / 102`) currently match only animation symbols. The new field-layout export keeps those entry-2 leaders structurally separate: Warboy entry 2 has 11,997 zero-padded u16 candidates, 10,754 marker-adjacent u16 candidates, 2,494 immediate u16 candidates, and 148 no-field candidates. The layout-prefix roll-up adds 99 Warboy groups; the largest is long-zero entry 1 / marker-adjacent entry 2 / `00 01 00 ff` / immediate post-marker with 3,308 candidates across 235 exact shapes. The secondary-slot export shows 15,047 Warboy entry-2 records have a nonzero local `+6` field, led by `ff 00 -> 66 00` (3,179). The effective-field export now normalizes marker-adjacent entry-2 rows to that secondary slot when present: 10,754 Warboy candidates use the secondary source, and `66 00 / 102` becomes the dominant effective entry-2 value with 9,743 observations. The new `entry 2 effective` field-summary role adds 298 normalized Warboy groups, 249 with imported resource references, while the effective-shape table exposes source splits such as `ba 13 / 5050 -> 66 00 / 102 -> 00 01 00 ff -> f0 0f / 4080` with 878 candidates all sourced from secondary slot `66 00` behind primary marker `ff 00`. The header-correlation export adds 892 Warboy effective-shape groups with known local RPC-header byte windows, covering 2,843 candidate instances; 81 candidate instances include vendor-header windows, led by the all-vendor `ba 13 / 5050 -> 21 0a / 2593 -> 00 41 00 ff -> b0 74 / 29872` shape with `SERVER_VENDOR_OPEN` at selector-payload `+11` / object `+29` inside `e0 2d 81 0d`. The header-name rollup adds 89 Warboy rows and three vendor rows: `SERVER_VENDOR_OPEN` has 49 candidates across 15 shapes, `CLIENT_VENDOR_SELL` has 27 across 11, and `CLIENT_VENDOR_BUY` has five across five. Downstream searches can now use the likely data slot and vendor-byte-window labels without discarding primary marker evidence. Top combined shapes are resource-labeled enough to use as stable search/debug labels, but not as proof of field semantics.

The header-sample export now preserves concrete byte slices for follow-up decoding. `Protocol03SelectorFfRepeatListHeaderSamples` is capped at 120 Warboy rows and includes all 81 vendor de-duplicated windows: 49 `SERVER_VENDOR_OPEN`, 27 `CLIENT_VENDOR_SELL`, and five `CLIENT_VENDOR_BUY`, each with source file/line, selector payload/object offsets, `HeaderRegion`, header context, repeat-list context, normalized shape fields, and decoded position anchors. The Warboy vendor region split is 28 `field +9 u32`, 27 `continuation body`, 22 `pre-position body`, and four `repeat entry 1` hits.

`Protocol03SelectorFfRepeatListHeaderRegionSummaries` now keeps the same evidence grouped for search and follow-up: Warboy has 207 header-region rows, including 10 vendor rows across `SERVER_VENDOR_OPEN`, `CLIENT_VENDOR_SELL`, and `CLIENT_VENDOR_BUY`. The vendor rows preserve all 81 windows while making the region split directly queryable for parser tests and archive searches. They now also carry resource-reference labels: all 10 vendor rows have entry-1 and effective entry-2 refs, and 9 have post-marker refs. Highest-value search labels include `SERVER_VENDOR_OPEN` / `field +9 u32` / `21 0a` / `Shield_F_Coat_A4_C5_5`, `CLIENT_VENDOR_SELL` / `pre-position body` / `ba 13 -> 66 00`, and `CLIENT_VENDOR_BUY` / `continuation body` / `21 0a`.

`Protocol03SelectorFfRepeatListContinuationMarkerSummaries` now gives parser tests a compact target for the continuation marker hypothesis: Warboy has 405 marker rows covering all 25,393 modeled two-entry candidates, and all rows use marker byte `ff`. The largest prefix families are `00 01 00`, `00 01 10`, `00 00 00`, and `00 01 04`, which are good search labels for old archives and controlled captures. The same rows now carry header-window evidence plus resource-reference labels for entry 1, effective entry 2, and post-marker fields: 22 marker families include all 81 vendor windows, 384 Warboy rows have entry-1 refs, 386 have effective entry-2 refs, and 339 have post-marker refs. The export also separates vendor-only resource labels: 21 vendor-bearing rows have vendor-only entry-1 refs, 21 have vendor-only effective entry-2 refs, and 16 have vendor-only post-marker refs. The highest-signal vendor rows are now searchable by both marker shape and vendor-only resource label: `00 41 00 ff -> b0 74` has 19 `SERVER_VENDOR_OPEN` windows, effective entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5`; `00 01 00 ff -> b0 74` has 16 vendor windows, entry-1 refs led by `NPCVendorArchStandOutdoor_GatherRound` / `Fclothing_Shirt_A12_C13`, and the same post-marker refs; `00 00 00 ff -> 1b ee` has nine `CLIENT_VENDOR_SELL` windows with entry refs `ba 13 / 5050 -> 66 00 / 102`; and `00 01 00 ff -> fd 0f` has six vendor windows plus the `Aggro_StandMovementDisruption_WalkF` post-marker animation ref. The tracked Warboy triage artifact now also records raw archive-search counts for these strings: 80 hits for `e0 2d 81 0d`, 42 for the fixed `01 43 ... e0 2d 81 0d 22 89 20` body prefix, 54 for `00 41 00 ff 00 00 00 00 b0 74`, and 13 for `00 00 00 ff 00 00 00 00 1b ee`.

The newest vendor target exports classify spans so outreach and parser work can separate likely collisions from useful body-length targets. Warboy continuation-body vendor windows now classify as 22 deep body tails, four mid-body tails, and one short tail after the matched header bytes; pre-position vendor windows classify as nine near-position overlaps, 10 mid-body gaps, and three deep pre-position gaps. The boundary interpretation roll-up now calls out continuation body tiers directly: 21 deep-body targets / 22 windows, three mid-body targets / four windows, and one short-tail target / one window. It also calls out pre-position tiers directly: nine mid-body targets / 10 windows, three deep-body targets / three windows, and nine near-position targets / nine windows. New `TargetBodySpanBytes` values expose exact remaining-byte counts, such as `continuation tail 125` or `pre-position gap 53`, for archive searches and parser tests. Use the short-tail and near-position rows cautiously and prioritize captures or old logs that preserve enough bytes around the mid/deep continuation and pre-position bodies.

Current nested-movement evidence now has the same machine-readable action split. The committed corpus exports 5,477 mode `06` object-state boundary targets with no bounded wrapper examples, led by inner selectors `0e` (2,974), `0c` (2,235), `08` (206), and `0a` (62). The dominant suffix remains `ff 00 00 00 10 00 00 00` (4,181 windows), followed by bounded-marker-looking `80 80 80 80 80 80 01 12`, `80 80 80 80 80 80 01 07`, and `80 80 80 80 c0 4c 0a 00` controls. The earlier Warboy-inclusive scratch report remains a useful external comparison set for the same suffix class. Parser work can start with the checked-in mode `06` queue, then compare against Warboy when a body-length hypothesis survives local evidence.

Current Object599 / `NPC_BASE` dynamic-tail evidence is also machine-readable now. The committed corpus parses 3,684 Object599 creation records; all have post-attribute bytes, 1,558 include `00 0c 57 02` inside the first eight suffix bytes, and 2,813 expose a plausible next object-view header inside the first eight suffix bytes. Those boundary leads include 230 distinct bounded pre-header byte sequences; 2,540 use a three-byte `<value> 00 00` pre-header, and selector-length plus dynamic-creation boundary parsing now accounts for 2,383 follow-up object-view headers without consuming them into the parent record. The dynamic-tail exports now include seven tail-summary rows, 220 header-like lead rows, 30 body-anchor rows, and 43 extended-boundary rows after common view-1 selector `50`/`67`/`68`/`69`/`76`/`90`/`a3`/`d0` follow-up headers were promoted into bounded Object599 boundary parsing. The earlier Warboy-inclusive scratch report remains a useful external comparison set with larger body-anchor rows that split selector-body bytes before and after embedded `cd ab` separators. Ask archive holders for logs preserving at least 600 bytes after `0c 57 02 <spawn counter> cd ab <attribute count> 8e <CharacterName>`, especially around those post-creation view selectors.

## Search Strings To Include

Use these search strings when asking contacts to scan old archives:

```text
PacketLog.txt
UnknownRPC.txt
UnknownClient03Request.txt
DebugLog.txt
ServerLog.txt
sniffer
world server
reality.exe
python server
combat packet logs
vendor_items.csv
Neo packet logs
hdneo/mxopackets1
003_vendor_weapon.log
008_buyed_weapons_for_30000
sell_item_FM13sk_saikungoperative.log
sellawakened.log
use_item_vendor_sai_kung.log
SERVER_LOAD_MARKETPLACE
Furious Angels
rajkosto-mxoemu
svn2.xp-dev.com/svn/rajkosto-mxoemu
RealityD
Proxy Solution
code storage
mission logs
collector 8113
collector 8114
collector 8115
e0 2d 81 0d
01 43 00 00 00 00 00 00 00 e0 2d 81 0d 22 89 20
00 41 00 ff
00 01 00 ff
00 01 04 ff
00 01 10 ff
00 00 00 ff
00 41 00 ff 00 00 00 00 b0 74
00 00 00 ff 00 00 00 00 1b ee
0c 57 02
00 0c 57 02
01 00 08 50
01 00 08 67
01 00 08 68
01 00 08 69
01 00 08 90
01 00 08 a3
01 00 08 d0
01 00 0c 76
```

## Intake Rules

1. Ask for raw text packet logs and context notes, not screenshots or summaries.
2. Do not accept account passwords, private keys, client binaries, or unrelated asset archives into the tracked corpus.
3. Run `scripts/triage-packet-capture.sh --source <path> --out <triage.md>` before importing.
4. Preview with `scripts/import-packet-capture.sh --source <path> --label <lead> --sender <sender> --provenance <source> --context <notes> --dry-run`.
5. Import only reviewed text-like packet logs, then regenerate `docs/generated/packet-research-summary.md` and `.json` with `scripts/packet-research.sh`.

## Current Ask By Lead

| Lead | Ask |
| --- | --- |
| Hardline Dreams / RaGEZONE | Combat packet logs with object serialization, ability state, FX, equipment, health, inner-strength, and before/after object attributes. |
| Public `mxopackets1` / `mxopackets2` archives | No duplicate ask yet. Use the existing `Vibrantic` import, the Warboy triage report, and the selected standalone `hdneo` vendor/sell/combat/ability/marketplace/redacted-Trinity/inventory-last-day/Rajko-near-end/two-person/mission-code/Luizoe-old-sniffer/movement-spawn-hardline/combat-PvP-Rajko-state/remaining-small-ability-loot-market/Vector-state-static/Vector-supplemental/session-spawn-movement intakes first; continue targeted public hdneo imports for remaining trade and manageable endgame slices before choosing large-corpus storage for the 365 MB Warboy extraction or huge last-day logs. |
| HD_Morpheus replay thread | Old world-server replay logs, raw packet text, Python replay server source, or notes about the packet-log format it consumed. |
| MxOEmu forum | Old sniffer captures, source drops, `PacketLog.txt` archives, Object599 / `NPC_BASE` spawn tails, vendor-open/buy/sell captures, and archive-holder introductions. |
| Discord/community maintainers | Pointers to old log holders, `vendor_items.csv` provenance, collector/code-storage RPC captures, and static-vendor override history. |
| Furious Angels veterans/admins | Mirrors or local copies of early Rajko proxy/SVN source, RealityD-era packages, packet-log archives, or packet-analysis notes from the 2009 MxO emulation thread. |
| MxOEmu testers | New controlled captures for vendor open, buy, sell, trade, hardline arrival, movement burst, door/static object interaction, animations/effects, and inventory changes. |
