# Project TODO

This checklist tracks the current modernization push from "builds locally" to "fresh clone is runnable and verifiable."

## 1. Bootstrap and Runtime
- [x] Make `Config.xml.dist` match the parser, or make the parser support the checked-in config shape.
- [x] Add a tracked `WorldConfig.xml.dist` and make local setup copy it to `WorldConfig.xml`.
- [x] Make data-file loading work on macOS/Linux and Windows.
- [x] Copy required config/data files to build output where needed.
- [x] Document the actual one-command local setup path.
- [x] Verify a fresh clone can reach the server-ready log with Docker MySQL.
  - Verified locally with Homebrew `docker` + `docker-compose` + Colima on Apple Silicon macOS.
  - `scripts/smoke-local.sh` reached `Smoke test passed: server reached ready state.`

## 2. Automated Tests
- [x] Add a real test project to the solution.
- [x] Cover config parsing for old and new config shapes.
- [x] Cover path-stable data loading helpers.
- [x] Cover `SpatialGrid`, `ViewManager`, and combat calculator edge cases.
- [x] Keep startup health checks separate from automated tests.

## 3. CI
- [x] Add GitHub Actions for restore, build, and test.
- [x] Run CI on pull requests and pushes to `master`.
- [x] Cache NuGet packages.

## 4. Validation Harness
- [x] Add a smoke script for config, database, build, and launch readiness.
- [x] Add a load-test harness for 50/100/200 simulated clients.
- [ ] Record CPU, tick duration, queue depth, and packet flush latency.
- [ ] Verify player, mob, static object, and subway view spawn/despawn behavior.
  - The current harness sends lightweight UDP client load; full view validation still needs a running server and client/protocol fixtures.

## 5. First Gameplay Economy Loop
- [x] Make vendor buys deduct cash.
- [x] Reject vendor buys when cash is insufficient.
- [x] Send updated cash to the client after a vendor transaction.
- [x] Implement a basic vendor sell path or document the packet format still needed.
- [x] Add tests around pricing and cash updates where possible.

## 6. Packet Research
- [x] Inventory public MxO preservation packet/protocol resources.
- [x] Import useful text packet dumps into `research/packet-dumps`.
- [x] Record source metadata for imported packet dumps.
- [x] Triage the public `mxopackets2` Warboy/Rajko packet archive without unpacking it into Git.
  - `research/packet-dumps/public-mxopackets2-warboy-triage.md` records the public `Vibrantic/mxo-resurgence` archive URL, Git blob SHA, SHA-256 `3dde9509d4f1b97e09938315dc591acfde2e32c5f4f1fb3153ce738f816f6341`, and focused packet-signature evidence. The archive expands to about 365 MB and includes two files over GitHub's 100 MB limit, so it remains an external corpus until a large-file storage policy is chosen. After teaching the packet research tool to treat Warboy `Client->WORLD` / `WORLD->Client` packet labels as normal game client/server directions, a scratch run over the committed corpus plus an external Warboy extraction produced 1,712,918 packet-like lines, 607,278 protocol 03 object views, 100 protocol 04 interaction payloads, 1,187 additional protocol 04 server payload shape samples, and embedded byte-window hits for vendor buy, sell, and open headers. The tracked triage now adds raw selector-`ff` vendor-body search evidence, including 80 `e0 2d 81 0d` hits, 42 fixed vendor-body-prefix hits, 54 vendor-open resource-shape hits, and 13 vendor-sell resource-shape hits. It also found three Warboy static interaction ids with tracked vendor inventory rows: `277874480`, `277874441`, and `959447528`. The vendor signatures are parser targets, not final decoded vendor exchanges yet, because the protocol 04 block parser still reports zero parsed top-level vendor RPC hits and context grouping places the byte windows mostly inside protocol 03 object/state frames or unknown packet bodies. `scripts/packet-research.sh` now supports `PACKET_RESEARCH_EXTRA_DUMPS`, `PACKET_RESEARCH_JSON`, and `PACKET_RESEARCH_MARKDOWN`; `scripts/run-warboy-packet-research.sh` wraps that path with archive download/reuse, SHA-256 verification, safe extraction, triage, stable external labels, and ignored scratch report outputs.
- [x] Add a repeatable packet research tool for RPC map comparison and dump summarization.
- [x] Generate `docs/generated/packet-research-summary.md` and `.json`.
- [x] Import the public MxOEmu FX symbol list and cross-reference packet payload windows.
- [x] Parse animation definitions and cross-reference two-byte packet field summaries.
  - `tools/PacketResearch` now loads 5,442 rows from `data/animations.txt` and uses them alongside item, game-object, and ability resources when enriching protocol 03 selector-`ff` repeat-list field summaries.
- [x] Import decoded MxOEmu `goprops` vendor price data and use it for runtime vendor pricing.
  - `data/vendor_prices.csv` has 41,788 price rows generated from public `goprops.7z`; `VendorPricing` now uses decoded `VendorPrice` and `NonSellable` values with the previous default as fallback.
- [x] Track small public MxOEmu semantic resources with hashes.
  - `scripts/import-mxoemu-community-data.sh` now imports `abilities.txt`, `fxlisthex.txt`, `fxlistreal.txt`, normalized public `gameobjects.csv`, `invitems.txt`, and `invitems_command.txt` under `research/community-data/`, then writes `research/community-data/MANIFEST.md` with URLs, bytes, line counts, SHA-256 hashes, Last-Modified, and ETags.
- [x] Mine public MxOEmu Discord exports for packet-resource leads.
  - `scripts/mine-discord-resource-leads.sh` now turns the public 34,471-message `emulation` JSON export into `research/community-data/discord-emulation-packet-leads.md` with source hashes, term counts, message anchors, and paraphrased packet-resource probes for collector RPC `8113`/`8114`/`8115`, code-storage-to-inventory gaps, incomplete vendor log coverage, Neo packet-log archives, combat packet-log material, old sniffer code, mission-log archives, and replay/source inputs. It also mines the 97,015-message `mara-central` export into `research/community-data/discord-mara-central-packet-leads.md`, now with curated probes for the public GitHub packet corpus, Rajkosto sniffer provenance, observed-only NPC spawn/log limitations, final-shutdown-window coverage, last-day log holders, combat packet decoding notes, server-resource priority, and a tutorial packet-log parser/document lead. `research/community-data/discord-backup-channel-scan.md` records the checked-but-not-retained `mxo-faq`, `cmnty-mish-chat`, and `matrix-games` channels.
- [x] Decode normal vendor buyback pricing from public MxOEmu Python resources.
  - `pythonMXO.zip` contains `resource/Python/Monolith.ltp/rshooks.py`; normal buyback is `VendorPrice / 10`, recycle info is `VendorPrice / 20`, and the default vendor price is `1000`. Runtime normal-item sell pricing now follows the one-tenth rule.
- [x] Implement full ability-code vendor buyback semantics.
  - Runtime sell pricing now follows the high-bit ability-code branch from public `rshooks.py`, including base ability-code lookup, level-based premiums, and the mapped full-price exception list for specific abilities.
- [x] Decode protocol 04 object-interaction payload fields used by the local static/dynamic interaction handlers.
  - Current generated report finds 316 top-level object-interaction payloads: static `80 c8` rows with object types `0`, `2`, `3`, and `4`, and dynamic `80 c7` rows with object types `1`, `2`, `3`, `4`, `5`, `6`, `8`, `9`, `13`, and `14`. More captures should pair those interaction payloads with before/after vendor, loot, inventory, and object-state context.
- [x] Mine raw external `HandleCommand` debug logs for object-interaction command examples.
  - TasteeWheat's `Reality.log` contributes two raw CR2 static interaction commands (`80 c8 02 00 20 4e 03 00`), confirming the same payload shape outside the top-level packet dump corpus.
- [x] Correlate decoded object-interaction ids with tracked vendor inventory rows.
  - `data/vendor_items.csv` has 44 parser-derived vendor inventory rows from HD_Neo's old log parser. The generated packet report now flags matching static object ids including `888143880`, `277874426`, `277874441`, `277874480`, and the external raw-command object `1310720002`.
- [x] Correlate decoded object-interaction ids with tracked static object rows.
  - The generated packet report now resolves decoded packet-dump static interaction ids and the external raw-command id to tracked `data/staticObjects_*.csv` rows using the same little-endian `mxoId` interpretation as the runtime loader. Repeated door, environment/elevator-panel, and human-NPC/vendor-path matches remain high-priority vendor-open capture targets rather than final vendor semantics.
- [x] Safely import redacted Trinity vendor-session derivatives from standalone `hdneo/mxopackets1`.
  - Two previously skipped public Trinity vendor-session logs are now imported as sanitized derivatives after removing password-bearing auth lines and stripping one NUL byte from each source. Their triage adds two `SERVER_VENDOR_OPEN` hits, five `CLIENT_VENDOR_SELL` hits, 610 `80 bc` object/state bundle hits, 24 ability-load hits, and 11 ability-unload hits without tracking the original unsafe source lines.
- [x] Import focused public inventory, last-day, and Rajko near-end packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T015831Z-hdneo-public-inventory-lastday-rajko-nearend-selected` imports 11 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` logs after removing password-bearing auth lines from affected sources and stripping NUL bytes. Its triage adds four `SERVER_VENDOR_OPEN` hits, 29 `CLIENT_VENDOR_BUY` hits, seven `CLIENT_VENDOR_SELL` hits, 7,150 `80 bc` object/state bundle hits, 96 CR1/CR2 static-object interaction signature hits, and selector-`ff` continuation-prefix evidence from `enti_lastday2`.
- [x] Import public two-person/player-interaction packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T022135Z-hdneo-public-two-person-actions-sanitized-selected` imports two reviewed public `hdneo/mxopackets1` two-person logs after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds five `SERVER_VENDOR_OPEN` byte-window hits, one `CLIENT_VENDOR_BUY` byte-window hit, five `CLIENT_VENDOR_SELL` byte-window hits, 1,814 `80 bc` object/state bundle hits, 317 Object599 / `NPC_BASE` creation-prefix hits, six static-object interaction signature hits, a new dynamic `80 c7` object type `8` interaction for object id `10092570`, embedded vendor byte-window context, and the expanded mode `06` movement-boundary queue.
- [x] Import public mission/code/Rajko world-state packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T024558Z-hdneo-public-mission-code-rajko-worldstate-sanitized-selected` imports 15 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` mission, code-storage, Rajko, and world-state logs after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds six `SERVER_VENDOR_OPEN` byte-window hits, three `CLIENT_VENDOR_BUY` byte-window hits, 8,661 `80 bc` object/state bundle hits, 479 ability-load hits, 515 ability-unload hits, 150 CR1 static-object interaction signatures, 14 CR2 static-object interaction signatures, 341 Object599 / `NPC_BASE` creation-prefix hits, mission/code state-bundle context, and additional mode `06` movement-boundary evidence.
- [x] Import public Luizoe old-sniffer recursion/trade packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T030922Z-hdneo-public-luizoe-old-sniffer-selected` imports five reviewed public `hdneo/mxopackets1` old-sniffer text logs with no redaction and no skipped files. Its triage adds one `SERVER_VENDOR_OPEN` byte-window hit, two `CLIENT_VENDOR_BUY` hits, 22 `CLIENT_VENDOR_SELL` hits, 2,444 `80 bc` object/state bundle hits, 580 Object599 / `NPC_BASE` creation-prefix hits, 568 adjacent Object599 boundary-prefix hits, 68 CR2 and 67 CR1 static-object interaction signatures, 75 ability-load hits, 36 ability-unload hits, and dense mode `06` movement-selector evidence. The regenerated report keeps top-level vendor transaction counts unchanged while adding embedded vendor-window controls, more Object599 creation coverage, and new equipment/effect labels such as `b4 02 00 00` / AutomaticRifle1 and `cd 08 00 28` / Intense Attack.
- [x] Import public movement, spawn, hardline, and door packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T032533Z-hdneo-public-movement-spawn-hardline-sanitized-selected` imports 23 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` logs after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds four `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_BUY` hits, five `CLIENT_VENDOR_SELL` hits, 10,696 `80 bc` object/state bundle hits, 340 Object599 / `NPC_BASE` creation-prefix hits, 333 adjacent Object599 boundary-prefix hits, 43 CR2 and 93 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, and movement-state tail samples. The regenerated report keeps top-level vendor transaction counts unchanged while expanding mode `06` movement-boundary evidence, Object599 creation/correlation rows, static door/object leads, PlayerCharacter creation records, and equipment/effect labels such as Dual Stitchers, D-A Arms Stitcher, Borenov GL-35, Edson BR-19, Fast Healing, and active accelerated-spawn FX.
- [x] Import public combat, PvP, corrupted, and Rajko Mara/Cyph/state packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T191353Z-hdneo-public-combat-pvp-rajko-state-selected` imports 12 reviewed public `hdneo/mxopackets1` and `hdneo/mxopackets2` logs after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds 20 `SERVER_VENDOR_OPEN` byte-window hits, 14 `CLIENT_VENDOR_BUY` hits, 13 `CLIENT_VENDOR_SELL` hits, 7,493 `80 bc` object/state bundle hits, 725 Object599 / `NPC_BASE` creation-prefix hits, 708 adjacent Object599 boundary-prefix hits, 14 CR2 and 61 CR1 static-object interaction signatures, 284 ability-load hits, 307 ability-unload hits, selector-`ff` continuation-prefix evidence, dense mode `06` movement-selector evidence, and FX/effect-id leads including adrenaline booster and active accelerated-spawn IDs. The regenerated report adds one parsed top-level `SERVER_VENDOR_OPEN` and expands combat-state equipment/effect labels; the following remaining-small intake now carries the current corpus totals.
- [x] Import remaining small public ability, loot, and marketplace packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T200024Z-hdneo-public-remaining-small-ability-loot-market` imports seven reviewed public `hdneo/mxopackets1` text logs without redaction or skipped files. Its triage adds four `SERVER_VENDOR_OPEN` byte-window hits, two `CLIENT_VENDOR_BUY` hits, five `CLIENT_VENDOR_SELL` hits, 3,043 `80 bc` object/state bundle hits, 140 Object599 / `NPC_BASE` creation-prefix hits, 137 adjacent Object599 boundary-prefix hits, five CR2 and 54 CR1 static-object interaction signatures, 807 ability-load/player-attribute hits, 387 ability-unload hits, 724 adrenaline-booster effect-id hits, 17 active accelerated-spawn FX hits, and equipment labels such as Heavy Meson Rifle and System FM-1500 Magnum.
- [x] Import public Vector state/static packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T203107Z-hdneo-public-vector-state-static-selected` imports 12 reviewed public `hdneo/mxopackets1` Vector old-world, teleport, unknown-object, and disconnect traces after stripping NUL bytes from `Vector35` only. Its triage adds 20 `SERVER_VENDOR_OPEN` byte-window hits, six `CLIENT_VENDOR_BUY` hits, four `CLIENT_VENDOR_SELL` hits, 15,180 `80 bc` object/state bundle hits, 1,028 Object599 / `NPC_BASE` creation-prefix hits, 998 adjacent Object599 boundary-prefix hits, 350 CR2 and 237 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, 3,355 ability-load/player-attribute hits, and 4,141 ability-unload hits. The regenerated report adds one parsed `SERVER_VENDOR_OPEN`, one parsed `CLIENT_VENDOR_BUY` (`13 02 00 00 00` / `CodeAnalyzerTool`, no observed price match yet), and one marketplace row.
- [x] Import public Vector supplemental state/static packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T205703Z-hdneo-public-vector-supplemental-state-selected` imports 16 reviewed public `hdneo/mxopackets1` Vector, login, Syntax, and old-world text logs without redaction or skipped files. Its triage adds eight `SERVER_VENDOR_OPEN` byte-window hits, five `CLIENT_VENDOR_BUY` hits, six `CLIENT_VENDOR_SELL` hits, 12,508 `80 bc` object/state bundle hits, 896 Object599 / `NPC_BASE` creation-prefix hits, 876 adjacent Object599 boundary-prefix hits, 149 CR2 and 162 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, 419 ability-load/player-attribute hits, 335 ability-unload hits, 1,406 adrenaline-booster effect-id hits, 188 fast-healing effect-id hits, and a single active accelerated-spawn FX hit. The following session/spawn/movement intake now carries the current corpus totals.
- [x] Import public session, spawn, movement, and static-interaction packet slices from standalone hdneo archives.
  - `research/packet-dumps/community-intake/20260607T212233Z-hdneo-public-session-spawn-movement-sanitized-selected` imports 10 reviewed public `hdneo/mxopackets1` Luizoe recursion, unsorted proxy, create/session, idle, Vector4, and Unknown traces as sanitized derivatives after stripping NUL bytes and removing auth/margin handshake blocks through `MS_EstablishUDPSessionReply`. Its triage adds six `SERVER_VENDOR_OPEN` byte-window hits, 42 `CLIENT_VENDOR_BUY` hits, seven `CLIENT_VENDOR_SELL` hits, 16,249 `80 bc` object/state bundle hits, 14 CR2 and 122 CR1 static-object interaction signatures, selector-`ff` continuation-prefix evidence, Object599/profile context, and movement/session/create evidence. The regenerated report expands the checked-in corpus to 257 packet-like files and 1,000,047 packet-like lines, raises mode `06` boundary targets to 5,477, raises unresolved nested-movement suffix leads to 6,905, raises Object599 creation records to 3,684, raises post-creation object-view boundary leads to 2,813, raises Object12 / `PlayerCharacter` creation records to 1,817, and raises marketplace payloads to 65.
- [ ] Acquire or capture vendor buy/sell packet dumps with known before/after cash and inventory state.
  - The checked-in corpus now includes selected public `hdneo` vendor, Rajko near-end, mission/code, combat-state, ability-loadout, loot, marketplace-lockpicks, Vector state/static, Vector supplemental, and session/spawn/movement logs with 44 parsed top-level `CLIENT_VENDOR_BUY` (`81 0e`) records, 11 parsed top-level `CLIENT_VENDOR_SELL` (`81 11`) records, 15 parsed top-level `SERVER_VENDOR_OPEN` (`81 0d`) records, and 65 parsed `SERVER_LOAD_MARKETPLACE` (`81 25`) records. Before/after cash and inventory semantics are still missing.
  - The generated report now exports 70 top-level vendor transaction-flow rows. Buy request payload prefixes split across `c1 09`, `c1 1e`, `c6 1e`, `cb 1e`, `01 a8 00 00 00`, `00 08 00 80 00`, and `13 02 00 00 00`; many are followed by `5e -> 80 e7 -> 80 e4 -> 81 0f`, while the Rajko Icy Snowball buys are followed by `69 -> 80 e7 -> 80 e4 -> 81 0f`. Sell requests now include payloads `00 45`, `00 46`, `00 4e`, `00 52`, `00 55`, `00 57`, and `00 58`, followed by `80 e7 -> 80 e4 -> 5f -> 81 12` or adjacent sell-neighborhood rows. The buy-price join decodes four 30000-price weapon rows, `WinterSnowballIcy` (`01 a8 00 00 00`, price 100, six matched cash deltas and buy values), high-bit `AwakenedAbility`, and an unmatched `CodeAnalyzerTool` lead (`13 02 00 00 00`, vendor price 1000, no observed cash/value follow-up yet); the sell-price join now decodes `Shotgun2` (`00 55`, expected sell 900), `Shield_M_Shoes_A27_C3_1` (`00 52`, expected sell 1560), `FM-13SK` (`00 58`, expected sell 5000), and `AwakenedAbility` (`00 58`, expected sell 10). The matched rows align with observed cash deltas and positive buy/sell values, and remain the current packet-neighborhood targets for cash/inventory delta decoding.
  - The generated JSON now also exports parsed `VendorOpenItemLists`, `VendorBuyTransactions`, `VendorSellTransactions`, `VendorBuyPriceLeadSummaries`, and `VendorSellPriceLeadSummaries`, preserving the `81 0d` header bytes, list-start field, count field, candidate float/raw header fields, open-list item/ability ids, buy request item ids, following packet cash deltas, buy/sell values, value-kind fields, sell response candidate item/ability fields, resource-table names, expected prices, buyback values, and price-match classifications for downstream decoder tests.
  - The transaction exports now include `5e` buy ack sequence fields, echoed buy request prefixes, buy tail markers, Rajko `69` candidate buy ack fields (`36 00` / 54 sequence and `u32 +5` step fields `4`, `8`, `12`, `20`, `24`, `32`), and `5f` sell ack item/flag fields, with price-summary rollups for ack sequences, ack detail fields, item values, and flags.
  - The selected public intakes also add parsed static `CLIENT_OBJECTINTERACTION_STATIC` (`80 c8`) payloads across object types `0`, `2`, `3`, and `4`, parsed `CLIENT_OBJECTINTERACTION_DYNAMIC` (`80 c7`) payloads across object types `1`, `2`, `3`, `4`, `5`, `6`, `8`, `9`, `13`, and `14`, plus explicit sell, vendor-use, and two-person source context. More captures should focus on repeated static object type `0`, `2`, and `3` vendor interactions with known before/after inventory state.
  - The external public Warboy/Rajko `mxopackets2` triage report and direction-fixed scratch packet-research run show candidate vendor-open, vendor-buy, and vendor-sell byte windows, but those logs are not yet imported into the generated checked-in packet research summary because the archive expands to 365 MB. The Warboy-inclusive report now separates parsed protocol 04 RPC hits from embedded byte hits: vendor buy, sell, and open all have zero parsed top-level RPC hits, while their byte windows mostly sit inside server protocol 03 object/state frames or unknown packet bodies. The next step is packet-boundary work rather than assuming these are top-level vendor RPCs.
  - The same Warboy-inclusive report now groups immediate embedded-vendor byte windows and joins them to the containing protocol 03 object-view segment when packet offsets are available. The strongest repeated `SERVER_VENDOR_OPEN` boundary lead appears in server protocol 03 unknown-line contexts, with prefixes such as `00 00 00 00 e0 2d` before `81 0d` and stable suffix `22 89 20 00 0b 0c 11 79 80 00`; exact containment points at count-3 first-selector `0c` object views whose unresolved selector `ff` payload contains the window at object-relative offset `+29`, plus unresolved appearance/attribute and selector `2a`/`2e` tails.
  - The new variable-tail report broadens that exact containment into selector-body families. The top Warboy vendor-bearing family is selector `ff` in count-3 first-selector `0c` object views with lead prefix `01 43 00 00 00 00 00 00 00 e0 2d 81 0d 22 89 20`; 21 records carry `SERVER_VENDOR_OPEN` at selector-payload offset `+11` / object-relative offset `+29`. The nearby-four-byte window is consistently `e0 2d 81 0d` at payload offset `+9`, so this is likely packed selector body data rather than a clean nested vendor-open RPC boundary. The selector-`ff` packed-field table turns that into byte-slice targets and now reports resource-table refs for `u32 +9`; the repeated `e0 2d 81 0d / 226569696` value and the other top Warboy selector-`ff` values have no imported FX, game object, ability, item, static object, or vendor inventory match. The stable byte targets remain bytes `+13 = 22 89 20`, bytes `+16 = 00 0b 0c 11 79 80 00 00`, and bytes `+31 = 00 02 ff 00 00 00 00 ba`, with the `+24` slice splitting mostly between `01 00 00 00 00 12 01 00` and `01 00 00 00 00 1b 01 00`.
  - The selector-`ff` position-like pass gives the next body anchor: in the Warboy scratch report, 21,434 of 31,765 selector-`ff` tails expose conservative 24-byte little-endian xyz triples. The same vendor-bearing `e0 2d 81 0d` family has the triple at payload offset `+51`; the largest first-`0c` row has 16 records at `17595.543,495,2447.504`, the alternate first-`0c` row has three records at `17615.635,495,1816.967`, and first-`0e` rows match those positions. The nearest decoded-position hints now put those clusters near existing player/world coordinates: `17595.543,495,2447.504` is 85.4 units from player `Se7` and 102.2 units from `Zion Duelist`; `17615.635,495,1816.967` is 43.7 units from player `ricky303` and 96.9 units from player `Tsurai`. The post-position tail grouping shows all 27,716 Warboy position leads with suffix bytes have top groups shaped as post-position `+0 = ff`, ten zero bytes, and a little-endian `u16` at first nonzero offset `+11`. The repeat-stride scan shows 25,393 of those suffixes expose at least two `ff` markers at a consistent stride, with stride `18` dominating the visible top groups; the second-entry aggregate shows 25,245 of those repeat tails expose a local field inside entry 2. The largest first-`0c` repeat groups keep `ba 13 / 5050` as the first record field, then split into second-entry zero-run 5, local field `+6 = 66 00 / 102` across 1,133 records and a broader zero-run 0, local field `+1 = ff 00 / 255` family across 708 records. Next parser work should identify whether the `+0` marker begins a nested selector, a terminator plus fixed fields, or a repeated state-list entry whose next member starts at `+18`.
  - The repeat-entry transition table and matrix now broaden the same lead into entry-indexed evidence. The Warboy scratch report pairs entry 1 and entry 2 for all 25,393 repeat-stride tails and has 50,786 field observations across the first two consecutive entries: entry 1 usually preserves the first-record field at zero-run 10 / local field `+11`, while entry 2 splits into the high-volume `ba 13 / 5050 -> 66 00 / 102` and `ba 13 / 5050 -> ff 00 / 255` buckets plus smaller variants. Next parser work should model the stride-18 body as a repeated state-list entry and test whether the entry-2 `ff 00` bucket is a nested marker, a flag-like field, or a boundary artifact.
  - The repeat-continuation table now shows all 25,393 Warboy paired repeat-stride tails have bytes after the repeated records at continuation offset `36`; the visible prefixes start with separate-body shapes such as `00 41 00 ff`, `00 00 00 ff`, `00 01 00 ff`, or `00 01 04 ff`, not a third `ff` record. The continuation-field table groups those bodies by local bytes `+0..+3` and `u16` windows at `+4`, `+6`, `+8`, `+10`, `+12`, and `+14`; all 25,393 continuation bodies with at least four bytes have byte `+3 = ff`. Top groups expose useful windows such as `00 41 00 ff` with `u16 +8 = b0 74 / 29872` and `u16 +14 = 08 00 / 8`, `00 00 00 ff` families that often stay zero until `u16 +14 = 08 00 / 8`, `00 01 00 ff` variants with nonzero `+4`, `+6`, and `+8` fields, and `00 01 04 ff` with `f7 0f / 4087`, `00 04 / 1024`, and `d0 ee / 61136`. The marker-tail table treats local `+3 = ff` as a candidate marker and shows all 25,393 Warboy continuation bodies expose a post-marker u16 after full-continuation parsing. The post-marker split is now concrete: immediate `+4` fields, four-zero `+8` fields, and ten-zero `+14` fields, including `00 41 00 -> +8 b0 74`, `00 00 00 -> +14 08 00` or `+8 da 22`, `00 01 00 -> +4 fd 0f` / `76 0c` / `37 03` or `+8 b0 74`, and `00 01 04 -> +4 f7 0f`. The prefix-family table collapses the detailed rows into 532 global families; the largest are `00 01 00 -> +4 f0 0f` (912 bodies), `00 01 00 -> +4 fd 0f` (714 and 692 bodies with different post-marker leads), `00 01 04 -> +8 f2 22` (666), `00 01 10 -> +8 12 c9` (598), and `00 01 04 -> +4 f7 0f` (548). The repeat-list candidate table confirms every marker continuation has exactly two stride-18 entries before continuation offset `36` and exposes a post-marker u16; top combined shapes include `ba 13 -> ff 00 -> 00 01 00 ff/+4 f0 0f` (912), `ba 13 -> 66 00 -> 00 01 04 ff/+8 f2 22` (666), `01 00 -> ff 33 -> 00 01 00 ff/+4 fd 0f` (644), `7a 14 -> 66 00 -> 00 01 04 ff/+4 f7 0f` (444), and `ba 13 -> 66 00 -> 00 00 00 ff/+8 3f 24` (436). The field-value summary gives the next broad invariant check: across 76,179 modeled role observations, entry 1 is led by `ba 13 / 5050` and `01 00 / 1`, entry 2 by `ff 00 / 255` and `66 00 / 102`, and the post-marker field by `fd 0f / 4093`, `f2 22 / 8946`, `3f 24 / 9279`, and `f0 0f / 4080`. Next parser work should model a two-record stride-18 list followed by a continuation body and then test whether the continuation's stable `ff` at local offset `+3` is a nested marker, a flag-like field, or a boundary artifact.
  - The generated JSON now exports top-level `Protocol03SelectorFfRepeatListCandidates`, so downstream decoder work can consume the two-entry stride/list hypothesis directly. The checked-in `mxopackets1` corpus currently exports eight candidates: all have continuation offset `36`, prefix `00 01 00 ff`, entry 2 field `+6 = 19 31 / 12569`, and full-continuation post-marker field `+16 = 22 00 / 34`. It also exports `Protocol03SelectorFfRepeatListFieldSummaries`: seven checked-in field-summary groups, six with item/game-object/animation resource references. The Warboy scratch JSON has 2,621 field-summary groups split into 1,857 entry-1 groups, 245 primary entry-2 groups, 298 effective entry-2 groups, and 221 post-marker groups; 2,422 of those groups now have resource references split into 1,780 entry-1 groups, 206 primary entry-2 groups, 249 effective entry-2 groups, and 187 post-marker groups.
    It now also exports `Protocol03SelectorFfRepeatListShapeSummaries` for the full entry-1 -> entry-2 -> continuation -> post-marker shape. The checked-in corpus has four shape summaries, while Warboy has 3,567, all with at least one resource reference. Top Warboy shapes are `ba 13 / 5050 -> ff 00 / 255 -> 00 01 00 ff -> f0 0f / 4080` (912), `ba 13 / 5050 -> 66 00 / 102 -> 00 01 04 ff -> f2 22 / 8946` (666), `01 00 / 1 -> ff 33 / 13311 -> 00 01 00 ff -> fd 0f / 4093` (644), `7a 14 / 5242 -> 66 00 / 102 -> 00 01 04 ff -> f7 0f / 4087` (444), and `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff -> 3f 24 / 9279` (436). `Protocol03SelectorFfRepeatListEffectiveShapeSummaries` adds the normalized entry-2 roll-up: four checked-in groups and 3,662 Warboy groups. The largest Warboy effective shapes are `ba 13 / 5050 -> 66 00 / 102 -> 00 01 00 ff -> f0 0f / 4080` (878 secondary-sourced from primary `ff 00`), `ba 13 / 5050 -> 66 00 / 102 -> 00 01 04 ff -> f2 22 / 8946` (666 primary-sourced), `7a 14 / 5242 -> 66 00 / 102 -> 00 01 04 ff -> f7 0f / 4087` (444 primary-sourced), and `ba 13 / 5050 -> 66 00 / 102 -> 00 01 00 ff -> 3f 24 / 9279` (438 mixed primary/secondary). `Protocol03SelectorFfRepeatListEffectiveShapeHeaderSummaries` now joins those normalized shapes to local RPC-header byte windows without per-candidate bloat: checked-in dumps have three known-header groups covering seven candidate instances and zero vendor windows; Warboy has 892 known-header groups covering 2,843 candidate instances, including 81 candidate instances with vendor-header windows. Top Warboy vendor-correlated shapes include `ba 13 / 5050 -> 21 0a / 2593 -> 00 41 00 ff -> b0 74 / 29872` with all 12 candidates carrying `SERVER_VENDOR_OPEN` at selector-payload `+11` / object `+29` inside `e0 2d 81 0d`, and `ba 13 / 5050 -> 66 00 / 102 -> 00 00 00 ff -> 1b ee / 60955` with all nine candidates carrying vendor windows led by `CLIENT_VENDOR_SELL` at `81 11`. `Protocol03SelectorFfRepeatListHeaderNameSummaries` now inverts that by header name: checked-in dumps have two non-vendor header-name rows, while Warboy has 89 rows and three vendor rows. Warboy `SERVER_VENDOR_OPEN` covers 49 candidates, 49 de-duplicated windows, and 15 normalized shapes; `CLIENT_VENDOR_SELL` covers 27 candidates, 27 windows, and 11 shapes; `CLIENT_VENDOR_BUY` covers five candidates, five windows, and five shapes. Treat these as byte-window boundary leads, not nested-RPC proof. The new `FieldLayoutKind` values separate ordinary padded fields from marker-adjacent windows: Warboy entry 2 now splits into 11,997 zero-padded u16 candidates, 10,754 marker-adjacent u16 candidates, 2,494 immediate u16 candidates, and 148 no-field candidates. `Protocol03SelectorFfRepeatListLayoutSummaries` adds a layout-prefix roll-up: one checked-in group and 99 Warboy groups. The largest Warboy groups are long-zero entry 1 / marker-adjacent entry 2 / `00 01 00 ff` / immediate post-marker (3,308 candidates, 235 shapes), long-zero entry 1 / zero-padded entry 2 / `00 01 00 ff` / four-zero post-marker (3,028, 540), long-zero entry 1 / zero-padded entry 2 / `00 01 00 ff` / immediate post-marker (2,716, 288), and long-zero entry 1 / marker-adjacent entry 2 / `00 01 00 ff` / four-zero post-marker (1,899, 335). The secondary-slot export shows 15,047 Warboy entry-2 records carry a nonzero local `+6` field in addition to their primary field; strongest pairs are `ff 00 -> 66 00` (3,179), `ff 22 -> 66 00` (843), `ff 00 -> 5a 0a` (710), and `ff 00 -> 44 00` (686). The effective-field export now uses that secondary slot for marker-adjacent entry-2 layouts when present: Warboy has 10,754 secondary-sourced effective entry-2 fields, and `66 00 / 102` becomes the top normalized entry-2 value with 9,743 observations. The checked-in corpus has zero secondary-sourced effective entry-2 fields and remains `19 31 / 12569`. The new `entry 2 effective` field-summary role makes that normalized value available for downstream resource joins without replacing the original primary `entry 2` marker evidence. Top Warboy entry-1 references look clothing-item/game-object heavy with animation collisions (`ba 13 / 5050` -> `Fclothing_Shirt_A13_C7` / "Orange/Yellow Sif Leather Fighting Dress" / `NPCDJAtTurnTable_Idle4`; `7a 14 / 5242` -> `Fclothing_Shirt_A19_C26` / "Deep Magenta Knight Leather V-Neck Top" / `AD_ADLb_Aggro`). Primary and effective entry-2 leaders still match mostly animation symbols in imported resources, while post-marker leaders split between animation refs (`fd 0f / 4093`, `f0 0f / 4080`) and clothing item/game-object refs (`f2 22 / 8946`, `3f 24 / 9279`). Keep future repeat-list parser tests tied to this machine-readable export and treat resource refs as candidate names, not final semantics.
  - The same Warboy scratch run gives stronger static-interaction leads: object ids `277874480` (`ElevatorPanel`, 14 interactions), `277874441` (`Door Hell Club Elevator`, 11 interactions), and `959447528` (`Stance Site Invis Bench`, two interactions) all correlate with tracked vendor inventory rows. These extend the capture target set to object type `0` environment and object type `3` door-like static interactions.
  - Current object-id/vendor-list/static-object correlations suggest some vendor-related static interactions may appear with object type `0` or `3`, so future capture requests should include static interaction payloads with object type `0`, `2`, or `3`.
  - The older original import still has one embedded `CLIENT_VENDOR_SELL` candidate inside an `MRGN->Client` packet body; keep it separate from the two new checked-in top-level sell anchors.
  - `Protocol03SelectorFfRepeatListHeaderSamples` now keeps bounded byte samples for the header-window correlations. The checked-in corpus exports 28 non-vendor samples; the Warboy scratch JSON is capped at 120 samples and includes all 81 vendor windows: 49 `SERVER_VENDOR_OPEN`, 27 `CLIENT_VENDOR_SELL`, and five `CLIENT_VENDOR_BUY`, each with source file/line, selector payload/object offsets, `HeaderRegion`, parser interpretation, local target offset, header context, repeat-list context, normalized shape fields, and decoded position anchors. Warboy vendor samples split into 28 `field +9 u32`, 27 `continuation body`, 22 `pre-position body`, and four `repeat entry 1` hits; sample rows now label exact parser targets such as `continuation +54 / post-marker +50` and `pre-position +212`.
  - `Protocol03SelectorFfRepeatListHeaderRegionSummaries` now groups those windows by local header, encoded bytes, and `HeaderRegion`. Checked-in dumps have two non-vendor region rows; Warboy has 207 rows including 10 vendor rows: four for `SERVER_VENDOR_OPEN`, three for `CLIENT_VENDOR_SELL`, and three for `CLIENT_VENDOR_BUY`. The grouped counts preserve the same 81 vendor windows while making region-level parser targets directly queryable. Region rows now also carry entry-1, effective entry-2, and post-marker resource-reference labels; all 10 Warboy vendor rows have entry-1 and entry-2 refs, and 9 have post-marker refs. The largest labeled region is `SERVER_VENDOR_OPEN` / `field +9 u32` with 28 windows, entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5`.
  - `Protocol03SelectorFfRepeatListVendorBoundaryTargetSummaries` now groups vendor-only selector-`ff` header windows by exact header, region, selector-payload offset, object-relative offset, candidate-local offset, and position anchor. Checked-in dumps have zero vendor target rows; Warboy has 50 target rows covering all 81 vendor windows and 60 normalized shapes: 21 rows for `SERVER_VENDOR_OPEN` (49 candidates), 24 for `CLIENT_VENDOR_SELL` (27), and five for `CLIENT_VENDOR_BUY` (5). `Protocol03SelectorFfRepeatListVendorBoundaryInterpretationSummaries` adds a priority roll-up over those exact rows: continuation deep-body unresolved covers 21 target rows / 22 windows, continuation mid-body unresolved covers three / four, continuation short-tail body covers one / one, pre-position mid-body unresolved covers nine / 10, pre-position deep-body unresolved covers three / three, near-position overlaps cover nine / nine, packed `field +9 u32` overlaps cover two / 28, and repeat-entry-1 primary-field overlaps cover three / four. The continuation roll-up is led by deep-body offsets `continuation +107 / post-marker +103` and `+81 / +77`, plus mid-body offsets `+39 / +35`, `+116 / +112`, and `+54 / +50`; pre-position targets are led by mid-body offsets `pre-position +137`, `+152`, `+162`, `+170`, and `+180`, plus deep-body offsets `+176`, `+150`, and `+212`. The target and interpretation rows now export `TargetBodySpanBytes` plus clamped `TargetBodyHexes`, so parser tests can sort by exact `continuation tail N` or `pre-position gap N` values and then compare the local body bytes without deriving them from raw payload offsets. The deep/mid continuation and pre-position rows are the next parser/body-length targets; the short-tail, near-position, and modeled-overlap rows are likely byte-window collisions or short-span checks until a capture contradicts the current field model. The top exact target is `SERVER_VENDOR_OPEN` at selector payload `+11`, split into object `+29` with 22 windows and object `+30` with six windows; both sit inside the packed `e0 2d 81 0d` field. The target rows preserve resource labels, so the dominant `SERVER_VENDOR_OPEN` target carries entry-1 clothing/game-object refs such as `ba 13 / 5050` and `a0 13 / 5024`, effective entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5`.
  - `Protocol03SelectorFfRepeatListVendorBoundaryPrioritySummaries` now groups the exact vendor targets by parser priority before the detailed interpretation split. Checked-in dumps have zero priority rows; the refreshed Warboy scratch report has five priority rows: deep-body boundary candidates with 24 targets / 25 windows, mid-body boundary candidates with 12 targets / 14 windows, one short-span boundary candidate, modeled field overlaps with five targets / 32 windows, and near-position overlaps with nine targets / nine windows. Use this as the next work queue: prove or reject deep/mid continuation and pre-position body lengths first, keep the short continuation tail as a bounded check, and treat packed `field +9`, repeat-entry, and near-position rows as collision controls unless new captures contradict the current model.
  - `Protocol03SelectorFfRepeatListVendorBodyWindowProbeSummaries` now turns the continuation/pre-position part of that work queue into byte-level parser fixtures. Checked-in dumps have zero probe rows; Warboy has 45 rows covering the 49 continuation/pre-position vendor windows after excluding modeled packed-field and repeat-entry overlaps: 25 continuation-body rows / 27 windows and 20 pre-position rows / 22 windows. The priority split is 24 deep-body rows / 25 windows, 12 mid-body rows / 14 windows, one short-span row / one window, and eight near-position rows / nine windows. Each row keeps body-local offset, offset modulus, bytes before and after the matched header, body byte count, clamped body bytes, payload/object offsets, four-byte little-endian window, header `u16`, half-window `u16` resource references, nearby `u16` windows, simple length-match checks, repeat-list fields, and sample source. Start body-length tests with the repeated probes at continuation `+39` (`SERVER_VENDOR_OPEN`, tail 51), continuation `+81` (`SERVER_VENDOR_OPEN`, tail 81), and pre-position `+137` (`CLIENT_VENDOR_SELL`, gap 53), then use the near-position rows as collision controls. The first nearby-length pass found zero simple matches across all 45 Warboy probe rows, so prioritize boundary/collision modeling over searching for an adjacent `u16` length field at the suspect header-like bytes. The half-window reference pass found at least one resource collision in all 45 rows, but the high-half values mostly reflect the header-looking `81 xx` bytes colliding with animation ids. The useful low-half signal appears in 28 rows / 31 windows, with item refs in 23 rows / 25 windows. `Protocol03SelectorFfRepeatListVendorBodyWindowEvidenceSummaries` now buckets the full Warboy probe queue into 23 low-half item/gameobject collision-control rows / 25 windows, five lower-confidence low-half resource-collision rows / six windows, 17 high-half-only header-byte collision rows / 18 windows, and zero nearby length-match rows. `Protocol03SelectorFfRepeatListVendorBodyWindowParserActionSummaries` turns that into three Warboy parser actions and no active body-length-test rows: suppress 23 rows / 25 windows as packed item/gameobject field collisions, suppress five rows / six windows unless future capture context corroborates them, and suppress 17 rows / 18 windows as high-half header-byte collisions. `Protocol03SelectorFfRepeatListVendorBodyWindowResourceCollisionSummaries` exports the label details as 19 Warboy collision rows, including 16 low-half rows; repeated low-half examples include `e0 2d / 11744` resolving to `Armor_F_Shirt_A14_C2_1` across 15 rows and `00 28 / 10240` resolving to `Armor_F_Shirt_A4_C24_6` across four rows. Treat those as packed clothing/state-field collision controls while proving or rejecting the mid/deep body-length rows.
  - `Protocol03SelectorFfRepeatListVendorContinuationBodyOffsetSummaries` now narrows continuation-body windows into local continuation-offset parser targets. Checked-in dumps have zero rows; the Warboy scratch JSON has 23 rows covering all 27 continuation-body vendor windows, all anchored on candidate position `+51`. The repeated offsets are continuation `+39` / post-marker `+35` with two `SERVER_VENDOR_OPEN` windows, `+54` / `+50` with `CLIENT_VENDOR_BUY` plus `SERVER_VENDOR_OPEN`, `+81` / `+77` with two `SERVER_VENDOR_OPEN` windows, and `+107` / `+103` with `CLIENT_VENDOR_SELL` plus `SERVER_VENDOR_OPEN`. The rows now keep continuation-body byte counts, bytes remaining after the suspect two-byte window, span classes, and clamped local continuation body slices around the window. Warboy has 22 deep body tails, four mid-body tails, and one short tail after the matched header bytes. The boundary interpretation roll-up mirrors that as 21 deep-body targets / 22 windows, three mid-body targets / four windows, and one short-span target / one window, so next parser work can test body lengths beyond the first post-marker field by priority tier.
  - `Protocol03SelectorFfRepeatListVendorPrePositionBodyOffsetSummaries` now narrows the pre-position row into local body offsets before the decoded xyz anchor. Checked-in dumps have zero rows; the Warboy scratch JSON has 18 rows covering all 22 pre-position vendor windows. The repeated offsets are pre-position `+34` with two `CLIENT_VENDOR_SELL` windows four bytes before position, `+137` with two `CLIENT_VENDOR_SELL` windows 55 bytes before position, `+176` with two `CLIENT_VENDOR_SELL` windows split between four and 184 bytes before position, and `+205` with one `CLIENT_VENDOR_SELL` plus one `SERVER_VENDOR_OPEN` window split between four and 40 bytes before position. The rows now also keep clamped local pre-position body slices before the xyz anchor. The position-gap classes split the windows into nine near-position overlaps, 10 mid-body gaps, and three deep pre-position gaps; the boundary interpretation roll-up now mirrors that as nine mid-body pre-position targets / 10 windows, three deep-body targets / three windows, and nine near-position overlap targets / nine windows, so parser work can prioritize earlier body bytes with 38, 53, 80, or larger trailing spans.
  - `Protocol03SelectorFfRepeatListContinuationMarkerSummaries` now groups the two-entry continuation body by prefix-before-marker, marker byte, post-marker field, and effective entry-2 source, then cross-links each marker group to known-header and vendor-header windows. Checked-in dumps have one row covering eight candidates and four effective shapes; seven candidates have known non-vendor headers and zero vendor windows. Warboy has 405 rows covering all 25,393 modeled candidates and all 3,662 effective shapes; all rows keep marker byte `ff`. The largest prefix family is `00 01 00` with 15,228 candidates, followed by `00 01 10` with 3,842, `00 00 00` with 2,909, and `00 01 04` with 1,441. Warboy has 232 marker rows with known headers and 22 marker rows with vendor windows, covering all 81 vendor windows. The marker export now also carries resource-reference labels for entry 1, effective entry 2, and post-marker fields: 384 Warboy rows have entry-1 refs, 386 have effective entry-2 refs, and 339 have post-marker refs. It separately exports vendor-only resource labels so mixed marker families do not blur the signal: 21 vendor-bearing rows have vendor-only entry-1 refs, 21 have vendor-only effective entry-2 refs, and 16 have vendor-only post-marker refs. Top vendor-bearing marker rows now keep both the header evidence and the vendor-only resource labels, such as `00 41 00 ff -> b0 74` with 19 `SERVER_VENDOR_OPEN` windows, effective entry-2 ref `21 0a / 2593`, and post-marker refs to `Shield_F_Coat_A4_C5_5`.
  - Earlier embedded `SERVER_VENDOR_OPEN` candidates in the original imported `mxopackets1` subset remain payload-field evidence: two are confirmed as `80 bc` payload fields and one is inside an MRGN packet body. The selected hdneo intakes are the current checked-in top-level vendor-open evidence, with 15 parsed `81 0d` payloads beginning `7c ad d9 43` or closely related state-window prefixes and observed payload lengths 60, 72, 100, 200, 208, 480, and 760 bytes.
  - `docs/PACKET-RESOURCE-LEADS.md` now prioritizes public preservation-community routes, including the acquired Warboy/Rajko `mxopackets2` archive triage, RaGEZONE/Hardline Dreams combat logs, HD_Morpheus world-packet replay logs, MxOEmu source/log holders, Matrix Discord maintainers, MxOEmu testers, public data host references, client-protocol context, and source-code references. `research/community-data/discord-emulation-packet-leads.md` gives targeted Discord collector/vendor/code-storage/combat/sniffer/replay follow-ups, while `research/community-data/discord-mara-central-packet-leads.md` adds curated public-corpus/NPC/combat/server-resource/tutorial-parser probes plus broader packet-log, packet-capture, mission-log, torrent, `MxOSource`, and archive-holder follow-ups. `docs/PACKET-CAPTURE-REQUEST.md` gives preservation contacts exact target actions, required context, runtime log flags, and expected files. `docs/PACKET-CAPTURE-OUTREACH.md` adds ready-to-send forum/Discord/direct-message text plus archive search strings and an intake checklist. `scripts/collect-packet-capture.sh` bundles local packet logs with structured capture context, SHA-256 file hashes, skipped-file evidence, Markdown-safe manifests, and initial triage into `data/experiment_artifacts/packet-captures/`; `scripts/triage-packet-capture.sh` scans received folders or common archives for vendor, Object599 / `NPC_BASE`, movement, ability, and effect signatures in both spaced and compact hex forms before import; `scripts/import-packet-capture.sh` previews or stages reviewed text logs from folders or common archives under `research/packet-dumps/community-intake/` with provenance, SHA-256 hashes, duplicate hash leads, skipped-file evidence, unsafe archive path rejection, `.log`-to-`.txt` normalization, Markdown-safe reports, and triage output; and `scripts/test-packet-capture-tools.sh` verifies the capture tooling path plus Discord lead-report generation with synthetic logs, archives, dry-run previews, and table-sensitive filenames/context values.
- [x] Import a focused public combat/ability packet slice from standalone `hdneo/mxopackets1`.
  - The checked-in intake `research/packet-dumps/community-intake/20260607T003822Z-hdneo-mxopackets1-combat-ability-selected` adds 12 reviewed combat, ability load/unload, hyperspeed, and ability-use logs with provenance, hashes, and triage. It contributes 2,429 `80 bc` object/state bundle hits, 84 Object599 / `NPC_BASE` creation-prefix hits, 24 static-object interaction byte windows, and expanded equipment/effect observations.
- [x] Import a focused public marketplace/archive-vendor packet slice from standalone `hdneo/mxopackets1`.
  - The checked-in intake `research/packet-dumps/community-intake/20260607T013506Z-hdneo-mxopackets1-marketplace-vendor-selected` adds 23 reviewed marketplace category, empty marketplace search, archive-vendor teleport, and vendor-adjacent movement/state text logs with provenance, hashes, and triage. It records two skipped non-text Trinity vendor-session logs, contributes 760 `80 bc` bundle hits in triage, 27 Object599 / `NPC_BASE` creation-prefix hits, 35 repeated mode `06` suffix leads, 12 ability-load hits, 24 ability-unload hits, and new server state-bundle shape labels such as `EventSlot*_Effect`, `FixedBinkIDOverride`, `PvPMaxSafeLevel`, `PvPServer`, and `WR_RezEvents`.
- [ ] Implement recycle-info pricing if a decoded packet path needs it.
  - Public `rshooks.py` defines recycle info as `VendorPrice / 20`, but no current decoded gameplay handler uses that path.
- [x] Group repeated `80 bc` server payload structures and validate whether embedded `81 0d` values are headers or payload fields.
- [x] Mine Rajko and local hardcoded queued command examples and compare `80 bc` startup/world-state payloads against the imported corpus.
  - Current generated report finds 59 hardcoded command examples, including 37 `80 bc` examples. The strongest corpus field-group overlap is now `45 03` with 112 imported dump hits and dominant `3d 04` / `00 00` / `01 00` layout evidence; one local `15 00 / 45 00 / 00 f7` payload remains an exact corpus-hit lead.
- [x] Group top-level `80 b2` server payloads and compare them against hardcoded player attribute / ability load constants.
  - Current generated report finds 1,108 top-level `80 b2` payloads and 13 hardcoded `80 b2` examples. The combat/ability, two-person, mission/code/Rajko, combat/PvP/Rajko-state, remaining-small ability/loot/market, and Vector state/static intakes expand the state-bundle contexts around the same short player-attribute / ability-load shape family.
- [x] Group top-level `80 b3` ability-unload payloads and same-packet `80 bc` links.
  - Current generated report finds 1,497 top-level `80 b3` / `SERVER_ABILITY_UNLOAD` payloads. All are 2 payload bytes. Dominant fields continue to have same-packet `80 bc` field-1 matches, tying `80 b3` into the same short/long state identifier family as `80 b2`.
- [x] Group top-level `80 d7` friend-list status payloads and decode handle text.
  - Current generated report finds 1,259 top-level `80 d7` / `SERVER_FRIENDLIST_STATUS` payloads. They share leading fields `08 00`, status `3c 00` or rare `3b 00`, and `00 8e`, followed by a two-byte little-endian text length and ASCII `SOE+MXO+Vector-Hostile+...` handle text.
- [x] Group protocol 04 server-state header sequences to identify repeated state bundles.
  - Current generated report finds 1,960 protocol 04 packets containing `80 b2` and/or `80 bc`; 320 of those packets contain both families in the same top-level packet. Large `80 bc` bundles remain the strongest state-bundle sequences, now with additional `47`, two-person, mission/code, Rajko, combat-state, and ability-loadout contexts.
- [x] Group top-level `80 bd` coder-attribute payloads and sequence context.
  - Current generated report finds 678 top-level `80 bd` / `SERVER_CODER_ATTRIBUTE_UNKNOWN` payloads. All are 9 payload bytes. The dominant shape remains `05 11 00 00 00 00 00 00 00`, mostly in teleport and repeated `80 bc` state bundles, while selected public intakes add smaller `01 xx` attribute-like rows that remain state-bundle leads.
- [x] Decode unknown top-level `81 67` protocol 04 server payload shape.
  - Current generated report finds 343 samples, mostly 63-byte payloads with stable prefix fields `17 00`, `27 00`, and `1c 22`; Rajko, Trinity, entilsar, Corrupted, two-person, mission/code, movement/spawn, combat-state, and ability-loadout variants add shorter text/tail combinations.
- [x] Decode protocol 04 teleport startup `SERVER_LOAD_WORLD`, `SERVER_PLAYER_EXP`, and `SERVER_PLAYER_INFO` payloads.
  - Current generated report finds 125 `06` / `SERVER_LOAD_WORLD` payloads with marker `0e 00`, district ids, simulation-time bytes, flag `01`, an environment-string offset, world path, and environment options. It also finds 445 specialized player-value payloads across `SERVER_PLAYER_EXP`, `SERVER_PLAYER_INFO`, and related adjacent rows.
- [x] Decode protocol 04 flash-traffic and feature/event setting payloads.
  - Current generated report finds 92 `81 a9` / `SERVER_FLASH_TRAFFIC` payloads with fixed leading fields and a sized URL slot, plus 565 `3a` / `SERVER_FEATURE_EVENT` payloads with prefix `05 00`, declared byte counts, sized setting keys, and typed string or numeric values for `PvPServer`, `PvPMaxSafeLevel`, `FixedBinkIDOverride`, `EventSlot*_Effect`, and `WR_RezEvents`.
- [x] Decode protocol 04 social/faction payloads.
  - Current generated report finds 53 `7c` / `SERVER_FACTION_PLAYER_INFO` payloads split between mode 1 faction summaries and mode 2 fixed-width crew records. It decodes faction/crew names and leader handles, 58 `80 86` / `SERVER_CREW_MEMBERS_LIST` payloads, and 67 `80 de` / `SERVER_FRIEND_ONLINE` payloads with sized `SOE+MXO+Vector-Hostile+...` handles.
- [x] Isolate unknown protocol 04 `47` as a dedicated structural group.
  - Current generated report finds 353 fixed 46-byte `47` payloads, all with leading words `00 00`, `04 01`, `00 00`, mid words `1a 00` and `06 00`, and a repeated 21-byte tail. They commonly appear after `80 bc` state bundles and sometimes before `2e`, `81 a9`, and social/faction responses.
- [x] Decode named protocol 04 chat/faction response payloads.
  - Current generated report finds 322 `2e` / `SERVER_CHAT_MESSAGE_RESPONSE` system-style response samples, 16 `81 54` / `SERVER_CHAT_WHEREAMI_RESPONSE` coordinate samples, and 20 `80 f5` / `SERVER_FACTION_NAME_RESPONSE` samples.
- [x] Isolate unknown protocol 04 `6d` and `80 c1` as dedicated structural groups.
  - Current generated report finds 110 `6d` list-bearing payloads split across mode `01 00`, `02 00`, and `03 00` records. It also finds 18 fixed four-byte `80 c1` payloads in state-bundle contexts.
- [x] Group additional protocol 04 server payload shapes that do not yet have specialized decoders.
  - Current generated report has 1,087 additional non-specialized protocol 04 server payload shape samples after the specialized structural groups are applied.
- [x] Link `80 b2` field 0 to `80 bc` field 1 inside the same protocol 04 state bundles.
  - Current generated report finds 20 shared-field rows. The strongest are `11 00` in 82 packets, `35 04` in 41 packets, `28 04` in 33 packets, `26 04` / `27 04` in 29 packets each, and `3f 04` in 24 packets.
- [x] Validate linked short/long field-value consistency between `80 b2` and `80 bc`.
  - Current generated report finds 810 same-packet field links; 804 repeat the linked field at `80 bc` payload byte offset 9, and 802 also repeat the `80 b2` field 1 value at `80 bc` payload byte offset 11.
- [x] Cross-reference linked `80 b2`/`80 bc` state fields with tracked gameobject symbols.
  - Current generated report loads 44,406 `data/gameobjects.csv` entries and maps repeated shared fields to clothing GoIDs: `3d 04` / 1085 to `Mclothing_Pants_A8_C1`, `35 04` / 1077 to `Mclothing_Pants_A6_C8`, and `3e 04` through `40 04` to adjacent `Mclothing_Pants_A8_C2` through `Mclothing_Pants_A8_C4`.
- [x] Group `80 bc` internal field layout by leading field.
  - Current generated report separates 20-byte `80 bc` payloads into fixed-offset schema families. The largest leading-field families are `56 00` (12,175), `55 00` (8,518), `15 00` (6,953), `45 03` (1,878), and `45 00` (1,711). The strongest fixed schema is `56 00` / `b3 00 00` / byte-11 `21 00` / byte-15 `01 00` with 977 records, while `45 03` remains the main byte-9 repeat family.
- [x] Tie hardcoded `80 bc` constants back to corpus layout families.
  - Current generated report shows `45 03` has three hardcoded examples, three corpus-backed field groups, and 402 total field-group hits; `45 00` has 17 hardcoded examples, 17 corpus-backed field groups, and 257 total field-group hits; `15 00` has seven hardcoded examples, six corpus-backed field groups, and 98 total field-group hits.
- [x] Add layout-strength evidence for `80 bc` interaction attribute candidates.
  - Current generated report combines interaction-looking field 1 candidates with the byte-offset layout. Field 1 `11 00` / 17 as `SecureTradeFlag` is the strongest current lead: 373 payloads match that candidate and 330 repeat field 1 at payload byte offset 9, mostly in the `45 03` family. Candidate `TalkActiveTracker` and `BuyActiveTracker` rows remain capture targets but do not currently show the byte-9 repeat pattern.
- [ ] Validate `80 bc` field 1 interaction tracker candidates against paired NPC, vendor, inventory, trade, and loot captures.
  - Current candidate labels include `BuyActiveTracker`, `TalkActiveTracker`, `GiverActiveTracker`, `TakerActiveTracker`, `PutActiveTracker`, `SecureTradeFlag`, and `CurrentState`.
- [ ] Semantically decode `80 bc` field families and map them to object attributes, bonus state, or gameplay actions.
  - Current generated report separates `80 bc` 20-byte payloads into fixed-offset schema shapes. Highest-count targets are `56 00` / `b3 00 00` / byte-11 `21 00` / byte-15 `01 00` with 977 records, `55 00` / `01 00 00` / `05 00` / `01 00` with 921, `56 00` / `b3 00 00` / `21 00` / `00 00` with 839, `56 00` / `b3 00 00` / `02 00` / `01 00` with 677, and `55 00` / `01 00 00` / `05 00` / `00 00` with 596. Their capture scopes are mostly teleport plus selected combat, two-person, combat-state, and ability-loadout state traffic, and their top packet sequences are large `80 bc` state bundles such as `80 bc x30` or runs interleaved with `80 bd`, `80 b2`, `80 b3`, `47`, and social/faction responses, so treat them as world-state/state-bundle targets rather than strong vendor leads. The largest leading-field families are now `56 00` with 12,175 payloads, `55 00` with 8,518, `15 00` with 6,953, and `45 03` with 1,878; `45 03` remains the strongest state-link target because its main schema families repeat field 1 at byte offset 9.
- [x] Extend packet tooling to classify protocol 03 object/state packet families, not only protocol headers.
  - Current generated report parses 256,086 protocol 03 object messages, fully segments 156,499 messages, and identifies 350,749 known selector segments across movement, object state, spawn/profile, emote/effect, compact terminal selector `28` markers, selector `80` self-view attributes, selector `09` two-byte-prefixed movement positions, primary/secondary/adjacent movement wrappers, live mode `06` zero-update, guarded object-view, guarded marker-24/marker-25 57-byte object-view, guarded marker-16 and marker-16/marker-17 57-byte object-view, guarded marker-15, marker-19, marker-19-alt, and marker-25 57-byte object-view, guarded marker-24/marker-26 57-byte object-view, guarded marker-29/marker-2a 57-byte object-view, alternate guarded marker-24 57-byte object-view, guarded marker-1e/marker-1f 57-byte object-view, guarded marker-1d and marker-1d/marker-1e 57-byte object-view, guarded marker-34 94-byte object-view, residual guarded, non-dominant, terminal 96-byte, terminal long embedded marker-34/marker-2a/marker-04/marker-24, guarded long embedded marker-25 tuple bodies, selector `9b` state blocks, delete-view, and static object/door families.
- [x] Ingest compact and label-prefixed compact hex packet lines from community dumps.
  - Current generated report includes compact packet lines from movement and door captures plus colon-labeled compact request/answer lines, increasing packet-like lines by 43 and surfacing full compact door-answer payloads that were previously skipped by the spaced-byte parser.
- [x] Stitch conservative unmarked multi-line packet blocks from manual community notes.
  - Current parser starts a loose packet only at top-level protocol wrappers, appends following hex and short continuation lines, and flushes on the next top-level packet, packet-size header, non-hex text, or EOF. This preserves packet-like line totals while recovering full manual door-answer bodies from `doors_log.txt`.
- [x] Extend protocol 03 tooling from first object-view signature classification into conservative object-message segmentation and known per-selector payload sizing.
- [x] Treat protocol 03 selector `00` as a one-byte state marker.
  - This remains part of the complete segmentation path for common object-update tails and is included in the current 156,499 fully segmented object messages.
- [x] Decode secondary movement wrappers in two-update protocol 03 object packets.
  - Current generated report counts 22,960 terminated secondary movement wrapper segments, including rows exposed after adjacent object-view parsing.
- [x] Decode primary movement wrappers in two-update protocol 03 object packets.
  - Current generated report counts 2,076 bounded primary movement wrappers whose first selector is `12`, `13`, or `14` and whose payload starts `00 <id> 00 <mode> <movement selector>`. Selector `12` uses 17-byte payloads, selector `13` uses 18-byte payloads, and selector `14` uses 19-byte payloads.
- [x] Decode adjacent movement wrappers at protocol 03 object-view boundaries.
  - Current generated report counts 32,317 adjacent movement wrapper segments. These second segments start `00 02 <movement selector>`, carry plausible position data, omit the terminated-wrapper `00 00`, and are immediately followed by another plausible object-view header.
- [x] Decode protocol 03 selector `09` movement-state position prefixes.
  - Current generated report counts 25 update-count-3, first-selector-`09` movement bundles whose first segment is a 14-byte two-byte prefix plus xyz position.
- [x] Group protocol 03 movement-state post-position tail lead families.
  - Current generated report keeps update-count-3 movement-state tails after first selectors `08` and `09` unresolved when variable tail bytes collide with generic selectors. It groups 4,194 tails into lead families, led by first-selector `08` with `ff 01 aa 01` (1,107 records), first-selector `08` with `ff 01 4e 01` (1,013), first-selector `08` with `ff 01 64 01` (632), first-selector `08` with `ff 01 02 00` (382), first-selector `09` with `ff 01 02 00` (303), first-selector `08` with `80 80` (273), first-selector `09` with `ff 01 aa 01` (206), and first-selector `08` with `ff 01 01 01` (91). The tail selector byte plus the next three payload bytes decode as a little-endian tag; adjacent captures commonly step by 124-126 or 250-251, making it a timestamp/sequence lead. The generated report now separates tag bytes, marker, and post-marker prefix so `ff 01` zero prefixes and variable `80 80` continuations can be tracked independently before claiming body lengths.
- [x] Decode one-update protocol 03 selector `9b` state blocks.
  - Current generated report counts 22 one-update selector `9b` records with fixed 10-byte payload `01 08 80 fd 00 00 00 00 00 00`. Longer selector `9b` contexts remain unresolved.
- [x] Extract embedded movement-window leads from protocol 03 selector payloads.
  - Current generated report finds embedded movement windows across outer selectors after adjacent wrappers and movement-state tails are bounded. The extractor recognizes `00 <mode> <movement selector>` windows instead of only the `00 02` form. The parser-action rollup now separates 5,477 mode `06` object-state boundary/evidence windows and 2,076 bounded primary wrappers, while keeping 6,905 unresolved suffix-comparison leads in the evidence exports. The dominant mode `06` suffix lead remains `ff 00 00 00 10 00 00 00`, with repeated continuation `00 00 01 ff 00 00 00 00`; after preserving lookahead through consumed bodies, the parser now consumes repeated guarded/terminal mode `06` tuple-body families, including seven terminal long embedded marker-34 trailer bodies, six terminal long embedded marker-2a trailer bodies, three terminal long embedded marker-04 trailer bodies, eight terminal long embedded marker-24 trailer bodies, and four guarded 57-byte long embedded marker-25 trailer bodies.
  - The post-prefix tuple body-shape export groups bytes after the eight-byte tuple and before the next parser-accepted object-view header, and `Protocol03NestedMovementMode06TupleBodyParserActionSummaries` now includes terminal tuple-body rows. The rollup records 151 exact guarded 57-byte marker-24/marker-25 object-view tuple bodies, 58 exact guarded 57-byte marker-16 object-view tuple bodies, 115 exact guarded 57-byte marker-16/marker-17 object-view tuple bodies, 12 exact guarded 57-byte marker-15 object-view tuple bodies, 9 exact guarded 57-byte marker-19 object-view tuple bodies, 8 exact alternate guarded 57-byte marker-19 object-view tuple bodies, 41 exact guarded 57-byte marker-24/marker-26 object-view tuple bodies, 28 exact guarded 57-byte marker-29/marker-2a object-view tuple bodies, 17 exact alternate guarded 57-byte marker-24 object-view tuple bodies, 15 exact guarded 57-byte marker-1e/marker-1f object-view tuple bodies, 13 exact guarded 57-byte marker-1d object-view tuple bodies, 12 exact guarded 57-byte marker-1d/marker-1e object-view tuple bodies, 11 exact guarded 57-byte marker-25 object-view tuple bodies, 31 remaining generic guarded 57-byte object-view tuple bodies, 92 guarded unclassified tuple-body windows, 99 terminal 96-byte tuple bodies, 200 generic guarded 94-byte object-view tuple bodies, 15 exact guarded 94-byte marker-34 object-view tuple bodies, seven terminal marker-34 long embedded trailer bodies, six terminal marker-2a long embedded trailer bodies, three terminal marker-04 long embedded trailer bodies, eight terminal marker-24 long embedded trailer bodies, and four guarded marker-25 long embedded trailer bodies. The held queue still separates eight marker-34 long object-view evidence-only windows and 184 generic low-count/non-dominant tuple-body windows; terminal tail tables keep 31 grouped terminal rows / 218 windows for comparison and carry pre-tail field offsets/bytes.
  - `Protocol03NestedMovementMode06LongTupleBodyTailAnchorSummaries` now keeps the broader long bounded/terminal queue at 71 grouped rows and 98 windows. The embedded-trailer exports narrow that to 48 pre-anchor windows: 45 decode through the existing movement selector table as repeated or singleton shared-trailer shapes, while three unsupported selector controls (`06`, `28`, `2a`) stay evidence-only.
  - The long-body tail-anchor rows preserve pre-anchor first-field, trailing-lead, and pre-lead samples. The repeated pre-anchor lead groups are now live only when the exact embedded movement trailer also matches: `00 e9 07 00 00 00 00 34` with pre-lead bytes `ff cd cc 4c be 00 00 00` (seven terminal-96 windows), `00 cf 03 00 00 00 00 25` with zero-padded pre-lead (four guarded 57-byte windows), and `00 96 00 00 00 00 00 04` with zero-padded pre-lead (three terminal-96 windows). Singleton `00 af ... 05`, `00 39 ... 1f`, `00 46 ... 34`, and the unsupported selector-`28` control remain evidence-only.
  - `Protocol03NestedMovementMode06LongTupleBodyPreAnchorLeadSummaries` and body/offset exports now keep the exact pre-anchor scaffold as evidence while the live parser consumes only the repeated trailer shapes that satisfy the stricter embedded-movement rule. The pre-anchor byte spans are 167 (12), 166 (four), 165 (one), and 171 (one), and the body-byte split is 263 (seven), 224 (four), 262 (three), 261 (two), 228 (one), and 260 (one). Header-like offsets 70, 74, 78, 79, and 94 remain collision targets, not parser boundaries.
  - `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementSummaries` now proves the offset-95 scaffold is an embedded `00 06 <movement selector>` window. Forty-five of 48 pre-anchor spans decode through the existing movement selector table, and three unsupported selector rows stay evidence-only.
  - `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementTrailerSummaries` now groups the decoded windows into a shared 55-byte trailer with post-movement lead `ff 00 00 00 10 00 00 00`, next lead `00 00 01 ff 00 00 00 00`, 22 zero bytes, pre-tail field offset 38, and exact pre-tail/pre-lead/trailing-lead combinations for live and candidate shapes.
  - `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementTrailerShapeSummaries` now splits those aggregate trailer bytes into exact rows. The repeated decoded shared shapes are `02` + `ff cd cc 4c be` + `00 e9 ... 34` before terminal-96 suffixes (seven windows), `03` + zero pre-lead + `00 4c ... 2a` before terminal-96 suffixes with matching outer post-prefix (six live windows plus one held mismatch row), `03` + zero pre-lead + `00 b6 ... 24` before terminal-96 suffixes (eight live windows plus repeated guarded-state/object-view pre-anchor controls), `03` + zero pre-lead + `00 cf ... 25` before guarded 57-byte suffixes (four), and `03` + zero pre-lead + `00 96 ... 04` before terminal-96 suffixes (three). Those five repeated families are now live parser rules under exact guards; the remaining rows are singleton decoded shared controls and unsupported-selector controls.
- [x] Extract string leads from variable-length protocol 03 object-view packets.
  - Current generated report extracts printable strings from unresolved variable blocks. The strongest family initially appeared as update count `12`, selector `57`; creation-aware parsing now identifies it as Object599 / `NPC_BASE` dynamic creation records with repeated names such as `A.S.P. Major`, `Gold Blood Champion`, and `Sisters of Fate Student`.
- [x] Correlate protocol 03 variable-block strings with tracked mob/NPC rows.
  - Current generated report loads 14,642 world entity rows from `data/mob_parsed_untouched.csv` and `data/NPC_COLLECTION.xml`, then exact-matches protocol 03 string leads to known entity names. Top matches include `A.S.P. Major`, `Gold Blood Champion`, `Sisters of Fate Student`, and `Black Tiger Pupil` with level, zone, RSI samples, and bounded prefix/suffix byte context.
- [x] Parse Object12 / `PlayerCharacter` dynamic creation records.
  - Current generated report directly extracts 1,817 protocol 03 update count `12`, selector `0c` player spawn/profile records as GoID 12 / `PlayerCharacter` creation packets, and 1,765 include a decoded `CharacterName` attribute. The Object12 fixed-width creation table decodes `RealLastName`, `RealFirstName`, `CharacterName`, `Level`, `Health`, `MaxHealth`, `RSIDescription`, `CombatantMode`, title/combat-exclusive fields, cancel/exclusive fields where present, and 24-byte position values.
- [x] Aggregate Object599 / `NPC_BASE` dynamic creation record-prefix candidates.
  - Current generated report directly extracts records matching the observed `0c 57 02 <spawn counter> cd ab <attribute count> 8e <name>` prefix shape. It maps `57 02` to tracked GoID 599 / `NPC_BASE`, records spawn counters, creation attribute counts, first masks, and entity-row counts for repeated names.
  - Spawn counter bytes are reused across different names, as expected for packet-local creation state. The byte after `cd ab` is now treated as creation attribute count rather than a name/entity id candidate.
- [x] Identify Object599 / `NPC_BASE` `CharacterName` slot size.
  - All 3,684 directly extracted Object599 / `NPC_BASE` creation records in the current corpus have a 32-byte fixed `CharacterName` attribute after first creation mask `8e`.
- [x] Identify Object599 / `NPC_BASE` attributes after `CharacterName`.
  - The directly extracted Object599 / `NPC_BASE` creation records in the current corpus share `TitleAbility = 00 10 00 00` and `CombatantMode = 22`, matching the local mob creation defaults.
- [x] Parse Object599 / `NPC_BASE` creation masks into fixed-width attributes.
  - The current generated report walks the creation mask stream with local fixed-width Object599 attribute definitions. All 3,684 directly extracted records have matching declared and parsed attribute counts, covering 12 through 27 attributes and grouping raw values for core fields such as `CharacterName`, `TitleAbility`, `CombatantMode`, `StopFollowActiveTracker`, `Description`, `Position`, `IsEnemy`, `Level`, `Health`, `MaxHealth`, and `CurrentState`. It also records consumed creation-attribute bytes and post-attribute suffix prefixes; all 3,684 records have post-attribute bytes, 1,558 include `00 0c 57 02` inside the first eight suffix bytes, and 2,813 expose a plausible next object-view header inside the first eight suffix bytes with 230 distinct bounded pre-header byte sequences. Of those boundary leads, 2,540 use a three-byte `<value> 00 00` pre-header. Selector-length plus dynamic-creation boundary parsing now accounts for 2,383 follow-up object-view headers without consuming them into the parent record.
  - The Object599 dynamic-tail exports are now machine-readable in JSON as `Protocol03NpcBaseDynamicCreationTailSummaries`, `Protocol03NpcBaseDynamicCreationHeaderLikeLeadSummaries`, `Protocol03NpcBaseDynamicCreationBodyAnchorSummaries`, and `Protocol03NpcBaseDynamicCreationExtendedBoundarySummaries`. The committed corpus now exports seven dynamic-tail summary rows, 220 header-like lead rows, 30 body-anchor rows, and 43 extended-boundary rows after promoting common follow-up headers into bounded Object599 boundary parsing; across 1,535 measured nested dynamic creations, 108 parse the Object599 body but still do not expose another object-view header inside the current eight-byte search window. The extended-boundary rows now carry parser-action labels: 10 post-separator evidence rows, one repeated later-header evidence row, 23 single-sample evidence rows, and nine rejected `cd ab` separator-collision rows. Next parser work should consume these exact summary rows instead of scraping Markdown tables.
- [x] Correlate typed Object599 / `NPC_BASE` creation attributes with tracked mob rows.
  - Across 3,665 creation records with tracked same-name mob rows, decoded `Level` matches 3,663/3,665, `Health` plus `MaxHealth` matches 3,588/3,605, `Description`/RSI-like ids match 3,647/3,665, `NPCRank` matches 3,665/3,665, `DebuffState` matches 3,560/3,560, and `CurrentState` matches 3,639/3,660. Decoded `Position` values exact-match 36/3,665 old mob rows, with nearest same-name row distances putting 3,292/3,665 within 1k units, 3,476/3,665 within 2.5k, and 3,588/3,665 within 5k.
- [x] Resolve Object599 / `NPC_BASE` equipment and effect symbol leads.
  - Current generated report decodes `EquippedItemID` as a little-endian gameobject id and resolves the visible equipment table through imported gameobject symbols plus MxOEmu `&gib` command names from imported item rows. The selected hdneo intakes add the `3d b9 00 00` / 47421 `e50autorifle2003` vendor/loot lead, two-person `15 b9 00 00` / 47381 `e50autorifle1001`, two-person `0d ba 00 00` / 47629 `e50shotgun2002`, the `ef 1a 00 00` / 6895 rifle lead, the Luizoe old-sniffer `b4 02 00 00` / 692 `AutomaticRifle1` row, movement/spawn equipment rows such as `c7 09 00 00` / DualSubmachinegun2, `ec 1a 00 00` / Submachinegun3, `f0 1a 00 00` / AutomaticRifle3, combat/PvP/Rajko-state rows such as `c2 1e 00 00` / 7874 `AutomaticRifle8`, `d7 1e 00 00` / 7895 `DualSubmachinegun6`, `6a ba 00 00` / 47722 `e50submachinegun2dlsl001`, and `d5 1e 00 00` / 7893 `Submachinegun9`, plus remaining-small ability/loot rows such as `63 9a 00 00` / 39523 MesonRifle2B / "Meson Rifle Mk II", `99 95 00 00` / 38297 MesonRifle3 / "Heavy Meson Rifle", and `22 b6 00 00` / 46626 SemiAutoPistolSystem / "System FM-1500 Magnum". Resolved `EffectID` rows now include the two-person `83 00 00 de` event-corrupted-death rows, mission/code booster effect rows, the Luizoe old-sniffer `cd 08 00 28` / Intense Attack row, movement/spawn `b5 07 00 28` / Fast Healing and `66 00 00 04` / active accelerated-spawn FX rows, plus combat/PvP/Rajko-state `30 04 00 28` / Health Drain, `03 08 00 28` / Devastation Field, `62 01 00 28` / Plague Zone 2, `9c 05 00 28` / Deflection, `11 0a 00 28` / Code Nuke 3, and `2e 0a 00 28` / Repair RSI 4 rows.
- [x] Import ability definition names for packet correlation.
  - Current generated report loads 1,404 ability definitions from public `abilities.txt` plus local `data/abilityIDs.csv`. Current decoded PlayerCharacter creation `TitleAbility` and combat-exclusive fields are still raw small values rather than direct public high-bit ability GoIDs, which is useful negative evidence for later ability-state decoding.
- [x] Extract protocol 03 selector `28` effect/emote prefix leads.
  - Current generated report finds 5,302 selector `28` payloads, including 4,105 first-selector records and additional non-first occurrences exposed after bounded selector `80` self-view attribute, adjacent movement-wrapper decoding, and compact-line ingestion. 5,030 expose a 12-byte little-endian float position prefix, and 3,530 contain at least one imported FX ID window.
- [x] Extract protocol 03 selector `2a`/`2e` position-like prefix leads.
  - Current generated report finds 14,838 selector `2a`/`2e` variable tails, of which 5,878 expose known position-like leads with plausible little-endian xyz triples. These remain prefix/body-boundary leads rather than complete variable-tail decodes.
- [x] Decode protocol 03 selector `80` self-view attribute mask/value blocks.
  - Current generated report parses 30,688 selector `80` payload starts with the local Object12 self-view attribute table, yielding 40,338 fixed-width attributes. Another 1,502 selector `80` tails remain layout leads, with 117 containing at least one imported FX ID window. The largest groups now cover `SelectionRangeDebuff`, `MissionTypeFlags`, `IsDuelDeath`, `JumpEndTime`, `DuelID`, `Health`, and related self-view attributes; the previous `4c 0a 00 28` FX-looking window is one bounded `Health = 0a4c` case followed by the next selector `28`.
- [x] Extract protocol 03 static object/door spawn prefix leads.
  - Current generated report finds 34 selector `3c`/`9e`/`9f`/`a0` static object leads across 11 distinct static object ids with a stable `cd ab` separator after compact, label-prefixed compact, loose manual door-answer blocks, movement/spawn/hardline, Vector state/static, Vector supplemental, and session/spawn/movement intakes are included.
- [x] Mine external hardcoded protocol 03 object-message examples from MxOEmu forks.
  - Current generated report finds 34 hardcoded protocol 03 examples from local code, Rajko MxOEmu, and the TasteeWheat door/hardline fork.
- [ ] Resolve variable-length protocol 03 spawn/profile, Object599 / `NPC_BASE`, appearance/attribute, effect/emote, static object/door, and unknown payload layouts.
  - Object12 / `PlayerCharacter` and Object599 / `NPC_BASE` creation parsing now names fixed-width attributes and decodes core identity, health, RSI/effect, state, and position fields. Object599 post-attribute suffix prefixes now show repeated adjacent creation-boundary leads, especially `00 0c 57 02`, and 2,813/3,684 records expose a plausible next object-view header inside the first eight suffix bytes with 230 distinct bounded pre-header byte sequences; 2,540 of those boundary leads use a three-byte `<value> 00 00` pre-header. Selector `80` now decodes 30,688 Object12 self-view attribute mask/value blocks, while selector `2a`/`2e` and `28` parsing extracts stable prefix, compact terminal markers, position, and FX-window leads. Static object records now expose a stable object-id prefix plus bounded post-quaternion marker/vector/tail fields from full door answers, and movement wrapper parsing now bounds selector `08`/`09` movement-state tail leads, selector `12`/`13`/`14` primary wrappers, terminated secondary wrappers, adjacent boundary wrappers, and live mode `06` zero-update, guarded object-view, guarded marker-24/marker-25 57-byte object-view, guarded marker-16 and marker-16/marker-17 57-byte object-view, guarded marker-24/marker-26 57-byte object-view, guarded marker-34 94-byte object-view, residual guarded, non-dominant, terminal 96-byte, terminal long embedded marker-34/marker-2a/marker-04/marker-24, and guarded long embedded marker-25 tuple-body families. The unresolved work is remaining creation/profile tail body lengths, effect/equipment behavior validation, movement-state post-position tail body lengths, selector `2a`, `2e`, and longer `28` variable tails, residual selector `80` tails, static object view post-quaternion field semantics, remaining guarded unclassified mode `06` tuple bodies, remaining marker-34 long-body evidence-only windows, longer terminal mode `06` body controls, low-count/non-dominant mode `06` tuple bodies, singleton/unsupported long-embedded controls, full unknown selector body lengths, and before/after gameplay captures.
  - The next nested-movement parser work should start from the 217 remaining guarded unclassified mode `06` tuple-body windows, the remaining longer terminal body controls, the eight marker-34 long-body evidence-only windows, the 184 held generic mode `06` tuple-body windows, the marker-2a outer-post-prefix mismatch row, repeated marker-24 guarded-state/object-view pre-anchor rows, singleton long-embedded controls (`00 f0`, `00 72`, `00 91`, `00 dc`, `00 39`, `00 af`, `00 f5`), and the unsupported selector-`06`/`28`/`2a` controls. The zero-update, guarded object-view, residual guarded, non-dominant, terminal 96-byte, terminal long embedded marker-34/marker-2a/marker-04/marker-24, and guarded long embedded marker-25 paths are now live and preserve boundary metadata through segmentation so the body-shape analyzer keeps the evidence that proves each consumed body.
  - Use `Protocol03NestedMovementMode06LongTupleBodyPreAnchorLeadSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorBodySummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorOffsetSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorHeaderCandidateSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementSummaries`, `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementTrailerSummaries`, and `Protocol03NestedMovementMode06LongTupleBodyPreAnchorEmbeddedMovementTrailerShapeSummaries` as the first machine-readable queue for the remaining controls. The five repeated exact trailer families are already live under exact guards; the marker-2a outer-post-prefix mismatch row, repeated marker-24 guarded-state/object-view pre-anchor rows, singleton rows, and unsupported-selector rows stay as collision controls until more captures corroborate them.
- [ ] Follow up on prioritized packet resource leads.
  - Start with the P0 leads in `docs/PACKET-RESOURCE-LEADS.md`: RaGEZONE/Hardline Dreams combat packet logs, HD_Morpheus world-packet replay logs, and MxOEmu source/log holders. Then work the P1/P2 historical archive leads, including the Furious Angels 2009 Rajko proxy/SVN thread, for mirrors or local copies of early proxy source and packet logs. Request logs with object serialization, ability state, FX, and before/after attribute context.
- [ ] Use new packet evidence to implement vendor selling.
  - New buy/open/sell evidence should first be used to map the `CLIENT_VENDOR_BUY` and `CLIENT_VENDOR_SELL` sequence bytes plus related `5e`, `5f`, `80 e7`, `80 e4`, `81 0f`, and `81 12` follow-up packets. Final sell semantics still need known before/after cash and inventory state, but the checked-in corpus now has true top-level `CLIENT_VENDOR_SELL` anchors.
